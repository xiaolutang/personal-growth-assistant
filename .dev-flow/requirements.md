# 需求记录

> 项目：personal-growth-assistant
> 最后更新：2026-04-13

## 当前生效结论

- 当前范围：修复认证上线后历史内容在线上不可见的问题，补齐 `_default` 数据认领、已有账号回填和部署前数据校验。
- 当前约束：
  - 不修改已完成的 R002 认证主链路定义，新增缺陷修复任务闭环处理。
  - 数据恢复必须幂等，不覆盖已有用户数据。
  - 生产修复必须同时验证 SQLite、Markdown 和部署挂载路径。
- 当前未确认项：
  - 认领入口最终采用自动认领、显式确认，还是仅保留运维回填。

## 架构宪法

详见 `architecture.md`

## 历史需求

详见 `_archive/archive_index.md`
