# Evidence: F01

## Task Snapshot

- id: F01
- name: 通用骨架屏组件
- module: mobile/widgets
- verification: F1
- started_at: 2026-05-10

## Execution Evidence

### Implementation Summary

1. **SkeletonLoading widget** (`mobile/lib/widgets/skeleton_loading.dart`)
   - 支持 `SkeletonLayout.listCard` 和 `SkeletonLayout.textLine` 两种预设布局
   - shimmer 动画使用纯 AnimationController + AnimatedBuilder，1500ms 周期，ease-in-out 曲线
   - 使用 `Theme.of(context).colorScheme.surfaceContainerHighest` 而非硬编码灰色
   - 支持外部传入 `AnimationController`（可选），便于列表共享动画驱动
   - 深色模式自动适配（Theme.of(context) 自动获取当前 brightness 对应颜色）

2. **SkeletonList 便捷组件** (`mobile/lib/widgets/skeleton_loading.dart`)
   - 接受 `itemCount` 参数批量生成列表骨架
   - 共享单个 AnimationController，避免每项持有独立动画驱动
   - 默认 listCard 布局，可指定 textLine

3. **替换 _DigestSkeleton** (`mobile/lib/widgets/morning_digest_card.dart`)
   - 删除原有的 `_DigestSkeleton` 私有类（约 100 行）
   - 改用 `SkeletonLoading(layout: SkeletonLayout.listCard)` 替代
   - 消除代码重复，统一骨架屏实现

### Acceptance Criteria Check

| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
| 1 | SkeletonLoading widget 支持 list-card 和 text-line 两种预设布局 | PASS | `SkeletonLayout` 枚举 + `_buildListCard` / `_buildTextLine` 方法 |
| 2 | shimmer 动画流畅，1500ms 周期，使用 Theme.of(context) 而非硬编码灰色 | PASS | `AnimationController(duration: Duration(milliseconds: 1500))` + `surfaceContainerHighest` |
| 3 | 提供 SkeletonList 便捷组件，接受 itemCount 参数批量生成列表骨架 | PASS | `SkeletonList({required itemCount, layout})` |
| 4 | 深色模式下颜色自动适配 | PASS | `Theme.of(context).colorScheme.surfaceContainerHighest` 自动适配 dark/light |
| 5 | 无第三方 shimmer 包依赖，纯 AnimationController 实现 | PASS | 无新增 pubspec.yaml 依赖 |

### Tests Run

```
mobile/test/widgets/skeleton_loading_test.dart — 13 tests, all passed
mobile/test/widgets/morning_digest_card_test.dart — 8 tests, all passed (regression check)
```

Test scenarios:
- list-card 模式渲染 8 个 shimmer blocks
- text-line 模式渲染 3 个 shimmer blocks
- 主题颜色验证：shimmer 使用 surfaceContainerHighest，验证 RGB 通道匹配
- 深色模式验证：dark theme 下 shimmer 颜色使用 dark surfaceContainerHighest
- 1500ms 动画周期验证：alpha 在半周期变化、全周期回归
- SkeletonList 生成指定数量的骨架项
- SkeletonList 共享单个 AnimationController（同步 alpha 验证）

### Static Analysis

```
flutter analyze lib/widgets/skeleton_loading.dart test/widgets/skeleton_loading_test.dart
No issues found!
```

### Quality Gates

- flutter analyze: PASS (zero errors, zero warnings on changed files)
- flutter test skeleton_loading: PASS (13/13)
- flutter test morning_digest_card: PASS (8/8, no regression)
- No third-party shimmer packages added
