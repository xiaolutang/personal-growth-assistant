# B04 执行证据

## Task Snapshot

| 字段 | 值 |
|------|-----|
| Task ID | B04 |
| Phase | P2 |
| Module | data-isolation |
| Name | SQLite entries 表添加 user_id + 数据迁移 |
| Verification | L1 |
| Started At | 2026-04-13 |
| Completed At | 2026-04-13 |
| Evidence File | .dev-flow/evidence/B04.md |

## Architecture Constraints

- 所有数据操作必须携带 user_id
- 已有数据 user_id = '_default'
- 迁移脚本幂等
- 不破坏现有 API 兼容性

## Acceptance Criteria

1. entries 表新增 user_id 列（TEXT, NOT NULL）和索引 — **PASS**
2. 数据迁移：已有条目 user_id 设为 '_default' — **PASS**
3. list_entries/search/count_entries 等方法新增 user_id 参数 — **PASS**
4. create_entry 自动关联当前 user_id — **PASS**
5. 迁移脚本可重复执行（幂等） — **PASS**

## Execution Evidence

### 实现摘要

- 修改 `sqlite.py`：_migrate_schema 添加 user_id 列迁移（DEFAULT "_default"）
- 修改 `sqlite.py`：_init_db 添加 idx_entries_user_id 索引
- 修改 `sqlite.py`：upsert_entry/delete_entry/get_entry/list_entries/count_entries/search 全部添加 user_id 参数（默认 "_default"）
- 修改 `sqlite.py`：_build_filter_query 添加 user_id 过滤条件
- 修改 `sqlite.py`：search 的 FTS5 和 LIKE 回退都添加 user_id 过滤
- 新建 `test_sqlite_user_isolation.py`：9 个用户隔离测试

### Code Review

- routing: pass
- 所有验收条件通过
- Minor: sync_from_markdown 未传 user_id（当前阶段不影响，后续迭代处理）
- Minor: 迁移测试为间接验证（实际 ALTER TABLE DEFAULT 行为正确）

### 验证

```bash
uv run pytest tests/unit/storage/test_sqlite_user_isolation.py -v  # 9 passed
uv run pytest tests/unit/storage/test_storage_sqlite.py -v         # 15 passed
uv run pytest tests/unit/ -q                                       # 356 passed
```
