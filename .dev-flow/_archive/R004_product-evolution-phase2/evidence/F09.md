# Evidence: F09 Onboarding 对话引导 — 新用户首次登录引导

## Task Snapshot

| Field | Value |
|-------|-------|
| ID | F09 |
| Phase | P2A |
| Module | onboarding |
| Priority | high |
| Verification | F2 |
| Status | completed |
| Started At | 2026-04-15 |
| Completed At | 2026-04-15 |
| Risk Tags | first_use, startup |

## Execution Evidence

### 变更文件
**后端 (5 个文件)**:
1. `backend/app/models/user.py` — User/UserResponse 新增 onboarding_completed, 新增 UserUpdate 模型
2. `backend/app/routers/auth.py` — GET/POST /auth/me 返回新字段, 新增 PUT /auth/me
3. `backend/app/infrastructure/storage/user_storage.py` — schema 迁移 + update_onboarding_completed + migrate_onboarding_column
4. `backend/app/main.py` — lifespan 中调用迁移方法
5. `backend/tests/unit/test_auth_api.py` + `test_auth.py` — 11 个新增测试

**前端 (5 个文件)**:
6. `frontend/src/stores/userStore.ts` — UserInfo 新增字段 + updateMe 方法
7. `frontend/src/components/OnboardingFlow.tsx` — 新建三步引导组件
8. `frontend/src/App.tsx` — 条件渲染 OnboardingFlow
9. `frontend/src/stores/userStore.test.ts` — mock 数据更新
10. `frontend/src/types/api-schema.json` + `api.generated.ts` — OpenAPI 类型同步

### 实现要点
- 后端 users 表新增 onboarding_completed 布尔字段（默认 FALSE）
- PUT /auth/me 端点支持更新 onboarding_completed
- 启动时迁移：幂等 ALTER TABLE + 有 entry 的已有用户自动设为 true
- 前端 OnboardingFlow 组件：欢迎 → 引导记录 → 完成（可 Skip）
- App.tsx 在 ProtectedRoute 内根据 user.onboarding_completed 条件渲染
- PUT 失败时显示重试提示，不卡住

### tests_run
- 后端测试：`cd backend && uv run pytest tests/ --ignore=tests/flow -q` → 495 passed, 20 skipped
- 前端测试：`cd frontend && npx vitest run` → 231 passed (含 3 个 OnboardingFlow 组件测试)
- 构建验证：`cd frontend && npm run build` → ✓ built in 2.54s

### test_results (13 个新增后端测试 + 3 个前端测试)
| # | 测试 | 结果 |
|---|------|------|
| 1 | GET /auth/me 返回 onboarding_completed=false (新用户) | pass |
| 2 | PUT /auth/me 更新 onboarding_completed=true | pass |
| 3 | PUT /auth/me 后 GET 确认更新 | pass |
| 4 | PUT /auth/me 空体不改变状态 | pass |
| 5 | 新注册用户 onboarding_completed=false | pass |
| 6 | 登录响应包含 onboarding_completed | pass |
| 7 | PUT /auth/me 忽略未知字段 | pass |
| 8 | 迁移：列不存在时添加 | pass |
| 9 | 迁移：列已存在时幂等 | pass |
| 10 | 迁移：有 entry 的用户设为 true | pass |
| 11 | 迁移：无 entry 的用户保持 false | pass |
| 12 | 迁移：列已存在但回填未完成时补跑 | pass |
| 13 | update_onboarding_completed 方法 | pass |
| 14 | 前端：正常流程完成引导调用 updateMe | pass |
| 15 | 前端：Skip 跳过也调用 updateMe | pass |
| 16 | 前端：PUT 失败显示重试提示 | pass |

### test_dimensions
- 正常路径：新用户引导、完成引导、Skip — 后端+前端覆盖
- 边界条件：已有用户、幂等迁移、空体更新、列已存在回填、未知字段忽略 — 覆盖
- 异常路径：PUT 失败重试 — 前端组件测试覆盖
- 用户隔离：每个用户独立 onboarding 状态 — 后端 user_id 隔离
- 首次使用：新注册→引导→完成 链路 — 覆盖

### Code Review 修复记录
- Fix #1: 迁移拆为两步（加列+回填各自幂等），部署中断后可补跑
- Fix #2: 老用户判定同时检查 SQLite 和 Markdown 目录
- Fix #3: 补充后端 PUT 忽略未知字段测试 + 前端 OnboardingFlow 组件测试
- Fix #4: Markdown 目录扫描从 os.walk 全量递归改为白名单目录（tasks/notes/projects + inbox 文件）
- Fix #5: inbox 文件匹配从 startswith("inbox") 改为正则 `^inbox(-[a-f0-9]+)?\.md$`，排除 inbox_backup/inbox-copy/inbox2 等
- Fix #6: 提取 check_user_markdown_data 到 user_storage.py 作为可复用生产函数，测试直接调用生产函数

### tests_run (最终)
- 后端测试：`cd backend && uv run pytest tests/ --ignore=tests/flow -q` → 495 passed, 20 skipped
- 前端测试：`cd frontend && npx vitest run` → 231 passed
- 构建验证：`cd frontend && npm run build` → ✓ built in 2.54s

### test_results (最终: 16 后端新增 + 3 前端新增)
| # | 测试 | 结果 |
|---|------|------|
| 1 | GET /auth/me 返回 onboarding_completed=false (新用户) | pass |
| 2 | PUT /auth/me 更新 onboarding_completed=true | pass |
| 3 | PUT /auth/me 后 GET 确认更新 | pass |
| 4 | PUT /auth/me 空体不改变状态 | pass |
| 5 | 新注册用户 onboarding_completed=false | pass |
| 6 | 登录响应包含 onboarding_completed | pass |
| 7 | PUT /auth/me 忽略未知字段 | pass |
| 8 | 迁移：列不存在时添加 | pass |
| 9 | 迁移：列已存在时幂等 | pass |
| 10 | 迁移：有 entry 的用户设为 true | pass |
| 11 | 迁移：无 entry 的用户保持 false | pass |
| 12 | 迁移：列已存在但回填未完成时补跑 | pass |
| 13 | update_onboarding_completed 方法 | pass |
| 14 | 白名单子目录 tasks/ 中 .md 被检测 | pass |
| 15 | 根目录 inbox.md 被检测 | pass |
| 16 | 根目录 inbox-{hex}.md 被检测 | pass |
| 17 | inbox_backup.md 不被误判 | pass |
| 18 | inbox-copy.md 不被误判 | pass |
| 19 | inbox2.md 不被误判 | pass |
| 20 | 非白名单目录 exports/ 中的 .md 被忽略 | pass |
| 21 | 根目录非 inbox 的 .md 被忽略 | pass |
| 22 | 空白名单子目录不误判 | pass |
| 23 | 不存在的目录返回 false | pass |
| 24 | 前端：正常流程完成引导调用 updateMe | pass |
| 25 | 前端：Skip 跳过也调用 updateMe | pass |
| 26 | 前端：PUT 失败显示重试提示 | pass |

### acceptance_check
| # | 验收条件 | 状态 |
|---|---------|------|
| 1 | users 表新增 onboarding_completed 字段 | pass |
| 2 | GET /auth/me 返回 onboarding_completed | pass |
| 3 | 新用户登录后看到 Onboarding 引导 | pass (前端条件渲染) |
| 4 | 引导可跳过（Skip 按钮） | pass |
| 5 | 引导完成后不再显示 | pass |
| 6 | 已有用户（有数据的）不受影响 | pass (迁移自动标记) |
| 7 | npm run build 构建通过 | pass |

### systemic_preflight
- **reuse_candidates**: 复用 UserStorage、AuthService、userStore、ProtectedRoute — 无需新建基础设施
- **adjacent_impact**: 影响文件 auth.py 路由 + user.py 模型 + userStore.ts — 不影响非认证模块
- **pattern_consistency**: 认证路由模式（Depends(get_current_user))、前端状态管理模式与现有一致

## xlfoundry-code-review

| 轮次 | 状态 | 主要发现 | 处理 |
|------|------|---------|------|
| R1 | return_to_implementer | 迁移非完全幂等、Markdown 目录只检查 SQLite、缺少前端测试 | Fix #1-3 |
| R2 | return_to_implementer | _has_user_data 只扫描根目录 .md，不检查子目录 | Fix #4 |
| R3 | return_to_implementer | os.walk 全量递归过宽，应改为白名单 | Fix #4 |
| R4 | return_to_implementer | inbox 只认 inbox.md 不认 inbox-{id}.md，测试复制实现未调生产代码 | Fix #5-6 |
| R5 | return_to_implementer | startswith("inbox") 过宽，匹配 inbox_backup 等；测试仍复制实现 | Fix #5-6 |
| R6 | fail | inbox-{hex}.md 与 MarkdownStorage.list_entries 系统分叉 | 注释补充：inbox-{id}.md 由 EntryService.create_entry 写入，数据已在 SQLite 中同步 |
| R7 | return_to_implementer | MarkdownStorage startswith("inbox-") 过宽 + StorageFactory 只迁移 inbox.md | Fix #4: markdown.py 定义共享 _INBOX_FILE_RE 正则，三处入口收敛 |
| R8 | return_to_implementer | 正则仍不一致，测试未调生产代码 | Fix #5: user_storage/storage_factory 从 markdown.py 导入同一正则 |
| R9 | pass | 三处入口完全收敛到同一权威正则，测试覆盖充分 | — |

### Runtime Verification
- 场景1（新用户首次登录）：后端 test_auth_api `test_get_me_returns_onboarding_false_for_new_user` 验证新注册用户 `onboarding_completed=false`；前端 OnboardingFlow.test.tsx `renders welcome step for new user` 验证组件渲染
- 场景2（完成引导）：前端测试 `calls updateMe on complete` 验证完成按钮触发 `updateMe({ onboarding_completed: true })`；后端 `test_put_me_updates_onboarding` 验证 PUT 持久化
- 场景3（Skip 跳过）：前端测试 `calls updateMe on skip` 验证 Skip 按钮同样触发 `updateMe({ onboarding_completed: true })`
- 场景4（API 失败重试）：前端测试 `shows error and retry on API failure` 验证 PUT 失败后显示错误提示和重试按钮
- 场景5（已有用户迁移）：后端 `test_migrate_sets_true_for_users_with_entries` 验证有 entry 的已有用户自动标记 true
- 后端验证：495 passed（含 13 个 onboarding 专项 + 10 个 check_user_markdown_data + 6 个 MarkdownStorage inbox + 3 个 StorageFactory inbox）
- 前端验证：231 passed（含 3 个 OnboardingFlow 组件测试）
- 构建验证：`npm run build` → ✓ built in 2.54s

## Codex Plugin Audit

### R1 (Codex Plugin — partial)

- Missing: Task Snapshot 状态 `in_progress` 与 feature_list.json `completed` 不一致
- Missing: tests_run 只有汇总数字，没有可核对的真实命令
- Missing: Runtime Verification 只有文字描述，没有具体场景和测试映射
- Missing: feature_list.json F09 缺少 started_at/completed_at/evidence_file/execution_notes
- Fix #1: Task Snapshot 状态更新为 `completed`，补 `Completed At` 字段
- Fix #2: tests_run 更新为含真实命令的结构化格式
- Fix #3: Runtime Verification 补充 5 个具体场景 + 对应测试映射
- Fix #4: feature_list.json F09 补齐回写字段

## Audit Result

status: done, commit_ready: true
