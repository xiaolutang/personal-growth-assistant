# Evidence: B19 条目关联 API

## Task Snapshot

| Field | Value |
|-------|-------|
| ID | B19 |
| Phase | P2C |
| Module | entry-relations |
| Priority | medium |
| Verification | L2 |
| Status | completed |
| Started At | 2026-04-15 |
| Completed At | 2026-04-15 |

## Execution Evidence

### 改动文件

1. `backend/app/api/schemas/entry.py` — 新增 RelatedEntry + RelatedEntriesResponse
2. `backend/app/api/schemas/__init__.py` — 导出新 schema
3. `backend/app/services/entry_service.py` — 新增 get_related_entries 方法（三层关联）
4. `backend/app/infrastructure/storage/sqlite.py` — 新增 find_entries_by_tag_overlap 方法
5. `backend/app/routers/entries.py` — 新增 GET /{entry_id}/related 端点

### tests_run

| Suite | Command | Result |
|-------|---------|--------|
| Backend | `cd backend && uv run pytest tests/ --ignore=tests/flow -q` | 493 passed, 20 skipped |
| Frontend build | `cd frontend && npm run build` | PASS (built in 2.78s) |
| Frontend vitest | `cd frontend && npx vitest run` | 231 passed (18 files) |

### test_results

- 9 个 B19 专项测试 (test_related_entries.py):
  - `test_returns_siblings_when_same_parent` — 同项目条目优先返回，relevance_reason="同项目"
  - `test_tag_overlap_fills_when_no_siblings` — 无同项目时标签重叠补位，relevance_reason="标签相关"
  - `test_vector_similarity_fallback` — 无同项目、无标签重叠时混合搜索兜底，relevance_reason="搜索相关"
  - `test_max_5_results` — 最多返回 5 条
  - `test_empty_when_no_relations` — 无关联时返回空数组
  - `test_returns_none_when_entry_not_found` — 目标条目不存在时返回 None
  - `test_vector_failure_graceful` — 向量搜索失败时不报错，返回前两层结果
  - `test_user_id_isolation` — 不同用户 _verify_entry_owner 返回 False → 返回 None
  - `test_owner_verification_called_with_correct_user_id` — 验证 user_id 正确传入 _verify_entry_owner

### acceptance_check

| 验收条件 | 状态 | 证据 |
|----------|------|------|
| GET /entries/:id/related 返回关联列表（最多 5 条） | PASS | test_max_5_results: 8 siblings → 返回 5 条 |
| 每条包含 relevance_reason 字段 | PASS | 同项目="同项目"，标签重叠="标签相关"，搜索="搜索相关" |
| 关联优先级：同项目 > 标签重叠 > 搜索相关 | PASS | 三层完整：entry_service.py:143-191，优先级递减 |
| 数据按 user_id 隔离 | PASS | test_user_id_isolation: _verify_entry_owner False → None + test_owner_verification_called_with_correct_user_id: 断言 user_id 正确传入 |
| 无关联时返回空数组 | PASS | test_empty_when_no_relations: len(result.related) == 0 |
| 单元测试覆盖关联逻辑和隔离 | PASS | 9 个专项测试覆盖：同项目/标签/搜索/最多5条/空数组/不存在/失败降级/user_id隔离/owner验证 |

### systemic_preflight

- **reuse_candidates**: RelatedEntry schema 可被推荐系统复用；find_entries_by_tag_overlap 可被标签分析功能复用
- **adjacent_impact**: 新增 GET /entries/:id/related 端点不影响已有 CRUD 端点；三层关联逻辑在 get_related_entries 方法内，不污染其他 service 方法
- **pattern_consistency**: _verify_entry_owner 复用已有鉴权模式；list_entries + HybridSearchService 复用已有查询能力

## xlfoundry-code-review

### R1 (Codex Plugin — fail)

- 缺少第3层向量相似关联
- 缺少 B19 专门测试
- 修复：补 HybridSearchService 向量相似兜底 + graceful 降级
- 新增 7 个测试：同项目、标签重叠、搜索相关、最多5条、空数组、不存在404、搜索失败降级

### R2 (Codex Plugin — fail)

- 第3层用混合搜索而非纯向量，relevance_reason 应对齐
- 测试命名仍用"向量相似"旧表述

### R3 (Codex Plugin — conditional_pass)

- relevance_reason 改为"搜索相关"，准确反映混合搜索行为
- docstring、注释、测试命名统一对齐

### R4 (Codex Plugin — pass)

- 所有测试通过，命名与实现一致

## Audit Result

status: done, commit_ready: true
