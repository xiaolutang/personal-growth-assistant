# S001: R003 生产内容恢复 — 规划 Session

> 日期：2026-04-13
> 需求周期：R003
> 状态：规划中

## 用户输入

- 在 Claude Code 中已按计划执行数据迁移
- 发布到远程服务器 `https://xiaolutang.top/growth/` 后发现没有内容数据
- 当前使用账号为真实线上账号，不是 `_default`

## 需求分析

### 当前状态

- R002 已将历史数据迁移到 `_default`
- 线上代码中未发现 `_default` 数据自动认领或显式回填实现
- 部署配置存在 `../data:/app/data` 挂载，但现有发布验证只覆盖“服务是否可访问”

### 初步根因

1. **归属缺口**：历史数据停留在 `_default`，真实账号 `tangxiaolu` 登录后天然查不到这些数据。
2. **发布门禁缺口**：部署链路未校验远程 `DATA_DIR` 内是否存在有效内容、是否与目标账号映射一致。

### 修复方向

1. 补 `_default` 到真实用户的认领/回填能力。
2. 补部署前 dry-run 和部署后 smoke，校验数据卷、SQLite、Markdown 用户目录。
3. 将本次线上逃逸写入 `defect_feedback.md`，作为后续迁移类需求模板约束。

## 任务概览

| Phase | 任务 | 类型 |
|-------|------|------|
| P6 | S03 契约与验收定义 | 规划/缺陷回流 |
| P6 | B12 数据认领与回填服务 | 后端修复 |
| P6 | B13 部署探针与运维命令 | 运维修复 |
| P6 | S04 生产恢复 smoke | 集成验证 |

总计 4 个新增任务。

## 规划说明

- 本轮沿用现有 `workflow.mode = A`
- 保持 `runtime = team_orchestrated`
- 当前未执行独立 plan-review，需在进入执行前补一次审查
