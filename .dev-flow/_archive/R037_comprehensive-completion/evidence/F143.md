# F143: 多选框架

## 状态：已完成

## 验收标准

| # | 验收标准 | 状态 | 证据 |
|---|---------|------|------|
| 1 | Explore 页编辑按钮进入多选模式 | PASS | Explore.tsx L140: 编辑按钮调用 `batch.enterSelectMode` |
| 2 | checkbox 可见且可点击 | PASS | TaskCard L154-161: selectable 时渲染 CheckSquare/Square，点击卡片触发 onSelect |
| 3 | 单卡动作在多选模式下禁用 | PASS | TaskCard L169: 状态按钮 disabled; L223: inbox菜单不渲染; L258: 删除按钮不渲染 |
| 4 | 选中计数实时更新 | PASS | BatchActionBar 显示 `selectedIds.size`，useBatchOperations toggleSelect 更新 Set |
| 5 | ESC 退出多选模式 | PASS | useBatchOperations L47-54: keydown 监听 Escape 调用 exitSelectMode |
| 6 | Tab 切换不清空选择状态 | PASS | handleTabChange 不调用 exitSelectMode，selectMode/selectedIds 独立于 tab 状态 |

## 实现分析

多选框架在 F120 阶段已完整实现，包含：
- `useBatchOperations` hook: selectMode/selectedIds 状态管理、ESC 监听、全选
- `BatchActionBar` 组件: 底部操作栏（转笔记、转灵感、删除）
- `TaskCard` 组件: selectable/selected/onSelect/disableActions props
- `TaskList` 组件: 透传 selectable props 到 TaskCard
- `Explore.tsx` 页面: 编辑按钮、取消按钮、全选按钮

本次任务无需修改功能代码，仅需补充测试。

## 测试覆盖

### useBatchOperations.test.ts (新增 2 条)
- ESC 键退出多选模式并清空选中
- ESC 不在多选模式时无效

### TaskCard.select.test.tsx (新增 9 条)
- checkbox 可见性（selectable=true/false/selected=true）
- checkbox 可点击（selectable 模式点击触发 onSelect）
- disableActions（状态按钮禁用、删除按钮不渲染、inbox菜单不渲染）

### TaskList.select.test.tsx (新增 4 条)
- selectable/selectedIds/onSelect/disableActions 透传
- 点击未选中任务触发 onSelect
- 空状态渲染
- disableActions 禁用状态按钮

## 测试结果

```
Test Files:  3 passed (3)
Tests:      25 passed (25)
Build:      SUCCESS
```

## 文件变更

| 文件 | 变更 |
|------|------|
| `frontend/src/pages/explore/__tests__/useBatchOperations.test.ts` | 新增 ESC 退出测试 |
| `frontend/src/components/__tests__/TaskCard.select.test.tsx` | 新建 - TaskCard 多选测试 |
| `frontend/src/components/__tests__/TaskList.select.test.tsx` | 新建 - TaskList 多选测试 |

## Code Review: PASS
## Audit: DONE
