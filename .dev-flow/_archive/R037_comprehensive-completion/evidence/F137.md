# F137 Review 加载态

## 任务快照

- **任务ID**: F137
- **模块**: ux-polish
- **验证级别**: F2
- **描述**: Review 页数据加载中显示 spinner，加载失败显示错误提示 + 重试按钮，空数据状态有友好提示。

## 验收条件

- [x] Review 页数据加载中显示 spinner
- [x] 加载失败显示错误提示 + 重试按钮
- [x] 空数据状态有友好提示
- [x] build 无 TS 错误

## Execution Evidence

### tests_run

```bash
cd frontend && npx vitest run src/pages/__tests__/Review.loading.test.tsx --reporter=verbose 2>&1
cd frontend && npm run test:run 2>&1 | tail -5
cd frontend && npm run build 2>&1 | tail -3
```

### test_results

- F137 专项测试: 7 passed (Review.loading.test.tsx)
  - 数据加载中显示 spinner 和加载文案
  - 加载失败显示错误提示和重试按钮
  - 点击重试按钮会重新加载数据
  - zero-filled 报告数据显示空数据友好提示
  - 有 taskStats 时正常显示任务统计卡片
  - 成功加载后刷新失败不残留旧的任务统计卡片
  - 切换 tab 时 goalSummary 被清除，不残留旧目标数据
- vitest 全量: 491 passed (46 files)
- build: 成功，无 TS 错误

### acceptance_check

| # | 验收条件 | 状态 | 证据 |
|---|---------|------|------|
| 1 | Review 页数据加载中显示 spinner | pass | Loader2 + "加载报告数据..." 文案 (Review.tsx:137-141) |
| 2 | 加载失败显示错误提示 + 重试按钮 | pass | AlertCircle 图标 + 错误文案 + 重试按钮 (Review.tsx:142-149) |
| 3 | 空数据状态有友好提示 | pass | Inbox 图标 + "暂无报告数据" + 引导文案 (Review.tsx:150-159) |
| 4 | build 无 TS 错误 | pass | npm run build 成功，无 TS 错误 |

### systemic_preflight

| 维度 | 状态 | 说明 |
|------|------|------|
| reuse | pass | 复用 Loader2/AlertCircle/Inbox 图标，空状态样式与 Home.tsx 保持一致 |
| adjacent_impact | pass | 趋势页(trend)不受影响，useReportData hook 只影响日报/周报/月报 |
| pattern_consistency | pass | 与 ServiceUnavailable.tsx 风格一致（圆形图标背景 + 文案 + 按钮） |
| stale_state | pass | refetch 前清除旧 report 数据 + goalSummary，防止混合状态 |

### code_review

- **code_review_status**: pass (第二轮通过)
- **第一轮 findings**:
  - High: 空状态分支不可达 → 修复：改用 isEmpty (zero-filled 判断)
  - Medium: 失败刷新残留旧数据 → 修复：refetch 前清除 report + goalSummary
  - Medium: 空 mock 不符合合约 → 修复：使用 emptyDailyReport()
- **第二轮 findings**:
  - Medium: goalSummary 未随 tab 切换清除 → 修复：useEffect 中 refetch 前清除

### audit

- **audit_status**: done
- **audit_commit_ready**: true
- **next_action**: null

## 实现变更

### files_changed

1. `frontend/src/pages/Review.tsx` — 增强 loading spinner(添加文案)、改善 error UI(圆形图标背景)、新增空数据友好提示、使用 isEmpty 替代 null 检查
2. `frontend/src/pages/review/useReportData.ts` — refetch 前清除旧 report 数据 + goalSummary，新增 isEmpty 派生值
3. `frontend/src/pages/__tests__/Review.loading.test.tsx` — 新增 7 个测试覆盖 spinner、error+retry、空数据、stale state 回归
