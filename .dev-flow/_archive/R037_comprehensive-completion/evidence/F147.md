# F147 笔记双链引用前端 — Evidence

## 任务信息
- task_id: F147
- module: note-link
- verification: F2
- depends_on: [B107]
- commit: 00c5447
- review_fix_commit: fe4d2ea

## 验收条件逐项确认

| # | 条件 | 状态 | 说明 |
|---|------|------|------|
| 1 | ContentSection 编辑区输入 [[ 触发引用补全弹窗 | PASS | `detectLinkTrigger` 检测 `[[` 触发，`handleContentChange` 异步加载笔记列表并展示补全弹窗 |
| 2 | 补全数据源复用 GET /entries?filter_type=note | PASS | `loadNotes()` 调用 `getEntries({ type: "note", limit: 200 })` |
| 3 | 补全列表按标题匹配度排序 | PASS | `filteredItems` 按 title.startsWith > title.includes > 其他排序 |
| 4 | 选中补全项插入 `[[note-id|标题]]` 格式 | PASS | `handleSelectItem` 生成 `[[${item.id}|${item.title}]]` |
| 5 | API 失败静默降级 | PASS | `completionError` 为 true 时 `showCompletion && !completionError` 隐藏弹窗 |
| 6 | 空结果显示「无匹配笔记」 | PASS | `filteredItems.length === 0` 时展示提示文字 |
| 7 | EntryDetail 底部展示反向引用列表 | PASS | `getBacklinks` API 获取数据，`backlinks.length > 0` 时渲染 Card |
| 8 | 反向引用可点击跳转 | PASS | `onClick={() => navigate(`/entry/${bl.id}`)}` |
| 9 | build 无 TS 错误 | PASS | `tsc -b && vite build` 成功 |
| 10 | vitest 覆盖补全和反向引用展示 | PASS | 13 个新测试全部通过（ContentSection 9 个 + BacklinksPanel 4 个） |

## 测试覆盖

### tests_run
- frontend: 55 files, 597 tests passed
- backend: 1180 tests passed

### test_results
- ContentSection.test.tsx: 9 passed
  - 输入 `[[` 触发补全
  - 补全列表按标题匹配度排序（含渲染顺序断言）
  - 选中补全项插入 `[[note-id|标题]]` 格式（含字符串精确断言）
  - 空结果显示「无匹配笔记」
  - API 失败静默降级
  - Escape 关闭补全弹窗
  - 预览模式有引用时展示列表
  - 预览模式无引用时隐藏
  - 引用笔记可点击跳转
- BacklinksPanel.test.tsx: 4 passed
  - 有反向引用时展示列表
  - 无反向引用时隐藏面板
  - 反向引用可点击跳转
  - API 失败时静默降级

### test_dimensions
- happy_path: covered (trigger, select, navigate)
- failure_branch: covered (API error silent fallback)
- empty_data: covered (no matching notes, no backlinks)
- boundary: covered (dedup via Set, stale response guard via activeTriggerRef)

## runtime_verification
- type: unit_test (F2 verification via vitest)
- status: passed
- note: 无独立运行时环境，通过 vitest jsdom 环境模拟验证

## quality_gates
- code_review: codex xlfoundry-code-review pass (conditional_pass, 2 medium data/ tracking issues non-functional)
- review_fixes: fe4d2ea — 竞态保护/去重/补全弹窗陈旧异步/helper提取/测试补强

## systemic_preflight
- reuse: getDueDateInfo shared helper extracted (TaskCard + EntryHeader)
- adjacent_impact: EntryDetail loadBacklinks race condition guard added
- pattern_consistency: activeTriggerRef pattern consistent with backlinksVersionRef

## 变更文件

| 文件 | 操作 |
|------|------|
| `frontend/src/services/api.ts` | 新增 `BacklinkItem`、`BacklinksResponse` 类型和 `getBacklinks` 函数 |
| `frontend/src/pages/entry-detail/ContentSection.tsx` | 新增 `[[` 触发补全弹窗完整逻辑 + 陈旧异步防护 |
| `frontend/src/pages/EntryDetail.tsx` | 新增反向引用面板 + `getBacklinks` 调用 + 竞态保护 |
| `frontend/src/pages/entry-detail/useRelatedEntries.ts` | 更新正则支持 `[[id|title]]` 格式 + Set 去重 |
| `frontend/src/pages/entry-detail/useEntryData.ts` | 更新正则支持 `[[id|title]]` 格式 |
| `frontend/src/pages/entry-detail/__tests__/ContentSection.test.tsx` | 新增 9 个测试 |
| `frontend/src/pages/entry-detail/__tests__/BacklinksPanel.test.tsx` | 新增 4 个测试 |

## 构建和测试结果

```
npm run build: 成功 (tsc + vite build, 0 errors)
npm run test:run: 55 files, 597 tests passed
pytest: 1180 passed, 1 warning
```
