# B107: 笔记双链引用后端

## 状态: DONE

## 验收条件覆盖

1. **解析 [[note-id]] 简写语法** -- `SQLiteStorage.parse_wikilinks()` 使用正则 `\[\[([^\]|]+?)(?:\|[^\]]*?)?\]\]` 提取引用
2. **解析 [[note-id|显示标题]] 完整语法** -- 同一正则支持两种语法，提取 note-id 部分
3. **SQLite 新增 note_references 表** -- (source_id, target_id, user_id) 联合主键，含索引
4. **GET /entries/{id}/backlinks** -- 返回反向引用列表，含 id/title/category
5. **创建/更新条目时自动解析并更新引用** -- `create_entry` 和 `update_entry` 中调用 `_update_note_references`
6. **删除条目时清理引用关系** -- `delete_entry` 中调用 `delete_note_references`（双向清理）
7. **reindex_backlinks(user_id) 方法** -- `EntryService.reindex_backlinks()` 扫描所有条目重建引用
8. **延迟初始化** -- `get_backlinks` 首次调用时检查 `is_backlinks_indexed`，未索引则自动触发 reindex
9. **单元测试** -- 23 个测试全部通过

## 修改文件

- `backend/app/infrastructure/storage/sqlite.py` -- 新增 note_references 表、backlinks_index_status 表、parse_wikilinks/upsert_note_references/delete_note_references/get_backlinks/is_backlinks_indexed/mark_backlinks_indexed 方法
- `backend/app/services/entry_service.py` -- 新增 _update_note_references/get_backlinks/reindex_backlinks 方法，create/update/delete 中集成引用管理
- `backend/app/routers/entries.py` -- 新增 GET /entries/{id}/backlinks 路由
- `backend/app/api/schemas/entry.py` -- 新增 BacklinkItem/BacklinksResponse schema
- `backend/app/api/schemas/__init__.py` -- 导出新增 schema
- `backend/tests/unit/api/test_note_backlinks.py` -- 23 个单元测试

## 测试结果

```
tests/unit/api/test_note_backlinks.py ....................... (23 passed)
全量测试: 1279 passed, 20 skipped, 1 warning
```
