# F140 — NotificationCenter 时间戳 + 后台轮询

## 变更文件

| 文件 | 操作 |
|------|------|
| `frontend/src/components/NotificationCenter.tsx` | 修改：轮询逻辑改为 open?60s:300s |
| `frontend/src/components/__tests__/NotificationCenter.test.tsx` | 新增：11 个测试用例 |

## 验收条件验证

1. **每条通知显示相对时间戳** — 已有 `formatRelativeTime` 函数 + 渲染；测试覆盖"刚刚"/分钟/小时/天级
2. **面板关闭时 300s 后台轮询** — useEffect 无条件注册 setInterval，open=false 时 intervalMs=300_000；测试验证 299s 不触发、300s 触发
3. **面板展开时 60s 高频轮询** — open=true 时 intervalMs=60_000；测试验证 59s 不触发、60s 触发
4. **组件卸载清理定时器** — useEffect return clearInterval；测试验证 unmount 后推进 600s 不再调用
5. **build 无 TS 错误** — `npm run build` 通过

## 测试结果

```
✓ 11 tests passed
```

## Build 结果

```
✓ tsc -b && vite build — 成功
```
