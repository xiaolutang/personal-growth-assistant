# F03 执行证据

| Task ID | F03 |
| Phase | P4 |
| Name | 路由守卫 + API 拦截器 |

## 实现摘要

- 创建 `frontend/src/components/ProtectedRoute.tsx`
  - 未登录跳转 /login，已登录渲染子路由
- 修改 `frontend/src/lib/uid.ts` → `initFetchInterceptor()`
  - 自动注入 Authorization: Bearer {token} header
  - 自动注入 X-UID header（真实 user.id 或匿名 ID）
  - 401 响应自动清除 token 并跳转 /login
- 修改 `frontend/src/App.tsx`
  - /login 和 /register 路由不需要守卫
  - 其他路由被 ProtectedRoute 包裹
  - 启动时调用 initFetchInterceptor 和 loadFromStorage

## 验证

```bash
npm run build  # 构建成功
npm run test:run  # 177 passed
```
