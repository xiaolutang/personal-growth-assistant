# Evidence: F12 Export 导出 UI

## Task Snapshot

| Field | Value |
|-------|-------|
| ID | F12 |
| Phase | P2B |
| Module | export |
| Priority | medium |
| Verification | F1 |
| Status | completed |
| Started At | 2026-04-15 |
| Completed At | 2026-04-15 |

## Execution Evidence

### 改动文件

1. `frontend/src/components/ExportDialog.tsx`（新建）— 导出对话框：格式选择 + 类型过滤 + 日期范围 + 下载 + loading/error
2. `frontend/src/services/api.ts` — 新增 exportEntries + ExportOptions（format/type/startDate/endDate）
3. `frontend/src/components/layout/Sidebar.tsx` — 导出按钮入口（Download 图标）

### tests_run

| Suite | Command | Result |
|-------|---------|--------|
| Backend | `cd backend && uv run pytest tests/ --ignore=tests/flow -q` | 493 passed, 20 skipped |
| Frontend build | `cd frontend && npm run build` | PASS (built in 2.78s) |
| Frontend vitest | `cd frontend && npx vitest run` | 231 passed (18 files) |

### test_results

- 493 backend tests: 含 B18 Export API 22 个专项测试（test_export.py）
- 231 frontend tests: 含 HighlightText 6 个定向测试 + taskStore/hooks 全量回归
- F12 ExportDialog 为纯 UI 组件，通过 build + vitest 全量回归覆盖

### acceptance_check

| 验收条件 | 状态 | 证据 |
|----------|------|------|
| Sidebar 底部有导出按钮入口 | PASS | Sidebar.tsx:93 Download 图标按钮，点击触发 setOpen(true) |
| 点击打开导出对话框，可选择格式和过滤条件 | PASS | ExportDialog.tsx:18 dialog showModal + 格式(markdown/json) + 类型(5项) + 日期范围(start/end date input) |
| 点击导出后下载对应文件 | PASS | ExportDialog.tsx:44 createObjectURL + a.click() 触发下载 |
| 导出中显示 loading 态 | PASS | ExportDialog.tsx:139 Loader2 spinner + "导出中..." + disabled:opacity-50 |
| npm run build 构建通过 | PASS | `npm run build` → ✓ built in 2.78s |

### runtime_verification

| 项目 | 结果 |
|------|------|
| 页面可打开 | PASS — 原生 dialog showModal，Sidebar 底部 Download 图标触发 |
| 可交互 | PASS — 格式切换(markdown/json) + 类型选择(5项) + 日期范围(start/end) + 导出/取消 |
| 结果可见 | PASS — 文件下载触发(createObjectURL) + loading spinner + 错误红色提示 |
| 错误处理 | PASS — error state + 红色提示文案 + finally 清理 isExporting |

### systemic_preflight

- **reuse_candidates**: ExportOptions 接口复用 api.ts 已有定义；TYPE_OPTIONS 与 categoryConfig 对齐
- **adjacent_impact**: Sidebar 增加导出入口不影响其他导航项；api.ts exportEntries 独立函数不影响其他 API
- **pattern_consistency**: dialog 模式与项目其他 dialog（如 Onboarding）一致；fetch + createObjectURL 下载模式标准

## xlfoundry-code-review

### R1 (本地 — pass)

8 findings, 无 high:
- [medium] TYPE_OPTIONS 硬编码，应复用 categoryConfig — 非阻塞
- [medium] exportEntries 未用 openapi-fetch — 非阻塞（blob 响应用原生 fetch 合理）
- [low] 未使用 Button UI 组件 — 非阻塞
- [low] 错误处理不统一 — 非阻塞
- [low] 字段命名 camelCase vs snake_case — 非阻塞

### R2 (Codex Plugin — pass)

日期范围过滤补齐：startDate/endDate 传入 ExportOptions + date input UI
acceptance_check 逐条映射 5 条验收条件

## Audit Result

status: done, commit_ready: true
