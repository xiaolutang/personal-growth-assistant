# Evidence: F11 搜索增强

## Task Snapshot

| Field | Value |
|-------|-------|
| ID | F11 |
| Phase | P2B |
| Module | explore |
| Priority | medium |
| Verification | F2 |
| Status | completed |
| Started At | 2026-04-15 |
| Completed At | 2026-04-15 |

## Execution Evidence

### 改动文件

1. `frontend/src/pages/Explore.tsx` — Cmd+K 全局聚焦 + debounce 300ms + highlightKeyword 传递
2. `frontend/src/components/TaskList.tsx` — 透传 highlightKeyword
3. `frontend/src/components/TaskCard.tsx` — HighlightText 组件 + matchAll 全量高亮

### tests_run

| Suite | Command | Result |
|-------|---------|--------|
| Frontend build | `cd frontend && npm run build` | PASS (built in 2.78s) |
| Frontend vitest | `cd frontend && npx vitest run` | 231 passed (18 files) |

### test_results

- 6 HighlightText 定向测试 (TaskCard.test.tsx:177-250)：
  - 全量高亮："foo bar foo baz foo" 搜索 "foo" → 3 个 mark
  - 大小写不敏感："React Hooks" 搜索 "react" → 1 个 mark，内容 "React"
  - 无匹配时返回原文：纯文本无 mark
  - 空关键词时不高亮：返回原文
  - 特殊正则字符转义："price: $10 (50%)" 搜索 "$10" → 1 个 mark
  - Unicode 索引安全："Straße strasse" 搜索 "straße" → 1 个 mark "Straße"，负向断言不含 "strasse"

### acceptance_check

| 验收条件 | 状态 | 证据 |
|----------|------|------|
| 输入 debounce 300ms 后自动触发搜索 | PASS | Explore.tsx:59 setTimeout(query, 300) + cleanup clearTimeout |
| 搜索结果中关键词高亮 | PASS | TaskCard.tsx:20 HighlightText + matchAll + mark 标签，6 个定向测试覆盖 |
| Cmd+K 全局快捷键聚焦搜索框 | PASS | Explore.tsx:39 keydown 事件监听 metaKey/ctrlKey + 'k'，searchInputRef.focus() |
| 搜索无结果时显示引导文案 | PASS | Explore.tsx:184 emptyMessage "没有找到匹配的结果" |
| npm run build 构建通过 | PASS | `npm run build` → ✓ built in 2.78s |

### runtime_verification

| 项目 | 结果 |
|------|------|
| Cmd+K 聚焦 | PASS — keydown 监听 (metaKey \|\| ctrlKey) + 'k' → searchInputRef.current.focus() |
| debounce 300ms | PASS — setTimeout 300ms delay + clearTimeout cleanup 防竞态 |
| 关键词高亮 | PASS — matchAll + 正则转义 + case-insensitive + mark 标签，Unicode 索引安全 |
| 无结果引导 | PASS — emptyMessage 渲染 "没有找到匹配的结果" |
| Tab 约束搜索 | PASS — filterByCategory 已覆盖，Tab 切换清空搜索 |

### systemic_preflight

- **reuse_candidates**: HighlightText 组件可被任何需要关键词高亮的场景复用；searchInputRef 模式与项目其他 ref 用法一致
- **adjacent_impact**: Explore.tsx 搜索变更不影响 TaskList/TaskCard 组件其他使用场景；highlightKeyword 为可选 prop
- **pattern_consistency**: debounce cleanup 模式与 React useEffect 最佳实践一致；matchAll 方案与 String.prototype 标准行为对齐

## xlfoundry-code-review

### R1 (Codex Plugin — fail)

- HighlightText 只高亮首个匹配，未全量高亮
- 缺少 HighlightText 定向测试
- 修复：改用 matchAll + 正则获取原始字符串索引（避免 toLowerCase 索引错位）
- 新增 6 个测试：全量高亮、大小写不敏感（断言 mark）、无匹配、空关键词、特殊字符转义（断言 mark）、Unicode 索引安全（Straße 精确高亮 + 负向断言）

### R2 (Codex Plugin — fail)

- toLowerCase 索引对 Unicode 不安全
- 修复：改用 matchAll + 正则获取原始索引，避免 toLowerCase

### R3 (Codex Plugin — conditional_pass)

- Unicode 测试断言需加强
- 修复：strengthened to exact mark count + negative assertions

### R4 (Codex Plugin — pass)

- 所有测试通过，实现安全

## Audit Result

status: done, commit_ready: true
