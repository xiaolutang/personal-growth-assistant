# B08 执行证据

| Task ID | B08 |
| Phase | P2 |
| Name | SyncService + EntryService 传递用户上下文 |

## 改动

- SyncService.sync_entry(user_id)
- SyncService.sync_to_graph_and_vector(user_id)
- SyncService._sync_to_neo4j(user_id)
- SyncService.delete_entry(user_id)
- EntryService 所有 CRUD + 查询方法增加 user_id 参数
- 所有存储层调用传递 user_id

## 验证

```bash
uv run pytest tests/unit/ -q  # 363 passed
```
