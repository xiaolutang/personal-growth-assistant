# B11 执行证据

| Task ID | B11 |
| Phase | P3 |
| Name | 所有路由添加认证守卫 |

## 实现摘要

- 8 个路由文件全部添加 `user: User = Depends(get_current_user)` 认证守卫：
  - entries.py — 7 个 endpoint，传递 user.id 到 EntryService
  - search.py — 1 个 endpoint，传递 user.id 到 Qdrant
  - knowledge.py — 3 个 endpoint，传递 user.id 到 KnowledgeService → Neo4j
  - intent.py — 1 个 endpoint（auth guard only）
  - parse.py — 已在 B09 完成
  - review.py — 3 个 endpoint（auth guard only）
  - feedback.py — 1 个 endpoint（auth guard only）
  - playground.py — 1 个 endpoint（auth guard only）
- KnowledgeService.get_knowledge_graph/get_related_concepts/get_learning_path 添加 user_id 参数
- KnowledgeService._analyze_sqlite_data/_recommend_from_tags 添加 user_id 参数
- reset_all_services 不再清除 _user_storage（避免测试中认证失效）
- conftest.py 的 client fixture 自动注入测试用户 token
- test_entries_503.py 更新为验证认证拦截

## 验证

```bash
uv run pytest tests/unit/ -q  # 371 passed
```
