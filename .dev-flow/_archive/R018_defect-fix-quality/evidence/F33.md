# F33: 增强 AI 晨报前端展示

## Task Snapshot

- **ID**: F33
- **Phase**: P2
- **Module**: home
- **Priority**: medium
- **Verification**: F2
- **Depends on**: B44 (completed)

### Acceptance Criteria

1. 晨报卡片顶部显示学习连续天数（火焰图标 + N 天），0 天时不显示
2. 新增「今日聚焦」卡片：显示 daily_focus.title + description，可点击跳转到 target_entry
3. 新增「洞察」区域：显示 pattern_insights 列表（最多 3 条）
4. 连续天数 ≥7 天时火焰变大/变色，视觉奖励
5. 向后兼容：新字段缺失时不报错，相关区域不显示

---

## Execution Evidence

### 实现文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `frontend/src/services/api.ts` | 修改 | 新增 DailyFocus 类型 + MorningDigestResponse 扩展字段 |
| `frontend/src/pages/Home.tsx` | 修改 | 晨报卡片增加连续天数、今日聚焦、洞察区域 |

### 验收标准逐条验证

1. **学习连续天数** ✅ — `(digest.learning_streak ?? 0) > 0` 时显示 Flame 图标 + "连续学习 N 天"
2. **今日聚焦** ✅ — `digest.daily_focus` 存在时显示 Target 图标 + title + description，可点击跳转
3. **模式洞察** ✅ — `digest.pattern_insights` 非空时显示 Eye 图标 + 洞察列表
4. **视觉奖励** ✅ — `learning_streak >= 7` 时 Flame 图标变大（h-5 vs h-4）+ 橙色加深 + "太棒了！" 提示
5. **向后兼容** ✅ — 所有新字段使用 `??` / `?.` 可选链，缺失时不显示对应区域

### 测试结果

- 前端构建: 成功
- 前端测试: 231 passed
