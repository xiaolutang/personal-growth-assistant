# B14 — 回顾趋势 API + review user_id 修复

## Task Snapshot

- **ID**: B14
- **Phase**: P1A
- **Module**: review
- **Verification**: L2
- **Priority**: high
- **Depends on**: S05 (completed)
- **Risk Tags**: auth
- **Smoke Required**: true

## Acceptance Criteria

1. ReviewService.get_daily_report/weekly/monthly 均接受 user_id 参数并传递到 list_entries
2. review.py 路由层通过 user.id 传递到 service 方法
3. GET /review/trend?period=daily&days=7 返回每日统计数组
4. GET /review/trend?period=weekly&weeks=8 返回每周统计数组
5. 返回结构：{ periods: [{ date, total, completed, completion_rate, notes_count }] }
6. 空数据返回空数组 periods: []
7. 用户隔离：不同用户只能看到自己的统计数据
8. uv run pytest backend/tests/ 全部通过

## Execution Evidence

### Changes

1. **review_service.py** — 所有方法 (get_daily_report/get_weekly_report/get_monthly_report) 新增 `user_id: Optional[str] = None` 参数，传递到所有 `list_entries()` 调用；新增 `get_trend_data()` 方法支持 daily/weekly 趋势数据
2. **review.py** — 所有路由传递 `user_id=user.id`；新增 `GET /review/trend` 路由，支持 period/days/weeks 参数，含 422 校验
3. **test_review_api.py** — 修复旧测试数据隔离问题（setup_data 带 user_id）；新增 TestTrendAPI 类（9 个测试覆盖正常/边界/异常/隔离/无认证场景）

### tests_run

| Command | Result |
|---------|--------|
| `cd backend && uv run pytest tests/unit/api/test_review_api.py -v` | 18 passed |
| `cd backend && uv run pytest tests/unit/ tests/integration/ -q` | 418 passed, 20 skipped |

### Acceptance Check

| # | Criteria | Status |
|---|----------|--------|
| 1 | 所有方法接受 user_id 并传递 | ✅ review_service.py |
| 2 | 路由层传 user.id | ✅ review.py |
| 3 | GET /review/trend?period=daily&days=7 | ✅ test_trend_daily_default + test_trend_daily_custom_days |
| 4 | GET /review/trend?period=weekly&weeks=8 | ✅ test_trend_weekly |
| 5 | 返回结构正确 | ✅ TrendPeriod(date,total,completed,completion_rate,notes_count) |
| 6 | 空数据返回空 periods | ✅ test_trend_empty_data |
| 7 | 用户隔离 | ✅ test_trend_user_isolation |
| 8 | 后端测试通过 | ✅ 418 passed |

## Code Review Result

- Provider: codex_plugin (xlfoundry-code-review)
- Status: pass (L2 任务，实现简洁，测试覆盖充分)
- reviewed_at: 2026-04-14

## Audit Result

（待审计）
