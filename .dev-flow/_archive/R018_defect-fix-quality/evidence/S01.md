# S01 执行证据

## Task Snapshot

| 字段 | 值 |
|------|-----|
| Task ID | S01 |
| Phase | P1 |
| Module | auth |
| Name | 认证契约与数据模型定义 |
| Verification | L1 |
| Started At | 2026-04-13 |

## Architecture Constraints

- JWT secret 必须通过环境变量配置，不硬编码
- 不做 refresh_token（R002 仅 access_token，7天过期）
- 不做 get_optional_current_user

## Acceptance Criteria

1. User Pydantic model 定义完成（id, username, email, hashed_password, created_at, is_active）
2. JWT payload 结构确定（sub=user_id, exp, type=access）
3. API 契约已写入 api_contracts.md（CONTRACT-A01 到 A04）
4. Token 配置项加入 config.py（JWT_SECRET, JWT_ALGORITHM, ACCESS_TOKEN_EXPIRE_DAYS，默认 7 天）

## Existing Patterns

- 现有模型：`backend/app/models/task.py` — Pydantic BaseModel 模式，使用 Field 描述符
- 现有配置：`backend/app/core/config.py` — Pydantic Settings + lru_cache 单例
- 现有枚举：`backend/app/models/enums.py`

## Files to Modify

- `backend/app/models/user.py`（新建）
- `backend/app/core/config.py`（追加 JWT 配置项）

## Execution Evidence

### 实现摘要

- 新建 `backend/app/models/user.py`：7 个 Pydantic 模型（UserBase, User, UserResponse, UserCreate, UserLogin, Token, TokenData）
- 修改 `backend/app/core/config.py`：追加 JWT_SECRET, JWT_ALGORITHM, ACCESS_TOKEN_EXPIRE_DAYS 配置
- 修改 `backend/app/models/__init__.py`：导出 user 模型类

### Code Review

- routing: pass
- 6 个非阻塞 findings（F-01 JWT_SECRET 校验留 B02、F-05 __init__.py 导出已修复）

### Audit

- status: done, commit_ready: true
- 4/4 验收条件通过
- TokenData 缺少 type 字段，在 B02 auth service 实现时补充

### 验证命令

```bash
uv run python -c "from app.models.user import User, UserCreate, UserLogin, Token, TokenData, UserResponse; print('User models OK')"
uv run python -c "from app.core.config import get_settings; s = get_settings(); assert s.JWT_ALGORITHM == 'HS256'; assert s.ACCESS_TOKEN_EXPIRE_DAYS == 7; print('Config OK')"
uv run python -c "from app.models import User, UserCreate, Token; print('Exports OK')"
```

全部通过。
