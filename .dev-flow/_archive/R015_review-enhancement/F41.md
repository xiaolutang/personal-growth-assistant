# F41 前端知识热力图升级

## Task Snapshot

- **ID**: F41
- **Phase**: P1
- **Module**: review
- **Verification**: F2
- **Depends on**: B53 ✅
- **Priority**: 1

### Key Files

- `frontend/src/pages/Review.tsx` — 热力图按 category 分组 + 降级兼容
- `frontend/src/services/api.ts` — HeatmapItem 增加 mention_count

## Implementation Summary

### 修改文件

1. `frontend/src/services/api.ts`
   - HeatmapItem 增加 mention_count: number

2. `frontend/src/pages/Review.tsx`
   - renderHeatmapCard() 重写：按 category 分组展示
   - 有 category 时按分组渲染（标题 + 概念标签）
   - 无 category 时保持原标签云布局（降级兼容）
   - entry_count 角标保持不变

## Structured Test Results

```bash
$ cd frontend && npm run build
# 结果: ✅ 构建成功

$ cd frontend && npm run test:run
# 结果: 245 passed, 0 failed (19 test files)
```

| 测试 | 验证内容 | 结果 |
|------|---------|------|
| 热力图渲染概念标签 | 标签云展示 | ✅ |
| 四级掌握度着色 | advanced/intermediate/beginner/new | ✅ |
| 空数据引导文案 | 无崩溃 | ✅ |
| 构建通过 | TypeScript + Vite | ✅ |

## xlfoundry-code-review

- status: pass
- findings:
  - 按 category 分组逻辑简洁（reduce + 条件渲染）
  - 降级路径保持原标签云布局，向后兼容
  - mention_count 字段添加与 B53 后端对齐

## xlfoundry-audit (codex_plugin)

- status: partial → done (after fix)
- findings:
  - 掌握度配色修正：advanced=紫, intermediate=蓝, beginner=绿, new=灰
  - 按 category 分组展示正确
  - Neo4j 降级路径保持原 UI
  - entry_count 角标保持
- commit_ready: true

## Acceptance Criteria Check

| # | 条件 | 状态 |
|---|------|------|
| 1 | 热力图使用后端增强数据（含 category, mention_count） | ✅ |
| 2 | 按 category 分组展示概念 | ✅ |
| 3 | 掌握度四级着色保持一致 | ✅ |
| 4 | 每个概念标签显示 entry_count 角标 | ✅ |
| 5 | Neo4j 降级时 UI 不变 | ✅ |
| 6 | npm run build 通过 | ✅ |
