# F40 前端趋势图多维展示

## Task Snapshot

- **ID**: F40
- **Phase**: P1
- **Module**: review
- **Verification**: F2
- **Depends on**: B52 ✅
- **Priority**: 1

### Key Files

- `frontend/src/pages/Review.tsx` — 多线趋势图 + 双 Y 轴 + 图例 + 环比标签
- `frontend/src/services/api.ts` — TrendPeriod 增加 task_count/inbox_count
- `frontend/src/pages/Review.trend.test.tsx` — 测试适配多线

## Implementation Summary

### 修改文件

1. `frontend/src/services/api.ts`
   - TrendPeriod 增加 task_count: number, inbox_count: number

2. `frontend/src/pages/Review.tsx`
   - 导入 Legend from recharts
   - 新增 VsLastPeriod 接口（delta_completion_rate + delta_total）
   - WeeklyReport 增加 vs_last_week, MonthlyReport 增加 vs_last_month
   - renderTrendChart() 提取为复用函数（消除原两处重复）
   - 4 条 Line：完成率(左Y轴) + 任务数/笔记数/灵感数(右Y轴)
   - 双 Y 轴：左侧 0-100% 完成率，右侧计数
   - Legend 图例说明各线含义
   - Tooltip formatter 区分完成率(%)和计数
   - renderDeltaLabel() 周报/月报环比标签
   - 趋势标签页和报告标签页共享 renderTrendChart()

3. `frontend/src/pages/Review.trend.test.tsx`
   - recharts mock 增加 Legend
   - 测试数据补齐 task_count/inbox_count
   - getByTestId("recharts-line") → getAllByTestId (4 条线)

## Structured Test Results

```bash
$ cd frontend && npm run build
# 结果: ✅ 构建成功

$ cd frontend && npm run test:run
# 结果: 245 passed, 0 failed (19 test files)
```

| 测试 | 验证内容 | 结果 |
|------|---------|------|
| 加载时显示 loading，完成后显示折线图 | 多线图表渲染 | ✅ |
| 点击「周」Badge 后 getReviewTrend 以 period=weekly 被调用 | 日/周切换联动 | ✅ |
| 空 periods 时显示「暂无趋势数据」 | 空数据引导文案 | ✅ |
| getReviewTrend 抛错时显示错误提示 | 错误降级 | ✅ |
| 有数据时图表下方显示平均完成率百分比 | 摘要显示 | ✅ |
| 趋势 API 失败时选择器仍正常显示 | 错误不影响主 UI | ✅ |
| 只有 1 天数据时折线图正常渲染 | 边界场景 | ✅ |

## xlfoundry-code-review

- status: pass
- findings:
  - TrendPeriod 接口扩展与 B52 后端对齐
  - 旧代码重复趋势图提取为 renderTrendChart() 消除重复
  - 双 Y 轴设计合理，Tooltip 正确区分类型
  - 测试 mock 正确适配多线

## xlfoundry-audit (codex_plugin)

- status: blocked (环境限制)
- findings:
  - 验收条件 1-6 代码层面全部满足
  - 验收条件 7 (build) 在只读沙箱无法验证，本地已通过
  - 证据文件缺失 → 已补齐
- commit_ready: true (本地验证通过)

## Acceptance Criteria Check

| # | 条件 | 状态 |
|---|------|------|
| 1 | LineChart 改为多线展示（完成率+任务数+笔记数+灵感数） | ✅ |
| 2 | 添加图例说明各线含义 | ✅ |
| 3 | Tooltip 展示完整多维数据 | ✅ |
| 4 | 日/周切换保留并正确联动 | ✅ |
| 5 | 周报/月报显示环比对比标签 | ✅ |
| 6 | 空数据时图表区域显示引导文案 | ✅ |
| 7 | npm run build 通过 | ✅ |
