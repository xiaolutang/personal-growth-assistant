# B52 趋势数据多维扩展 + 周环比对比

## Task Snapshot

- **ID**: B52
- **Phase**: P1
- **Module**: review
- **Verification**: L2
- **Depends on**: 无
- **Priority**: 1

### Key Files

- `backend/app/services/review_service.py` — TrendPeriod 模型 + get_trend_data() + VsLastPeriod + WeeklyReport/MonthlyReport 环比
- `backend/app/routers/review.py` — 导入 VsLastPeriod

## Implementation Summary

### 修改文件

1. `backend/app/services/review_service.py`
   - TrendPeriod 新增 task_count: int = 0, inbox_count: int = 0（笔记复用已有 notes_count）
   - 新增 VsLastPeriod 模型：delta_completion_rate + delta_total
   - WeeklyReport 新增 vs_last_week: Optional[VsLastPeriod]
   - MonthlyReport 新增 vs_last_month: Optional[VsLastPeriod]
   - get_trend_data() 按分类统计（list_entries type=task/note/inbox）
   - _calculate_weekly_vs_last_week() 计算周环比
   - _calculate_monthly_vs_last_month() 计算月环比

2. `backend/app/routers/review.py` — 导入 VsLastPeriod

3. `backend/tests/unit/api/test_review_b52.py`（新增）— 13 测试用例

## Structured Test Results

```bash
$ cd backend && uv run pytest tests/unit/api/test_review_b52.py tests/unit/api/test_review_api.py -q
# 结果: 55 passed (42 既有 + 13 新增)
```

| 测试 | 验证内容 | 结果 |
|------|---------|------|
| 趋势 API 返回 task_count/inbox_count | 新字段存在且默认 0 | ✅ |
| 分类计数正确 | task=2, notes=1, inbox=3 | ✅ |
| 空数据返回零值 | 所有计数字段为 0 | ✅ |
| 用户隔离验证 | 用户 A/B 数据互不影响 | ✅ |
| 周报 vs_last_week 字段存在 | delta_completion_rate + delta_total | ✅ |
| 周报 delta 精确验证 | delta_total = -3（负数，上周更多） | ✅ |
| 空数据 vs_last_week delta = null | 首周无对比数据时 null | ✅ |
| 月报 vs_last_month 字段存在 | delta_completion_rate + delta_total | ✅ |
| 月报 delta 精确验证 | delta_total = 2（本月更多） | ✅ |
| 空数据 vs_last_month delta = null | 首月无对比数据时 null | ✅ |
| 环比用户隔离 | 用户 A 环比不含 B 数据 | ✅ |

## xlfoundry-code-review

- status: passed (after fix)
- rounds: 1 (发现 notes_count/note_count 重复 → 修复)
- findings:
  - notes_count 和 note_count 语义重复 → 删除 note_count
  - _calculate_weekly_vs_last_week 和 _calculate_monthly_vs_last_month 代码重复 → 不阻塞
- simplify: not required

## xlfoundry-audit (codex_plugin)

- status: done (after delta precision fix)
- findings:
  - 6 条验收条件实现层全部满足
  - delta 精确断言已加强
- commit_ready: true

## Acceptance Criteria Check

| # | 条件 | 状态 |
|---|------|------|
| 1 | TrendPeriod 增加 task_count, inbox_count | ✅ |
| 2 | get_trend_data() 按分类统计 | ✅ |
| 3 | WeeklyReport vs_last_week | ✅ |
| 4 | MonthlyReport vs_last_month | ✅ |
| 5 | 空数据零值 + delta null | ✅ |
| 6 | 用户认证隔离 | ✅ |
