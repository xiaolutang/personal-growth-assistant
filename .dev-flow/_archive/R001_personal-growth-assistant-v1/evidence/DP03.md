# DP03 Evidence

## Task Snapshot

- task_id: DP03
- task_name: 构建与运行态验证
- phase: P11

## Execution Evidence

- `bash deploy/build.sh` 构建成功 → `personal-growth-assistant:latest`
- 容器启动：`docker run -p 8001:8001 -v data/:/app/data -e DATA_DIR=/app/data` 成功

### 容器级验证结果

| 检查项 | URL | HTTP | Content-Type | 结果 |
|--------|-----|------|-------------|------|
| 健康检查 | `/health` | 200 | `application/json` | `{"status":"ok"}` |
| 前端首页 | `/` | 200 | `text/html; charset=utf-8` | 返回 index.html |
| SPA 深链路由 | `/tasks` | 200 | `text/html; charset=utf-8` | 返回 index.html (SPA fallback) |
| SPA 深链路由 | `/projects` | 200 | `text/html; charset=utf-8` | 返回 index.html (SPA fallback) |
| API 文档 | `/docs` | 200 | — | Swagger UI |
| 静态资源 | `/assets/index-*.js` | 200 | `text/javascript; charset=utf-8` | JS 文件正确返回 |
| 路由隔离 | `/health` vs `/` | 200/200 | JSON vs HTML | API 与静态互不干扰 |

## Runtime Verification

### 容器级验证（裸 `docker run`）
9/9 通过：health/frontend/SPA深链×2/静态MIME/路由隔离/docs/vite.svg/entries

### 集成环境验证（`docker compose -f deploy/docker-compose.yml up`）

8/8 通过：

| # | 检查项 | 结果 |
|---|--------|------|
| 1 | /health → application/json | `{"status":"ok"}` |
| 2 | / → text/html | HTML |
| 3 | /tasks → text/html (SPA fallback) | HTML |
| 4 | /projects → text/html (SPA fallback) | HTML |
| 5 | /assets/*.js → text/javascript | JS MIME |
| 6 | /docs → 200 | Swagger UI |
| 7 | /entries → application/json | JSON |
| 8 | log-service 联通 → 200 | JSON |

### Traefik 网关验证（`http://localhost/growth/`）

通过 Traefik StripPrefix 路由的真实网关验证：

| # | 检查项 | URL | 结果 |
|---|--------|-----|------|
| 1 | 前端首页 | `http://localhost/growth/` | 200 HTML |
| 2 | SPA 深链 | `http://localhost/growth/tasks` | 200 HTML |
| 3 | API 健康检查 | `http://localhost/growth/api/health` | 200 JSON `{"status":"ok"}` |
| 4 | API 文档 | `http://localhost/growth/api/docs` | 200 |
| 5 | 静态资源 | `http://localhost/growth/assets/*.js` | 200 JS MIME |
| 6 | 路由隔离 | `/growth/api/health` JSON vs `/growth/` HTML | 互不干扰 |
| 7 | log-service 联通 | `http://localhost/growth/api/health` 日志到达 | 日志正常到达 |

- Dev compose config 验证通过
- Prod compose config 验证通过
- Dev build backend 成功

## Notes

- SPA fallback 通过 catch-all 路由 `/{full_path:path}` 实现，非文件路径返回 index.html
- Traefik 路由配置（pga-api priority=100 + pga-static priority=50）已在本地 Traefik 实例验证通过
- 开发/测试环境复用同一 `deploy/` 单容器构建，`scripts/test-docker.sh` 可独立验证
