# FB03 Evidence

## Task Snapshot

- task_id: FB03
- task_name: 前端反馈 API 层
- phase: P10

## Execution Evidence

- 在 `frontend/src/services/api.ts` 新增：
  - `FeedbackSeverity`
  - `FeedbackPayload`
  - `FeedbackResponse`
  - `submitFeedback()`
- 422 错误通过 `handleApiResponse` 透传为 `ApiError`
- 验证：
  - `npm run test:run -- src/services/api.feedback.test.ts`
  - 包含 `200 / 503 / 422` 场景
