# DP01 Evidence

## Task Snapshot

- task_id: DP01
- task_name: 创建 deploy/ 单容器文件
- phase: P11

## Execution Evidence

- 新增 `deploy/static_app.py` — Starlette StaticFiles(html=True) 挂载前端 dist
- 新增 `deploy/Dockerfile` — 3-stage 构建（node:20-alpine → python:3.12-slim + git → python:3.12-slim runtime）
- 新增 `deploy/build.sh` — 唯一构建入口，cd 到项目根后执行 docker build
- 新增 `deploy/docker-compose.yml` — 单服务 pga + bind mount `../data:/app/data`（保留 Markdown 主数据源）+ 两条 Traefik 路由标签
- 验证：`bash deploy/build.sh` 构建成功，镜像 `personal-growth-assistant:latest` 已生成
- Python deps stage 包含 git（log-service-sdk 从 git source 拉取成功）
- 前端构建：npm ci + npm run build 成功，输出 dist/index.html + assets/
- Code review 修复：命名卷改为 bind mount `../data:/app/data`，与三层存储架构（Markdown 主数据源）一致
