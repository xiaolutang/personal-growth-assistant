# B010 移除本地日志模块

## Task Snapshot
- id: B010
- name: 移除本地日志模块
- phase: P4
- commit: 1f37227 (最新运行态验证)
- original_commits: e582e8d (B010), dff7002 (B010+B011 合并)

## Acceptance Criteria Check
- [x] `infrastructure/logging/` 已删除
- [x] 项目能正常启动（307 单元测试通过）

## Execution Evidence

### Code Verification
- `backend/app/infrastructure/` 目录下不存在 `logging` 子目录
- 旧日志模块文件已全部删除：
  - `backend/app/infrastructure/logging/*`
  - `backend/app/core/logging.py`
  - `backend/app/routers/logs.py`
  - `backend/app/services/log_service.py`
- 无残留 import：`grep -r "from app.infrastructure.logging" backend/app/ --include="*.py"` 返回空

### Git Evidence
- commit `e582e8d`: refactor(logs): B010 移除本地日志模块
- commit `dff7002`: refactor(logs): B010+B011 移除本地日志模块并接入 log-service SDK

### Runtime Verification (2026-04-12, commit 1f37227)
```
$ uv run pytest tests/unit/ -q
============================= 307 passed in 2.33s =============================

$ npm run test:run
Test Files  9 passed (9)
Tests  170 passed (170)

$ npm run build
✓ built in 1.74s
```

## tests_run
- 2026-04-12 commit 1f37227: pytest tests/unit/ → 307 passed
- 2026-04-12 commit 1f37227: npm run test:run → 170 passed
- 2026-04-12 commit 1f37227: npm run build → 成功

## Runtime Validation (2026-04-12)
```
$ uv run uvicorn app.main:app --port 18925
INFO: Application startup complete.
INFO: Uvicorn running on http://127.0.0.1:18925
SQLite 索引同步完成: 26 条记录

$ curl http://127.0.0.1:18925/health
{"status":"ok"}

$ curl http://127.0.0.1:18925/api/logs
{"detail":"Not Found"}  ← 旧日志路由已删除，确认 B010

$ curl http://127.0.0.1:18925/entries
200 OK，正常返回条目列表
```
结论：应用正常启动，旧日志路由不存在，业务 API 正常工作。

## Quality Gates

### xlfoundry-code-review (2026-04-12)
- status: pass
- 审查范围：main.py, config.py, middleware/__init__.py, test_logging_integration.py
- 维度评估：
  - 架构与系统性：SDK 初始化位于 lifespan 启动阶段，全局变量模式与 graph/storage 一致，无遗漏同类入口
  - 复用与抽象：完全复用 SDK setup_remote_logging()，零自定义实现，配置复用 Settings 类
  - 代码质量：try/except 保护初始化，%s 占位符，.env.example 已补充，.gitignore 已更新
  - 效率与可测试性：SDK 内建攒批+重试不阻塞业务，任务级测试 mock 边界合理
- routing: pass

### xlfoundry-simplify (2026-04-12)
- 审查文件：main.py, config.py, deps.py, pyproject.toml
- 发现并修复：
  1. data/logs.db 残留 → 添加到 .gitignore
  2. main.py f-string 日志格式不一致 → 改为 %s 占位符
  3. .env.example 缺少 LOG_SERVICE_URL → 已补充
- simplify 后复测：307 backend + 170 frontend 全部通过（commit 1f37227）

### Acceptance Check
- [x] infrastructure/logging/ 已删除
- [x] 项目能正常启动 → 307 单元测试通过
- [x] 无旧日志模块残留引用
- [x] 旧日志路由 /api/logs 不再注册

### Task-Level Integration Tests (test_logging_integration.py, 5 tests)
- test_infrastructure_logging_dir_not_exists: PASSED
- test_no_old_logging_imports: PASSED
- test_project_starts_without_local_logging: PASSED
- test_lifespan_starts_without_local_logging: PASSED (实际触发 FastAPI lifespan)
- test_no_log_router_registered: PASSED

### Systemic Preflight
- adjacent_impact: 无旧日志模块消费者受影响（routers/deps.py 已清理）
- pattern_consistency: 全局变量模式与 storage/graph/_log_handler 统一
- reuse: 使用 SDK 的 setup_remote_logging()，零重复代码

### Failure Branch
- 旧模块已物理删除，不存在 fallback 分支
- 启动失败风险由 FastAPI lifespan try/except 覆盖
