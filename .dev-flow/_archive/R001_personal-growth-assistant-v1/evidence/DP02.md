# DP02 Evidence

## Task Snapshot

- task_id: DP02
- task_name: 切换 deploy.sh + 清理旧文件
- phase: P11

## Execution Evidence

### deploy/deploy.sh（原 scripts/deploy.sh）

- COMPOSE_FILE 改为 `deploy/docker-compose.yml`
- 新增 `custom_build()` 调用 `bash deploy/build.sh`
- 路径从 `scripts/deploy.sh` 迁移到 `deploy/deploy.sh`，与 log-service 对齐

### 旧生产文件删除

- `docker/Dockerfile.backend` — 已删除
- `docker/Dockerfile.frontend` — 已删除
- `docker/docker-compose.prod.yml` — 已删除
- `docker/nginx/frontend.conf` — 已删除

### 旧开发环境文件删除（用户决策：统一使用 deploy/ 单容器）

- `docker/docker-compose.dev.yml` — 已删除
- `docker/nginx/default.conf` — 已删除
- `docker/nginx/nginx.conf` — 已删除
- `scripts/dev.sh` — 已删除
- `docker/` 目录已完全移除

### 文档同步更新

- `CLAUDE.md` — 移除 dev docker 引用，部署路径改为 `./deploy/deploy.sh`
- `README.md` — 移除旧引用，目录结构对齐 log-service
- `docs/deploy.md` — 更新为 Traefik 单容器方案，路径改为 `./deploy/deploy.sh`
- `.dev-flow/architecture.md` — 更新开发环境描述为"复用 deploy/ 单容器构建"

### 验证

- `ls docker/` 返回不存在
- `ls scripts/dev.sh` 返回不存在
- CLAUDE.md / README.md / docs/deploy.md 无旧文件残留引用
