# B41: 性能基线建立

## Execution Evidence

**Commit**: `8852887`

### 实现内容
- `backend/tests/performance/test_api_bench.py`：4 个 benchmark 测试
  - `/health` < 50ms
  - `/entries` < 200ms
  - `POST /entries` < 300ms
  - `/search` < 300ms（允许 503）
- `@pytest.mark.benchmark` 标记，默认 `-m "not slow"` 排除
- `docs/performance-baseline.md`：前端 bundle 362.80 KB gzip + API 基线数据

### 测试
- 4 个 benchmark 测试通过（需 `-m benchmark` 显式运行）
