# B34: Health check 增强 — 依赖连接检查

## Execution Evidence

**Commit**: `fd2372e`

### 实现内容
- `GET /health` 返回 `{status, services: {sqlite, neo4j, qdrant}}`
- SQLite 核心依赖：执行 `SELECT 1` 真实查询
- Neo4j 非核心：`verify_connectivity()` 真实连接探测
- Qdrant 非核心：`get_collections()` 真实连接探测
- `_check_services()` 改为 async，`/health` 端点 await 调用
- Neo4j/Qdrant 连接失败标记 `error`（区别于未配置的 `unavailable`）
- SQLite 不可达返回 503，非核心降级返回 200 + `degraded`

### 测试
- 16 个单元测试全部通过（test_health.py）
- 含 Neo4j/Qdrant 连接失败测试、全部端点测试
- 619 后端测试通过

### Codex 修复
- `_check_services` 从 sync 改为 async（Finding 2）
- Neo4j/Qdrant 从只检查 handle 改为真实探测
