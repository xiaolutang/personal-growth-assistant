# B35: E2E 基础设施 — 双服务启停 + 认证 fixture + API helper

## Execution Evidence

**Commit**: `597f757`

### 实现内容
- `playwright.config.ts`：webServer 双服务（后端 uvicorn :18929 + 前端 vite :5173）
- `e2e/tests/helpers/auth.ts`：`registerAndLogin()` 注册+登录获取 JWT
- `e2e/tests/helpers/api.ts`：封装条目 CRUD（create/list/get/update/delete/search）
- baseURL 设为 `http://localhost:5173`
- 后端使用临时 DATA_DIR（mktemp 隔离）+ `JWT_SECRET=test_e2e_secret` + `DEBUG=true`

### Codex 修复
- DATA_DIR 从固定 `/tmp/pga_e2e_data` 改为 `mktemp -d` 唯一目录（Finding 3）
- `reuseExistingServer` 改为 `false`（Finding 3）
- 新增 `DEBUG=true` 环境变量支持 force_intent E2E（Finding 1 关联）
