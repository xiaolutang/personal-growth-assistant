# B38: Chat 对话 E2E 测试（read/delete 路径）

## Execution Evidence

**Commit**: `99d2870`

### 实现内容
- `e2e/tests/chat.spec.ts`：5 个测试
  - `force_intent=read` 搜索已有条目返回 results 事件
  - `force_intent=delete` 删除已有条目返回 deleted 事件
  - 空输入 422
  - 未认证 401
  - 会话列表展示
- `parseSSEEvents()` 手动解析 SSE 格式
- 会话通过 `PATCH /sessions/{id}` 创建

### Codex 修复
- `force_intent` 增加 `DEBUG=true` 环境门控（Finding 1）
- `generate()` 内提前导入 `sse_event` 和 `get_settings`
- 生产环境 `DEBUG=false` 返回 SSE error 事件并 return
- 新增 2 个安全门控测试：`detect_intent` 未调用断言

### 测试
- 5 个 E2E 测试通过
- 2 个新增 force_intent 门控单元测试通过
