# F113: GraphPage 状态拆分

## Task Snapshot
- id: F113
- module: frontend
- verification: F2

## Execution Evidence

### Implementation
- File: `frontend/src/pages/GraphPage.tsx`
- Extracted `CapabilityMapView` as same-file component (before GraphPage export, ~line 355)
- Encapsulated 6 states: capabilityMap, capabilityLoading, capabilityError, expandedDomain, capabilityFilter, capabilityRetryKey
- GraphPage renders `<CapabilityMapView />` when `activeView === "capability"`
- `pageData.domain_count` set to 0 (capability data encapsulated in child)
- GraphPage state count reduced from ~18 to ~12

### tests_run
- Frontend: 347 passed (vitest)
- Frontend build: success

### test_results
- All existing GraphPage tests pass
- Build output includes GraphPage chunk (20.96 kB gzip: 6.48 kB)

### runtime_verification
- Docker E2E: GET /growth/api/knowledge/capability-map → returns JSON with domains array
- Docker E2E: GET /growth/api/knowledge/stats → returns JSON with concept stats

### acceptance_check
- [x] CapabilityMapView extracted as same-file component
- [x] Encapsulates 6 state + useEffect + JSX
- [x] GraphPage no longer directly manages capability map states
- [x] Tab switching, domain expand, filter functionality preserved
- [x] Frontend tests pass, build succeeds
