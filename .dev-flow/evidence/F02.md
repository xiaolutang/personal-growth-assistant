# F02 — 搜索防抖工具

## 实现文件

- `mobile/lib/utils/debouncer.dart` — Debouncer 工具类
- `mobile/test/utils/debouncer_test.dart` — 单元测试（11 个用例）

## 验收标准

| # | 标准 | 状态 |
|---|------|------|
| 1 | Debouncer 类支持可配置延迟（默认 300ms） | PASS |
| 2 | run(action) 在延迟到期前再次调用取消上一次操作 | PASS |
| 3 | 提供 dispose() 方法取消 Timer 防止内存泄漏 | PASS |
| 4 | 提供 immediateRun(action) 用于即时场景 | PASS |

## 测试覆盖

| 场景 | 结果 |
|------|------|
| 默认延迟 300ms — 299ms 未执行，300ms 已执行 | PASS |
| 可自定义延迟 | PASS |
| 300ms 内多次调用只执行最后一次 action | PASS |
| 延迟到期后 action 被执行 | PASS |
| isActive 反映 Timer 状态 | PASS |
| Timer 回调执行后 isActive 自动变为 false | PASS |
| immediateRun 立即执行不延迟 | PASS |
| 取消已排队的防抖任务后立即执行 | PASS |
| dispose 后 Timer 被取消，action 不执行 | PASS |
| dispose 后 isActive 为 false | PASS |
| 多次调用 dispose 不抛异常 | PASS |

## 代码自审 (codex-plugin review)

**Review provider**: codex_plugin

### Review 结果: conditional_pass

**发现的问题及修复**:

1. **quality: medium** — 默认延迟测试不够严格，仅验证"不同步执行"而非精确 300ms 边界
   - **修复**: 使用 `fakeAsync` 精确验证 299ms 未执行、300ms 已执行

2. **efficiency: medium** — `run()` 方法 Timer 回调执行后未清除 `_timer`，导致不必要的引用保留
   - **修复**: 回调内添加 `_timer = null`

3. **efficiency: medium** — 测试依赖 wall-clock sleep，不够确定性和快速
   - **修复**: 全部改用 `fakeAsync` 模拟时间，测试瞬间完成

## 质量门

- `flutter analyze`: No issues found
- `flutter test`: 11/11 passed (0.0s，使用 fakeAsync)
