# F62: PWA 安装引导 (R019)

## Task Snapshot
| Field | Value |
|-------|-------|
| Module | pwa-install |
| Priority | 3 | Phase | 4 |
| Depends On | S03 (completed) |
| Risk Tags | — |

## Execution Evidence

### 修改文件
1. `frontend/src/lib/usePWAInstall.ts` — 增加 usageCount 追踪 + showBanner 逻辑 + dismissBanner + appinstalled 监听
2. `frontend/src/components/layout/Header.tsx` — 增加安装引导横条（primary 色背景 + 安装按钮 + 关闭按钮）
3. `frontend/src/hooks/useChatActions.ts` — onCreated 回调中调用 incrementPWAUsage()
4. `frontend/vite.config.ts` — manifest 添加 `lang: 'zh-CN'`
5. `frontend/src/lib/__tests__/usePWAInstall.test.ts` — 8 个单元测试
6. `frontend/src/__tests__/integration/pwa-install.integration.test.tsx` — 5 个集成测试

### 关键设计
1. **usageCount**：localStorage `pwa-usage-count`，useChatActions.onCreated 中递增
2. **showBanner**：canInstall && usageCount >= 3 && !isDismissed（dismissedAt 为 React state）
3. **dismissBanner**：localStorage + setDismissedAt(now) 触发立即重渲染
4. **状态同步**：`pwa-usage-updated` 自定义事件 + `storage` 事件（跨 tab）
5. **appinstalled**：监听事件，安装后 canInstall=false，横条自动消失
6. **Header 横条**：primary 色背景，包含文案+安装按钮+关闭按钮

### Code Review (Codex Plugin)
- reviewed_at: 2026-04-19
- status: FAIL → 修复后 PASS
- findings: 1 (High)
  1. dismissBanner() 只写 localStorage 不触发重渲染 → 修复：dismissedAt 改为 React state
- commit: 0b63c91 fix(review): F61/F62 代码审查缺陷修复

### tests_run
| Test | Result |
|------|--------|
| npm run build | PASS |
| npm run test:run | PASS (321 tests, 31 files) |
| usePWAInstall.test.ts | PASS (8 tests) |
| pwa-install.integration.test.tsx | PASS (5 tests) — banner lifecycle: show/dismiss/expire/appinstalled/promptInstall |

### Runtime Verification
- launchable: PASS (npm run build 通过，manifest 含 lang: zh-CN)
- interactive: PASS — 集成测试 pwa-install.integration.test.tsx 验证完整用户旅程：
  - 场景 1: canInstall=true, usageCount>=3, 未 dismissed → 显示横条 → 关闭后立即消失
  - 场景 2: usageCount<3 → 不显示横条
  - 场景 3: 关闭后 7 天过期 → 恢复显示
  - 场景 4: appinstalled 事件 → 横条消失
  - 场景 5: 点击安装按钮 → promptInstall 被调用
  - 测试文件：frontend/src/__tests__/integration/pwa-install.integration.test.tsx
  - 运行命令：npx vitest run src/__tests__/integration/ — 12/12 passed
- outcome_visible: PASS (安装成功后横条不显示，关闭后立即消失，7天后恢复)

### systemic_preflight
- reuse_candidates: usePWAInstall (已有 hook，增强 usageCount), Header.tsx (已有安装按钮，升级为横条), useChatActions.onCreated (唯一可靠创建事件源)
- adjacent_impact: useChatActions (incrementPWAUsage 调用), Header (横条渲染), vite.config.ts (manifest)
- pattern_consistency: localStorage 持久化模式与 userStore token 管理一致，自定义事件 + storage 事件跨 tab 同步与 chatStore SSE 模式一致

### acceptance_check
| AC | Result |
|----|--------|
| manifest lang 改为 zh-CN | PASS (vite.config.ts) |
| usePWAInstall usageCount 追踪 | PASS (test: incrementUsageCount) |
| useChatActions onCreated 调用 incrementPWAUsage | PASS (code) |
| showBanner: canInstall && usageCount >= 3 && 未关闭 | PASS (test) |
| Header.tsx showBanner 时渲染引导横条 | PASS (code) |
| 安装成功后横条不显示 | PASS (test: appinstalled) |
| 关闭后 7 天内不显示 | PASS (test: dismissed) |
| 已安装用户不显示 | PASS (test: canInstall=false) |

## Audit Result
- status: done
- commit_ready: true
