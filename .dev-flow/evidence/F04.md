# F04 执行证据

| Task ID | F04 |
| Phase | P4 |
| Name | 侧边栏用户信息 + 登出按钮 |

## 实现摘要

- 修改 `frontend/src/components/layout/Sidebar.tsx`
  - 底部显示当前用户 username（User 图标 + 文字）
  - 登出按钮（LogOut 图标），点击清除 token 并跳转 /login
  - X-UID header 使用匿名 ID（保留日志追踪能力）
- 修改 `frontend/src/main.tsx` 移除旧的 initUidHeader 调用

## 验证

```bash
npm run build  # 构建成功
npm run test:run  # 177 passed
```
