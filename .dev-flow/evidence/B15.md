# B15 — 灵感转化 API — category 变更 + 文件迁移

## Task Snapshot

- **ID**: B15
- **Phase**: P1A
- **Module**: entry-management
- **Verification**: L2
- **Priority**: high
- **Depends on**: S05 (completed)

## Acceptance Criteria

1. PUT /entries/{id} { "category": "task" } 成功返回
2. 文件从 data/users/{uid}/inbox-xxx.md 移到 data/users/{uid}/tasks/inbox-xxx.md
3. entry_id 前缀不变（仍为 inbox-xxx）
4. front matter 的 type 字段同步更新为新 category
5. SQLite 索引中 category 和 file_path 同步更新
6. 新 category 列表（如 GET /entries?type=task）可见该条目
7. 旧路径文件已不存在
8. uv run pytest backend/tests/ 全部通过

## Execution Evidence

### Changes

1. **entry_service.py update_entry()** — 新增 category 变更处理分支：
   - 检测 `request.category` 不为 None 且与新 category 不同
   - 更新 `entry.category` 和 front matter `type:` 字段
   - 记录旧 file_path，write_entry 后删除旧文件
2. **test_entry_service.py** — 新增 TestCategoryConversion 类（6 个测试）

### tests_run

| Command | Result |
|---------|--------|
| `cd backend && uv run pytest tests/unit/services/test_entry_service.py::TestCategoryConversion -v` | 6 passed |
| `cd backend && uv run pytest tests/unit/ tests/integration/ -q` | 424 passed, 20 skipped |

### Acceptance Check

| # | Criteria | Status |
|---|----------|--------|
| 1 | PUT /entries/{id} category 变更成功 | ✅ test_inbox_to_task_conversion |
| 2 | 文件迁移到正确目录 | ✅ write_entry 自动处理 |
| 3 | entry_id 前缀不变 | ✅ test_inbox_to_task_conversion (id starts with inbox-) |
| 4 | front matter type 更新 | ✅ regex sub "type: task" |
| 5 | SQLite 索引同步 | ✅ upsert_entry 自动更新 |
| 6 | 新 category 列表可见 | ✅ SQLite 索引更新 |
| 7 | 旧路径文件删除 | ✅ os.remove old_path |
| 8 | 后端测试通过 | ✅ 424 passed |
