# B011 接入 log-service SDK

## Task Snapshot
- id: B011
- name: 接入 log-service SDK
- phase: P4
- commit: dff7002 (合并 B010+B011)

## Acceptance Criteria Check
- [x] 中间件日志通过 SDK 发送到 log-service

## Execution Evidence

### Code Verification
- `backend/app/main.py:25` 导入 `from log_service_sdk import setup_remote_logging`
- `backend/app/main.py:44-50` 调用 `setup_remote_logging(endpoint, service_name, level)`
- `backend/app/core/config.py:53` 定义 `LOG_SERVICE_URL: str = "http://localhost:8001"`
- `backend/pyproject.toml` 引入 `log-service-sdk` 依赖
- `backend/app/middleware/__init__.py` 的 RequestLoggingMiddleware 通过 `logger.info(...)` 输出请求日志

### SDK Source Verification
- `RemoteLogHandler` 继承 `logging.Handler`（已验证 `issubclass(RemoteLogHandler, logging.Handler) == True`）
- `setup_remote_logging()` 将 `RemoteLogHandler` 挂载到 root logger
- Handler 通过 HTTP POST 向 `/api/logs/ingest` 批量发送日志
- 支持攒批（50条/2秒）和失败重试（3次）

### Runtime Verification (2026-04-12)
```
LOG_SERVICE_URL configured: http://localhost:8001
setup_remote_logging returned: RemoteLogHandler
Root logger handlers: ['RemoteLogHandler']
RemoteLogHandler attached: True
Middleware logger effective level: 20 (INFO)
Runtime verification PASSED
```

### Integration Chain
```
中间件 logger.info("Request completed...")
  → root logger (RemoteLogHandler attached)
    → 内存队列攒批
      → HTTP POST /api/logs/ingest (log-service)
```

## Smoke Test
- 后端启动时 `setup_remote_logging()` 被调用，返回 `RemoteLogHandler` 实例
- RemoteLogHandler 成功挂载到 root logger
- 中间件日志链路完整（代码层 + 运行态验证）

## Quality Gates

### xlfoundry-simplify (2026-04-12)
- 审查文件：main.py, config.py, deps.py, pyproject.toml
- 发现并修复：
  1. main.py f-string 日志 → 改为 %s 占位符（与第 50 行风格统一）
  2. .env.example 补充 LOG_SERVICE_URL 配置项
- 修复后复测：294 backend + 170 frontend 全部通过

### Acceptance Check
- [x] 中间件日志通过 SDK 发送到 log-service
  - 验证链路：`middleware logger.info()` → root logger → RemoteLogHandler → HTTP POST /api/logs/ingest
  - 运行态证据：`setup_remote_logging returned: RemoteLogHandler`, `RemoteLogHandler attached: True`

### Integration Test Evidence
```
$ uv run pytest tests/unit/ -q
============================= 304 passed in 2.36s =============================

$ npm run test:run
Test Files  9 passed (9)
Tests  170 passed (170)

$ npm run build
✓ built in 1.81s
```

### Task-Level Integration Tests (test_logging_integration.py)
- test_setup_remote_logging_importable: PASSED (SDK 可导入)
- test_log_service_url_configured: PASSED (配置项存在)
- test_setup_remote_logging_attaches_handler: PASSED (handler 挂载到 root logger)
- test_sdk_init_failure_does_not_block_startup: PASSED (SDK 失败不阻塞启动)
- test_middleware_logs_through_root_logger: PASSED (中间件通过 root logger)
- test_log_handler_close_on_shutdown: PASSED (关闭时 flush)

### Runtime Verification
```
$ uv run python -c "
from log_service_sdk import setup_remote_logging
import logging
handler = setup_remote_logging(endpoint='http://localhost:8001', service_name='personal-growth-assistant', level=logging.INFO)
root = logging.getLogger()
print(f'Handler type: {type(handler).__name__}')
print(f'Handler in root: {handler in root.handlers}')
"
Handler type: RemoteLogHandler
Handler in root: True
```

### Smoke Test (smoke_required=true)
- RemoteLogHandler 实例化并挂载到 root logger: PASSED
- 中间件 logger.info() 输出会被 root logger 的 RemoteLogHandler 捕获: PASSED
- SDK 内建攒批（50条/2秒）+ 重试（3次）+ HTTP POST /api/logs/ingest: SDK 源码验证通过
- 完整链路：middleware → root logger → RemoteLogHandler → 内存队列 → HTTP POST

### Systemic Preflight
- adjacent_impact: 中间件、业务服务均通过标准 logging 输出，无需任何修改
- pattern_consistency: _log_handler 全局变量模式与 storage/graph 一致
- reuse: 完全复用 SDK 的 setup_remote_logging()，零自定义实现

### Failure Branch
- SDK 初始化失败：测试 test_sdk_init_failure_does_not_block_startup 验证不阻塞启动
- log-service 不可达：SDK 内建重试（3次）+ 非阻塞设计，不影响业务请求
- first_use: SDK import 和 handler 挂载均在 lifespan 最早期完成

## Risk Tags Addressed
- network: SDK 内建重试机制（3次）
- startup: 运行态验证通过，setup_remote_logging 成功执行
- first_use: SDK import 和初始化均正常
