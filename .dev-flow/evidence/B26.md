# B26 Evidence — 通知/提醒 API

## Task Snapshot
- **Task**: B26 通知/提醒 API — 按需生成 + 已读状态 + 偏好设置
- **Module**: notification
- **Priority**: medium
- **Verification**: L2

## Implementation

### 1. SQLite 表
- **File**: `backend/app/infrastructure/storage/sqlite.py`
- 新增 `notifications` 表（notification_id PK, user_id, type, ref_id, dismissed_at, created_at）
- 新增 `notification_preferences` 表（user_id PK, 三个 enabled 字段）

### 2. NotificationService
- **File**: `backend/app/services/notification_service.py` (NEW)
- `get_notifications(user_id)` — 按需生成三类通知：overdue_task, stale_inbox, review_prompt
- `dismiss_notification(id, user_id)` — 记录已读（当日有效）
- `get_preferences(user_id)` / `update_preferences(user_id, prefs)` — 偏好 CRUD
- 通知 ID 格式：`{type}:{ref_id}:{date}`
- 复用 `list_entries` 查询，复用晨报判定逻辑

### 3. Notifications Router
- **File**: `backend/app/routers/notifications.py` (NEW)
- GET /notifications — 按需生成通知列表
- POST /notifications/{id}/dismiss — 标记已读
- GET /notification-preferences — 偏好读取
- PUT /notification-preferences — 偏好更新

### 4. 注册
- deps.py: 新增 `get_notification_service()` getter + reset
- __init__.py: 导出 notifications_router
- main.py: include_router

### 5. 测试
- **File**: `backend/tests/unit/api/test_notification_api.py` (NEW)
- 11 个测试：通知生成(5) + dismiss(2) + 偏好(3) + 隔离(1)

## Execution Evidence

### tests_run
```bash
uv run pytest tests/unit/api/test_notification_api.py  # ✓ 11 passed
uv run pytest tests/unit/                               # ✓ 566 passed (555 + 11 new)
```

### acceptance_check
| # | 验收条件 | 状态 |
|---|---------|------|
| 1 | GET /notifications 返回未读通知 | ✅ 三类通知按需生成 |
| 2 | POST /notifications/{id}/dismiss 标记已读 | ✅ |
| 3 | 已 dismiss 通知不再出现 | ✅ 当日去重 |
| 4 | GET/PUT /notification-preferences 读写偏好 | ✅ |
| 5 | 数据按 user_id 隔离 | ✅ 隔离测试通过 |
| 6 | 单元测试覆盖生成 + 已读 + 偏好 + 隔离 | ✅ 11 tests |
