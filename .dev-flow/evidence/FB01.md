# FB01 Evidence

## Task Snapshot

- task_id: FB01
- task_name: 后端反馈代理路由
- phase: P10

## Execution Evidence

- 新增 `backend/app/routers/feedback.py`
- 注册到 `backend/app/routers/__init__.py` 与 `backend/app/main.py`
- 行为：
  - `POST /feedback`
  - 成功时返回 `{ success: true, issue }`
  - SDK 异常映射为 `503`
  - `title` 空白和非法 `severity` 返回 `422`
- 验证：
  - `pytest backend/tests/unit/api/test_feedback_api.py`
  - 结果：4 passed

## Runtime Verification

- 全链路集成测试 `tests/flow/test_feedback_flow.py`（8 cases）
  - 正常提交 → SDK 被正确调用 → 响应 200
  - SDK 超时 → 503 降级
  - SDK 连接拒绝 → 503 降级
  - severity 四值边界（low/medium/high/critical）→ 全部通过
  - title 边界（空字符串/纯空格/短标题）→ 正确拒绝/接受
  - 默认 severity → medium
  - 不传 description → None
  - CORS 预检 → OPTIONS 通过
- 结果：12 passed（4 unit + 8 flow）
