# B75: 新类型条目 API E2E（R020）

## Task Snapshot

- **ID**: B75
- **Name**: 新类型条目 API E2E
- **Module**: e2e-new-types
- **Phase**: 3 (R020)
- **Status**: completed
- **Started**: 2026-04-20
- **Completed**: 2026-04-20

## Acceptance Criteria

1. 创建 decision → 201 + category=decision
2. 创建 reflection → 201 + category=reflection
3. 创建 question → 201 + category=question
4. 列表 type=decision 过滤 → 只含 decision
5. 列表 type=reflection 过滤 → 只含 reflection
6. 列表 type=question 过滤 → 只含 question
7. 全文搜索 decision 标题（GET /entries/search/query） → 结果命中
8. 搜索不匹配关键词 → 空结果
9. 更新 decision 标题 → 标题变更
10. 删除 reflection → 列表不再包含
11. 混合创建 3 种类型 → 各自独立存在
12. 未认证 → 401

## Execution Evidence

### Changes

1. **e2e/tests/new-types-api.spec.ts**（新增 ~300 行）— 14 个 E2E 测试：

   **创建（3 个）**：decision/reflection/question → category 验证
   **列表过滤（3 个）**：3 种类型各自过滤正确
   **全文搜索（2 个）**：命中 + 空结果
   **更新（1 个）**：PUT + GET 验证标题变更
   **删除（1 个）**：DELETE + 列表验证移除
   **混合类型（1 个）**：3 种独立存在 + 各自过滤
   **异常（3 个）**：未认证 401 / 不存在 404 / 特殊字符标题

2. **backend/app/infrastructure/storage/markdown.py**（缺陷修复）：
   - `read_entry` 的遍历列表从 `[NOTE, PROJECT, TASK, INBOX]` 扩展为 `[NOTE, PROJECT, TASK, INBOX, DECISION, REFLECTION, QUESTION]`
   - 修复新类型条目 update/delete 404 问题（CATEGORY_DIRS 已包含 decisions/reflections/questions 但 read_entry 不搜索它们）

### tests_run

| Command | Result |
|---------|--------|
| `cd e2e && npx playwright test tests/new-types-api.spec.ts` | 14 passed (19s) |
| `uv run pytest tests/unit/storage/test_storage_markdown.py` | 19 passed |
| `uv run pytest tests/unit/api/test_api_entries.py tests/unit/test_export.py` | 57 passed |

### Acceptance Check

| # | Criteria | Status |
|---|----------|--------|
| 1 | 创建 decision → category=decision | ✅ |
| 2 | 创建 reflection → category=reflection | ✅ |
| 3 | 创建 question → category=question | ✅ |
| 4 | type=decision 过滤 | ✅ |
| 5 | type=reflection 过滤 | ✅ |
| 6 | type=question 过滤 | ✅ |
| 7 | 全文搜索命中 | ✅ |
| 8 | 搜索空结果 | ✅ |
| 9 | 更新标题变更 | ✅ |
| 10 | 删除后列表不含 | ✅ |
| 11 | 混合类型独立 | ✅ |
| 12 | 未认证 401 | ✅ |

## 额外发现

- 发现后端 `read_entry` 对 decision/reflection/question 类型的条目无法查找（目录遍历不包含这些类型），已修复
- 这意味着 B75 的 E2E 测试发现了一个影响线上用户的真实缺陷：新类型条目创建后无法通过 API 更新或删除

## Code Review Result

- Provider: codex_plugin (xlfoundry-code-review)
- Round 1: conditional_pass — 列表/搜索/更新断言过宽，未严格对齐后端契约
- Fix: 列表改为 `data.entries` + `data.total`；搜索改为 `results.entries` + `results.query`；更新/删除补 `SuccessResponse.success === true`
- reviewed_at: 2026-04-20

## Audit Result

- Provider: codex_plugin (xlfoundry-audit)
- code review conditional_pass 已修复，断言已严格对齐后端契约
- 后端缺陷修复已验证（19 个 markdown 单测 + 57 个 entries/export 单测通过）

## Runtime Verification

- 类型: API 级 E2E（Playwright APIRequestContext）
- 依赖: 后端 uvicorn :18929 + 前端 vite proxy :5173
- 验证方式: 14 个 API 测试全部通过
- 结果: 14 passed (19s)
