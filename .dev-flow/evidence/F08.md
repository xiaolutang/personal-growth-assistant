# F08 Evidence — Flutter — ExplorePage per-tab state 缓存 + sseService 清理

## Code Review

- code_review_status: pass
- code_review_provider: codex_plugin
- Findings:
  - Architecture (medium): ExploreNotifier per-tab cache pattern correct, _fetchFromApi guards against stale tab switch
  - SSE (medium): ChatNotifier correctly uses sseServiceProvider singleton, cancels on dispose

## Audit

- audit_status: done
- audit_provider: codex_plugin
- audit_commit_ready: true
- Findings:
  - Per-tab cache isolation verified: _tabCache and _tabErrors per tab
  - Slow-response guard: _fetchFromApi checks _activeTabIndex before writing state
  - Mutation sync: _syncCacheAfterMutation keeps cache consistent
  - SSE singleton: _sseService cached in build(), cancel() on dispose and before new connection
  - 15 new tests pass, flutter analyze 0 errors

## Execution Evidence

### Changed Files

1. `mobile/lib/providers/explore_provider.dart` — Per-tab Map<int, List<Entry>> cache, refreshTab, _fetchFromApi with _activeTabIndex guard, _syncCacheAfterMutation
2. `mobile/lib/pages/explore_page.dart` — Updated to use loadEntries(tabIndex:), refreshTab()
3. `mobile/lib/providers/chat_provider.dart` — ChatNotifier uses sseServiceProvider singleton, deleted _createSseService()
4. `mobile/test/providers/explore_provider_tab_cache_test.dart` — 10 new tests: cache hit/miss, loading isolation, error isolation, refresh, mutation sync
5. `mobile/test/providers/chat_provider_test.dart` — 5 new tests: SSE singleton, cancel on dispose, single subscription

### Test Results

- flutter test: All tests passed (15 new + existing)
- flutter analyze: 0 errors

### Structured Summary

```json
{
  "task_id": "F08",
  "status": "success",
  "files_changed": [
    "mobile/lib/providers/explore_provider.dart",
    "mobile/lib/pages/explore_page.dart",
    "mobile/lib/providers/chat_provider.dart",
    "mobile/test/providers/explore_provider_tab_cache_test.dart",
    "mobile/test/providers/chat_provider_test.dart"
  ],
  "test_results": { "unit": "passed", "integration": "passed" },
  "code_review": { "status": "pass", "routing": "pass", "provider": "codex_plugin" },
  "audit": { "status": "done", "commit_ready": true, "next_action": null, "provider": "codex_plugin" },
  "commit": null,
  "summary": "ExploreNotifier per-tab 缓存完成：Map<int, List<Entry>> 按 tab 缓存，首次 API → 再次读缓存，refreshTab 清空重请求。ChatNotifier 改用 sseServiceProvider 单例，删除 _createSseService。15 新测试通过。",
  "blockers": []
}
```
