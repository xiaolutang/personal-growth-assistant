# B86 Evidence: AI 建议个性化

## Task Snapshot

- **ID**: B86
- **Name**: AI 建议个性化
- **Module**: review
- **Verification**: L2
- **Priority**: P1
- **Risk Tags**: [network]

### Acceptance Criteria

1. LLM prompt 包含用户活跃目标标题（如有活跃目标，从 GoalService 获取）
2. LLM prompt 包含近 30 天高频标签 top 5（从 entries tags 统计）
3. LLM 不可用时降级到现有模板逻辑完全不变
4. 有目标和没目标用户的建议 prompt 差异化
5. 10 秒超时保护不变
6. LLM 返回 5xx 或非预期结构时降级到模板文本

### Test Tasks

1. 有目标用户：prompt 包含目标信息，LLM 返回个性化建议
2. 无目标用户：prompt 无目标段落，LLM 返回通用建议
3. 高频标签：验证 top 5 标签出现在 prompt 中
4. LLM 降级：LLM 不可用时返回模板文本
5. 超时：LLM 超时时返回降级文本，不抛异常
6. LLM 5xx：模拟 LLM 返回 500 错误，降级到模板文本
7. LLM 异常结构：模拟 LLM 返回非字符串，降级到模板文本

---

## Execution Evidence

### Implementation

- **Modified**: `backend/app/services/review_service.py`
  - `_generate_ai_summary()`: 新增可选参数 `system_prompt_override`
  - `_generate_morning_suggestion()`: 新增活跃目标注入 + 高频标签统计 + 个性化 prompt
  - 活跃目标：通过 `deps.get_goal_service().list_goals(status="active")` 获取，try/except 保护
  - 高频标签：`self._sqlite.list_entries(start_date=30天前, limit=5000)` + dict 频次统计 top 5
  - 个性化 prompt：根据有/无目标和标签动态拼接系统提示词

- **New**: `backend/tests/unit/api/test_morning_digest_personalize.py` (9 tests)

### Test Results

```
B86 测试: 9 passed (1.06s)
回归测试: 110 passed (29.77s) — 含晨报缓存/增强/review API/review service
```

### Acceptance Check

1. ✅ 活跃目标注入 — test_suggestion_with_goals_includes_goals_in_stats
2. ✅ 高频标签 top 5 — test_top_tags_in_stats
3. ✅ LLM 不可用降级 — test_llm_unavailable_fallback_template
4. ✅ prompt 差异化 — test_prompt_diff_with_and_without_goals
5. ✅ 超时保护不变 — test_llm_timeout_fallback (asyncio.TimeoutError → 模板)
6. ✅ LLM 异常降级 — test_llm_5xx_fallback + test_llm_non_string_response_fallback

### xlfoundry-code-review: pass (local)
