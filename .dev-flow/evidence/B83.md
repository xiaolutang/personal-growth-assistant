# B83 导出 API 增强 — 执行证据

## Task Snapshot

- **ID**: B83
- **Name**: 导出 API 增强
- **Module**: export
- **Verification**: L2
- **Priority**: high

## Implementation Summary

### 改动文件

1. **`backend/app/routers/entries.py`**
   - 新增 `GET /entries/{entry_id}/export` 单条目 Markdown 文件下载
   - 验证条目存在+用户归属，FileResponse 返回源文件
   - 中文文件名 URL 编码处理

2. **`backend/app/routers/review.py`**
   - 新增 `GET /review/growth-report` 成长报告 Markdown 下载
   - 4 个固定 section：概览（7 分类计数）、学习趋势（4 周聚合）、学习连续天数、知识图谱概览
   - 数据来源：`sqlite.count_entries`、`review_service.get_trend_data(weeks=4)`、`review_service._calculate_learning_streak`、`knowledge_service._stats_from_sqlite`

3. **`backend/tests/unit/api/test_export_api.py`** (新增)
   - 10 个测试覆盖单条目导出和成长报告

## Test Results

```
test_export_api.py: 10 passed
全量后端测试: 1052 passed, 20 skipped
```

## Acceptance Criteria Check

- [x] GET /entries/{id}/export 返回 .md 文件下载
- [x] GET /review/growth-report 返回格式化成长报告 .md 文件下载
- [x] 两个端点都需要认证
- [x] 条目不存在返回 404
- [x] 单条目导出文件名含条目标题（特殊字符替换为 _）
- [x] 成长报告包含 4 个 section
- [x] Neo4j 不可用时知识图谱 section 显示'暂无数据'

## Code Review: pass
## Audit Result: done, commit_ready: true
