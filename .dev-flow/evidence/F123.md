# F123: Review 组件统一导出风格

## Task Snapshot

- **ID**: F123
- **Module**: review-components
- **Description**: 5 个 Review 组件统一为命名导出，移除 export default
- **Verification**: F1

## Execution Evidence

- **started_at**: 2026-04-25T10:20:00
- **completed_at**: 2026-04-25T10:20:00

### Verification

5 个组件均无 `export default` 行：
- InsightCard.tsx: `export function InsightCard` (named export only)
- AiSummaryCard.tsx: `export function AiSummaryCard` (named export only)
- HeatmapCard.tsx: `export function HeatmapCard` (named export only)
- MorningDigestCard.tsx: `export function MorningDigestCard` (named export only)
- GrowthCurveCard.tsx: `export function GrowthCurveCard` (named export only)

Review.tsx 全部使用 named import:
```typescript
import { MorningDigestCard } from "@/components/review/MorningDigestCard";
import { HeatmapCard } from "@/components/review/HeatmapCard";
import { GrowthCurveCard } from "@/components/review/GrowthCurveCard";
import { AiSummaryCard } from "@/components/review/AiSummaryCard";
import { InsightCard } from "@/components/review/InsightCard";
```

### Acceptance Check

| # | 验收条件 | 状态 |
|---|---------|------|
| 1 | 5 组件移除 export default 行 | ✅ 已无 export default |
| 2 | 所有导入改为命名导入 | ✅ Review.tsx 全部命名导入 |
| 3 | vitest 通过 | ✅ 366 passed |
| 4 | build 通过 | ✅ build success |

### Conclusion

R029 规划时发现的 F-015 双重导出问题，已在 R026-R033 期间被逐步修复。当前代码已满足全部验收条件，无需代码变更。
