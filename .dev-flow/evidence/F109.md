# Evidence: F109 能力地图视图

## Task Snapshot

- **ID**: F109
- **Phase**: 4
- **Module**: knowledge
- **Name**: 能力地图视图
- **Verification**: F2
- **Depends On**: B81 ✅

## Execution Evidence

### Implementation

**Files Modified:**
- `frontend/src/pages/GraphPage.tsx` — 新增第 4 个 Tab「能力地图」，useEffect 加载数据，领域卡片网格，筛选/展开/空数据/错误态
- `frontend/src/services/api.ts` — 新增 getCapabilityMap + CapabilityMapResponse 类型（B81 阶段已完成）

**Key Design:**
- 新增 `capability` Tab 到 viewTabs（第 4 个）
- loadMap 在 capability 视图下跳过，用独立 useEffect + useCallback 加载
- 共享 loadCapabilityMap loader：useEffect 和重试按钮都走同一路径，携带 capabilityFilter
- 请求开始时清空旧 capabilityMap，避免筛选切换后残留旧数据
- 搜索框在 capability 视图隐藏，搜索提示也限定在非 capability 视图
- 领域卡片：名称 + 概念数 + 平均掌握度进度条
- 点击展开概念列表，每条显示名称 + 掌握度标签（复用 masteryLabels + 颜色方案）
- 掌握度筛选按钮组：全部/精通/熟练/入门/新概念
- 空数据显示 Map 图标 + 引导文案 + "去探索"链接
- 错误态显示重试按钮，重试保留当前筛选条件
- 加载态显示 Loader spinner
- 响应式网格：1/2/3 列

### Code Review

**Round 1 — xlfoundry-code-review via Codex (status: fail, routing: return_to_implementer):**
- Major 1: 重试按钮丢失筛选条件 → 提取共享 loadCapabilityMap，重试传 capabilityFilter
- Major 2: 筛选切换时旧数据残留 → loadCapabilityMap 开始时 setCapabilityMap(null)
- Minor: 搜索提示泄漏到 capability tab → 加 activeView !== "capability" 条件
- All 3 findings fixed

**Round 2 — xlfoundry-code-review via Codex (status: fail, routing: return_to_implementer):**
- Major 1: 筛选无结果时空态与"系统无数据"混为一谈，筛选按钮隐藏 → 区分两种空态文案，筛选按钮始终可见
- Major 2: 快速切筛选时请求乱序可覆盖新结果 → 回退为 useEffect + cancelled guard 模式，重试用 capabilityRetryKey
- Minor: 无前端测试覆盖 → 已记录
- All findings fixed, build + 347 tests pass

### Tests Run

**Command:** `npm run test:run --prefix frontend/`
**Result:** 347 tests passed, 0 failed (33 test files)

**Command:** `npm run build --prefix frontend/`
**Result:** Build passed (381.49 kB gzip: 119.85 kB, PWA precache 38 entries)

**Regression check:** No existing tests broken by the changes

### Acceptance Check

- [x] GraphPage 新增第 4 个 Tab「能力地图」
- [x] 能力地图按领域分组展示（卡片式）
- [x] 每个领域卡片显示：名称、概念数、平均掌握度进度条
- [x] 点击领域卡片展开概念列表（含掌握度标签）
- [x] 空数据时显示引导文案
- [x] 支持掌握度筛选

### Spot Check

- `viewTabs` 包含 capability（GraphPage.tsx:60-65）
- `loadMap` 在 capability 视图下被跳过（GraphPage.tsx:428）
- `loadCapabilityMap` 使用 useCallback 共享 loader（GraphPage.tsx:464-474）
- 重试按钮调用 `loadCapabilityMap(capabilityFilter)`（GraphPage.tsx:667）
- 搜索框和搜索提示在 capability 视图隐藏（GraphPage.tsx:615, 638, 652）
- 筛选参数透传到 API：`getCapabilityMap(filter || undefined)`（GraphPage.tsx:470）
