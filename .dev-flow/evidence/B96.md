# B96: 修复 export_growth_report 趋势数据字段名

## Task Snapshot
- **ID**: B96
- **Module**: review
- **Status**: in_progress
- **Fix Level**: L1 (字段名拼写错误)
- **Root Cause**: review_service.py:1515 检查 `hasattr(trend_data, "daily_data")` 但 TrendResponse 模型字段为 `periods`，模型重命名时遗漏引用更新

## Execution Evidence

### Changes
- `backend/app/services/review_service.py` line 1515: `hasattr(trend_data, "daily_data")` → `hasattr(trend_data, "periods")`
- `backend/app/services/review_service.py` line 1518: `trend_data.daily_data` → `trend_data.periods`

### Tests Run
```bash
uv run pytest tests/unit/services/test_review_service.py::TestB96TrendDataFieldFix -v
# 3 passed
```

### Test Results
- `test_trend_data_with_periods_renders_weekly`: PASSED — 有趋势数据时正确渲染周维度趋势
- `test_trend_data_empty_periods_fallback`: PASSED — 空 periods 时 fallback 到"暂无数据"
- `test_regression_other_report_sections_intact`: PASSED — 其他 section 不受影响

### Regression
```bash
uv run pytest tests/ -q
# 1179 passed, 20 skipped
```

### Acceptance Check
1. [x] trend_data.daily_data 改为 trend_data.periods
2. [x] 有趋势数据时正确渲染周维度趋势
3. [x] 无趋势数据时正常 fallback 到"暂无数据"
4. [x] 现有测试通过（1179 pytest）
