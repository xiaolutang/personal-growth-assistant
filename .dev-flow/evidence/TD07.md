# TD07 taskStore 单元测试

## Task Snapshot

- **ID**: TD07
- **Phase**: P8
- **Priority**: high

### Acceptance Criteria

1. 创建 taskStore.test.ts ✓
2. 覆盖 fetchEntries（正常/失败） ✓ — 8 用例
3. 覆盖 addTasks（批量并行创建） ✓ — 4 用例
4. 覆盖 updateTaskStatus（状态变更） ✓ — 3 用例
5. 覆盖 deleteTask（乐观更新 + 失败回滚） ✓ — 4 用例
6. 覆盖 searchEntries（搜索结果存储） ✓ — 5 用例
7. 覆盖 getTodayTasks（日期过滤） ✓ — 6 用例
8. npm run test:run 通过 ✓ — 91/91 全部通过

### Changes

- 扩展 frontend/src/stores/taskStore.test.ts
- 8 个 describe 块，32 个测试用例
- 使用 vi.mock 隔离 API 调用
- 工厂函数 createMockTask / createMockSearchResult
- getTodayTasks 使用 vi.useFakeTimers 固定日期

## Quality Gates

### xlfoundry-code-review (2026-04-12)
- status: pass
- 审查范围：frontend/src/stores/taskStore.test.ts
- 维度评估：
  - 架构与系统性：pass — 8 个 describe 块按功能分组（fetchEntries/addTasks/updateTaskStatus/deleteTask/searchEntries/getTodayTasks），结构清晰
  - 复用与抽象：pass — createMockTask/createMockSearchResult 工厂函数复用，beforeEach 统一提升消除 7 处重复
  - 代码质量：pass — vi.mock 隔离 API 调用，fake timers 消除日期边界不确定性，91 测试全部通过
  - 效率与可测试性：pass — 32 个测试用例覆盖正常/失败/边界场景，resetStore 使用 setState 标准方式
- routing: pass

### Simplify

- resetStore 从直接 mutation 改为 useTaskStore.setState()
- beforeEach 统一提升到顶层，去除 7 处重复
- getTodayTasks 使用 fake timers 替代 new Date()，消除午夜边界不确定性

### Verification

- 前端测试通过（91/91）
- TypeScript 编译通过

## Audit

本地审核：done, commit_ready=true
