# B76: 导出 API E2E（R020）

## Task Snapshot

- **ID**: B76
- **Name**: 导出 API E2E
- **Module**: e2e-export
- **Phase**: 4 (R020)
- **Status**: completed
- **Started**: 2026-04-20
- **Completed**: 2026-04-20

## Acceptance Criteria

1. markdown 导出 → 200 + content-type=application/zip
2. json 导出 → 200 + JSON 数组 + 包含已创建条目
3. 按 type=task 过滤 → 只含 task 类型
4. 按 type=decision 过滤 → 只含 decision 类型（跨模块闭环）
5. 按 type=reflection 过滤 → 只含 reflection 类型
6. 按 type=question 过滤 → 只含 question 类型
7. 按日期范围过滤 → 只含范围内条目
8. 无效 format → 422
9. 无效 type → 422
10. 空数据导出 → json 返回空数组
11. 未认证 → 401

## Execution Evidence

### Changes

1. **e2e/tests/export-api.spec.ts**（新增 ~180 行）— 11 个 E2E 测试：

   **格式验证（2 个）**：markdown → application/zip / json → 数组含条目
   **类型过滤（4 个）**：task / decision（跨模块闭环）/ reflection / question
   **日期范围（1 个）**：start_date + end_date 过滤
   **无效参数（2 个）**：format 422 / type 422
   **边界（1 个）**：空数据 json 空数组
   **认证（1 个）**：未认证 401

### tests_run

| Command | Result |
|---------|--------|
| `cd e2e && npx playwright test tests/export-api.spec.ts` | 11 passed (18s) |

### Acceptance Check

| # | Criteria | Status |
|---|----------|--------|
| 1 | markdown → zip | ✅ |
| 2 | json → 数组含条目 | ✅ |
| 3 | type=task | ✅ |
| 4 | type=decision（跨模块） | ✅ |
| 5 | type=reflection | ✅ |
| 6 | type=question | ✅ |
| 7 | 日期范围 | ✅ |
| 8 | 无效 format → 422 | ✅ |
| 9 | 无效 type → 422 | ✅ |
| 10 | 空数据 → [] | ✅ |
| 11 | 未认证 → 401 | ✅ |

## Runtime Verification

- 类型: API 级 E2E（Playwright APIRequestContext）
- 依赖: 后端 uvicorn + vite proxy
- 结果: 11 passed (18s)
