# Evidence: F11 搜索增强

## Task Snapshot

| Field | Value |
|-------|-------|
| ID | F11 |
| Phase | P2B |
| Module | explore |
| Priority | medium |
| Verification | F2 |
| Status | completed |
| Started At | 2026-04-15 |
| Completed At | 2026-04-15 |

## Execution Evidence

### 改动文件

1. `frontend/src/pages/Explore.tsx` — Cmd+K 全局聚焦 + highlightKeyword 传递
2. `frontend/src/components/TaskList.tsx` — 透传 highlightKeyword
3. `frontend/src/components/TaskCard.tsx` — HighlightText 组件 + 关键词高亮

### tests_run

| Suite | Result |
|-------|--------|
| Frontend build | PASS |
| Frontend vitest (231) | PASS — 含 6 个 HighlightText 测试 |

### acceptance_check

| 验收条件 | 状态 |
|----------|------|
| 输入 debounce 300ms 后自动触发搜索 | PASS — F10 已实现 |
| 搜索结果中关键词高亮 | PASS — HighlightText + mark 标签 |
| Cmd+K 全局快捷键聚焦搜索框 | PASS — keydown 事件监听 |
| 搜索无结果时显示引导文案 | PASS — emptyMessage |
| npm run build 构建通过 | PASS |

### runtime_verification

| 项目 | 结果 |
|------|------|
| Cmd+K 聚焦 | PASS — searchInputRef + focus() |
| 关键词高亮 | PASS — case-insensitive match + mark 标签 |
| Tab 约束搜索 | PASS — filterByCategory 已覆盖 |

## xlfoundry-code-review

### R1 (Codex Plugin — fail)

- HighlightText 只高亮首个匹配，未全量高亮
- 缺少 HighlightText 定向测试
- 修复：改用 matchAll + 正则获取原始字符串索引（避免 toLowerCase 索引错位）
- 新增 6 个测试：全量高亮、大小写不敏感（断言 mark）、无匹配、空关键词、特殊字符转义（断言 mark）、Unicode 索引安全（Straße 精确高亮 + 负向断言）

### R2-R4 (Codex Plugin — conditional_pass → pass)

多轮修复后实现安全，测试覆盖充分。matchAll + 正则转义方案安全，Unicode 边界已锁定。

## Audit Result

status: done, commit_ready: true
