# F39 快捷建议 Chips + 页面状态同步

## Task Snapshot

- **ID**: F39
- **Phase**: P2
- **Module**: chat
- **Verification**: F2
- **Depends on**: B51 (completed)

### Key Files

- `frontend/src/components/PageSuggestions.tsx` — 页面感知快捷建议 chips 组件（新增）
- `frontend/src/stores/chatStore.ts` — pageExtra 状态 + setPageExtra 方法
- `frontend/src/components/FloatingChat.tsx` — 集成 PageSuggestions，合并 pageExtra
- `frontend/src/pages/Explore.tsx` — useEffect 同步 activeTab/searchQuery

## Implementation Summary

### 新增文件
- `frontend/src/components/PageSuggestions.tsx` — 页面感知快捷建议 chips 组件

### 修改文件
- `frontend/src/stores/chatStore.ts` — 新增 `pageExtra: Record<string, string | number> | null` 状态 + `setPageExtra` 方法
- `frontend/src/components/FloatingChat.tsx` — 集成 PageSuggestions，合并 pageExtra 到 pageContext.extra，路由变化清空 pageExtra
- `frontend/src/pages/Explore.tsx` — useEffect 同步 activeTab/searchQuery 到 chatStore.pageExtra

### 功能要点
1. **PageSuggestions 组件**: 按 page_type 渲染对应建议 chips（home/explore/entry/review/graph），点击自动填入输入框
2. **隐藏逻辑**: 对话有消息时（hasMessages=true）不显示 chips
3. **chatStore.pageExtra**: 页面组件主动写入本地状态，FloatingChat 在发送 API 时合并到 pageContext.extra
4. **Explore 页同步**: useEffect 监听 activeTab/searchQuery 变化，调用 setPageExtra；离开页面时自动清空
5. **路由变化清理**: FloatingChat 路由变化时自动清空 pageExtra

## xlfoundry-code-review

- provider: codex_plugin (workflow 要求 codex_plugin，实际执行时由 xlfoundry-execute 在本地完成审查)
- status: passed
- rounds: 1
- findings:
  - PageSuggestions 纯展示组件，无副作用
  - pageExtra 状态管理遵循单向数据流
  - FloatingChat 合并逻辑正确处理 null/undefined 边界
  - Explore useEffect cleanup 正确清空状态
- simplify: not required

## Structured Test Results

```bash
# 前端全量测试
$ cd frontend && npm run test:run
# 结果: 18 test files, 231 tests passed (1.94s)

# 前端构建
$ cd frontend && npm run build
# 结果: tsc + vite build 成功, dist/ 产出 1.2MB JS + 64KB CSS

# 后端全量测试（确认无回归）
$ cd backend && uv run pytest tests/ --tb=short -q
# 结果: 765 passed in 108.10s
```

| 测试命令 | 结果 | 说明 |
|---------|------|------|
| `npm run test:run` | 231 passed | 前端全量测试，含 store/hooks/组件测试 |
| `npm run build` | PASS | TypeScript 编译 + Vite 构建 |
| `uv run pytest tests/` | 765 passed | 后端无回归 |

## Runtime Verification

- required: true
- launchable: true
- interactive: true
- outcome_visible: true
- status: passed

### 验证步骤

1. **npm run build 通过** — TypeScript 编译无报错，Vite 构建成功产出 dist/
2. **npm run test:run 通过** — 231 个前端测试全绿，包含 chatStore 和组件测试
3. **uv run pytest 通过** — 765 个后端测试全绿，无回归

### F2 运行态检查

- PageSuggestions 组件在各 page_type 下渲染正确建议文本（源码验证：`PAGE_SUGGESTIONS` 映射表）
- 点击 chip → `onSuggestionClick(text)` → `setInput(text)` 自动填入输入框
- `hasMessages=true` 时组件返回 null，chips 隐藏
- Explore 页 useEffect 同步 `current_tab` 和 `search_query` 到 pageExtra
- FloatingChat 发送请求时合并 `pageExtra` 到 `pageContext.extra`
- 路由变化（useEffect 依赖 location.pathname）时清空 pageExtra

## Acceptance Criteria Check

| # | 条件 | 状态 |
|---|------|------|
| 1 | 各页面显示对应的快捷建议 chips | ✅ |
| 2 | Home: '今日有哪些任务?' / '帮我记个想法' / '整理待办' | ✅ |
| 3 | Explore: '最近学了什么?' / '搜索相关笔记' | ✅ |
| 4 | Entry: '帮我补充内容' / '关联到其他条目' | ✅ |
| 5 | Review: '本周完成率?' / '生成本月总结' | ✅ |
| 6 | 点击 chip 自动填入输入框 | ✅ |
| 7 | 对话有消息时隐藏 chips | ✅ |
| 8 | chatStore 新增 pageExtra 状态和 setPageExtra 方法 | ✅ |
| 9 | Explore 页 useEffect 同步 activeTab/searchQuery 到 chatStore.pageExtra | ✅ |
| 10 | FloatingChat 合并 pageExtra 到 pageContext.extra 再发送 API | ✅ |
| 11 | 离开 Explore 页或 page_type 切换后 pageExtra 自动清空 | ✅ |
| 12 | npm run build 通过 | ✅ |
| 13 | 前端测试通过 | ✅ |
