# F06 — Frontend 死代码清理

## 执行时间
2026-05-08

## 变更内容

### 删除文件
| 文件 | 原因 |
|------|------|
| `frontend/src/components/AgentChat/AgentChat.tsx` | 容器组件(162行)，FloatingChat 绕过它直接引用子组件 |
| `frontend/src/components/KnowledgeGraph.tsx` | KnowledgeGraph + KnowledgeGraphInline 两个导出组件无消费者 |
| `frontend/src/components/SearchResultCard.tsx` | 无外部消费者 |
| `frontend/src/components/ActionIndicator.tsx` | 无外部消费者 |

### 修改文件
| 文件 | 变更 |
|------|------|
| `frontend/src/components/AgentChat/index.ts` | 移除 `export { AgentChat } from "./AgentChat"` |

## 引用确认（grep 验证）
- `AgentChat` 组件：仅自身文件和 index.ts 引用，FloatingChat 只引用子组件(MessageList/ChatInput/ThinkingIndicator/AgentPrompt)
- `KnowledgeGraph`/`KnowledgeGraphInline`：仅自身文件定义，KnowledgeGraphThumbnail 在独立文件中不受影响
- `SearchResultCard`：仅自身文件内引用（含文件底部自我引用）
- `ActionIndicator`：仅自身文件内引用

## 验证结果
- vitest: 78 files, 905 tests passed
- build: 成功，无未解析导入
- FloatingChat 子组件导入路径不变，功能不受影响
