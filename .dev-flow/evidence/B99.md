# B99: tag_stats SQL 聚合升级

## Task Snapshot
- **ID**: B99
- **Module**: review-service-tags
- **Status**: completed
- **Fix Level**: L1 (性能优化)

## Execution Evidence

### Changes
1. **修改 `backend/app/services/review_service.py`** — `_compute_30d_tag_stats` 从 ~13 行 list_entries 循环改为 ~8 行 SQL 聚合（`get_tag_stats_in_range`）
2. **修改 `backend/app/infrastructure/storage/sqlite.py`** — 新增 `get_tag_stats_in_range` SQL 方法，含确定性排序 `ORDER BY freq DESC, tag_name ASC`
3. **新建 `backend/tests/unit/test_tag_stats_sql.py`** — 6 个 service 层委托测试
4. **新建 `backend/tests/unit/storage/test_get_tag_stats_in_range.py`** — 8 个真实 SQLite storage 测试

### Tests Run
```bash
uv run pytest tests/unit/test_tag_stats_sql.py -v
# 6 passed
uv run pytest tests/unit/storage/test_get_tag_stats_in_range.py -v
# 8 passed
uv run pytest tests/ -q
# 1213 passed, 20 skipped (含全部新增测试)
```

### Test Results
- test_tag_stats_sql (service mock): 6 项 — 频次排序、空列表、top_n 限制、频次一致、用户 ID 隔离、回归验证不调用 list_entries
- test_get_tag_stats_in_range (real SQLite): 8 项 — 基本频次、确定性 tie-break、start_date 包含、end_date 排除、空范围、top_n、用户隔离、重复 tag 去重

### Acceptance Check
1. [x] `_compute_30d_tag_stats` 使用 `get_tag_stats_in_range` SQL 聚合
2. [x] 不再调用 `list_entries`（回归测试验证）
3. [x] 排序确定性（`ORDER BY freq DESC, tag_name ASC` + tie-break 测试）
4. [x] 用户隔离（`WHERE e.user_id = ?` + 隔离测试）
5. [x] 日期边界正确（`created_at >= start AND created_at < end + \uffff` + 边界测试）
6. [x] top_n 限制（`LIMIT ?` 参数化 + 测试）
7. [x] 测试覆盖充分（6 service + 8 storage + 回归 0 failed）

### Code Review
- 第一轮: fail（排序缺 tie-break + 缺真实 SQLite 测试）
- 修复: 添加 `tag_name ASC` tie-break + 新增 8 个 storage 测试
- 第二轮: pass

## Audit Result
- status: partial → 回写完成后待重新审计
