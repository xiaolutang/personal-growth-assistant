# B04: Backend — feedback sync N+1 并发化 + AnalyticsService 连接修复

## 状态：已完成

## 变更文件

| 文件 | 变更 |
|------|------|
| `backend/app/routers/feedback.py` | sync_feedbacks 并发化 + asyncio.get_event_loop() 替换 |
| `backend/app/services/analytics_service.py` | 改用 _conn() 上下文管理器 |
| `backend/tests/unit/api/test_b04_feedback_concurrent_sync.py` | 新增 13 个测试 |

## 验收标准

- [x] sync_feedbacks 使用 asyncio.gather 并发请求，semaphore 限流 <=10
- [x] feedback.py 中 asyncio.get_event_loop() 替换为 asyncio.get_running_loop()
- [x] AnalyticsService 使用 _conn() 上下文管理器
- [x] 不再有手动 get_connection + commit + close 模式
- [x] 后端无 DeprecationWarning（asyncio.get_event_loop 已全部替换）
- [x] pytest 全部通过（1451 passed）

## 测试覆盖

| 测试类 | 场景 | 数量 |
|--------|------|------|
| TestConcurrentSync | gather 并发 + semaphore 限流 | 2 |
| TestTimeoutIsolation | 单条超时不阻塞其他 | 1 |
| TestNon200Isolation | 单条 500 不影响其他 | 1 |
| TestUnknownStatus | 未知状态值跳过 | 1 |
| TestMixedSuccessFailure | 部分成功部分失败 | 1 |
| TestNoDeprecationWarning | 无 get_event_loop() | 1 |
| TestAnalyticsServiceConnManager | 连接管理回归 | 6 |

## Code Review

- provider: local（codex 不可用）
- status: pass
- routing: pass

## Audit

- provider: local
- status: done
- commit_ready: true

## 技术细节

### sync_feedbacks 并发化
- 使用 `asyncio.Semaphore(10)` 限制并发上限
- `asyncio.gather(*[_sync_one(fb, http_client)], return_exceptions=True)` 并发请求
- `_sync_one` 返回 `(was_synced, was_updated)` 元组区分计数
- 单条超时/HTTP 错误/未知状态均不影响其他反馈

### AnalyticsService 连接管理
- `ensure_table` 和 `record_event` 均改用 `with self._sqlite._conn() as conn:`
- 消除了手动 `get_connection()` + `conn.commit()` + `conn.close()` 模式
- 移除了 `sqlite3` import 和 `_get_conn` 方法
