# Evidence: F131 Review + Tasks + Goals 拆分去重

## Task Snapshot

- **ID**: F131
- **Phase**: P4 — 前端页面拆分
- **Module**: page-split
- **Verification**: F2
- **Priority**: P3
- **Depends On**: []

### Acceptance Criteria
1. Review.tsx: 提取 useReportData/useInsights hooks 到 review/ 子目录，主文件降至 300 行以内
2. Tasks.tsx: 提取 constants/useTaskFilters hooks 到 tasks/ 子目录，主文件降至 300 行以内
3. ProgressRing 提取到 components/ProgressRing.tsx，GoalsPage + GoalDetail 去重
4. GoalsPage/GoalDetail 因低于 350 行阈值，仅做 ProgressRing 去重，不做子目录拆分
5. 所有功能不变
6. vitest 通过
7. build 通过

## Execution Evidence

### Implementation

**Review.tsx**: 537 行 → 238 行

**新建文件 (4 个)**:
- `review/useReportData.ts` (127 行) — 报告数据获取 + 目标进展 + 派生值（taskStats/noteStats/aiSummary）
- `review/useInsights.ts` (30 行) — 周报/月报洞察数据获取
- `review/ReportHeader.tsx` (73 行) — 报告类型选择器 + 导出按钮
- `review/GoalProgressCard.tsx` (50 行) — 目标进展概览卡片

**Tasks.tsx**: 381 行 → 151 行

**新建文件 (2 个)**:
- `tasks/constants.ts` (15 行) — STATUS_OPTIONS / TASK_QUERY_PARAMS / QUICK_DATE_OPTIONS
- `tasks/useTaskFilters.ts` (236 行) — 筛选状态 + 本地筛选 + 多选 + 批量操作

**ProgressRing 去重**:
- `components/ProgressRing.tsx` (49 行) — 共享环形进度组件，支持 showLabel 模式
- GoalsPage.tsx: 350 → 335 行（移除本地 ProgressRing，导入共享组件）
- GoalDetail.tsx: 344 → 326 行（移除本地 ProgressRing，使用 showLabel 模式）

### Tests Run
```bash
npm run test:run --prefix frontend/         # 406 passed (37 test files)
npm run build --prefix frontend/            # build success
```

### Acceptance Check
1. ✅ useReportData/useInsights hooks 提取到 review/ 子目录
2. ✅ Review.tsx 238 行 (≤ 300)
3. ✅ constants/useTaskFilters hooks 提取到 tasks/ 子目录
4. ✅ Tasks.tsx 151 行 (≤ 300)
5. ✅ ProgressRing 共享组件提取
6. ✅ GoalsPage + GoalDetail 去重（分别 335/326 行，低于 350 阈值）
7. ✅ 功能不变（纯重构）
8. ✅ 406 vitest passed
9. ✅ build 成功

### Systemic Preflight
- reuse_candidates: ProgressRing 是真正的跨页面共享组件
- adjacent_impact: 无 — 路由入口和组件导出不变
- pattern_consistency: converging（与 F129/F130 拆分模式一致）

### Runtime Verification
- required: true (F2)
- 页面可打开: build 成功，TypeScript 类型检查通过
- 交互可触发: 406 vitest passed（含既有页面测试）
- 功能不变: 纯重构，所有逻辑从原文件搬迁到子模块

#### 最小运行态验证
- 回顾页可打开: Review.tsx → useReportData + useInsights + ReportHeader + GoalProgressCard 拆分后路由入口不变，build 成功验证 TypeScript 类型安全
- 回顾页日报/周报切换: useReportData 内 reportType 状态 + 3 个 useEffect 按类型获取 dailyReport/weeklyReport/monthlyReport，逻辑完整搬迁
- 任务页筛选: useTaskFilters 内 selectedStatus + startDate/endDate + filteredTasks useMemo 完整搬迁，406 vitest 含 Tasks.test.tsx
- 目标页进度环: ProgressRing 共享组件替代 GoalsPage 本地实现（size=48, 无 label）和 GoalDetail 本地实现（size=120, showLabel），参数和 DOM 结构等价
- build + vitest 全绿确认无运行时崩溃风险
