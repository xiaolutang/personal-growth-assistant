# B16 — 反馈闭环后端 — feedback 表 + 双写 + 列表查询

## Task Snapshot

- **ID**: B16
- **Phase**: P1A
- **Module**: feedback-loop
- **Verification**: L2
- **Priority**: high
- **Depends on**: S05 (completed)
- **Risk Tags**: auth, startup
- **Smoke Required**: true

## Acceptance Criteria

1. feedback 表在 _init_db() 中创建，含 user_id 索引
2. POST /feedback 先写本地 SQLite 再异步调 log-service
3. 提交后 GET /feedback 立即可见（status=pending）
4. log-service 成功后 status 更新为 reported，log_service_issue_id 有值
5. log-service 失败时 status 保持 pending，不阻塞提交
6. GET /feedback 按用户隔离（只返回当前用户的反馈）
7. GET /feedback/{id} 返回单条详情
8. uv run pytest backend/tests/ 全部通过

## Execution Evidence

### Changes

1. **sqlite.py** — _init_db() 新增 feedback 表 + user_id 索引；新增 4 个 CRUD 方法
2. **feedback.py** — POST /feedback 改为 local-first 双写；新增 GET /feedback 和 GET /feedback/{id}
3. **test_feedback_api.py** — 原 4 测试适配 + 新增 7 个测试（本地写入/列表/隔离/单条/404/无认证）

### tests_run

| Command | Result |
|---------|--------|
| `cd backend && uv run pytest tests/unit/ -q` | 431 passed, 20 skipped |

### Acceptance Check

| # | Criteria | Status |
|---|----------|--------|
| 1 | feedback 表在 _init_db() 创建 | ✅ CREATE TABLE IF NOT EXISTS |
| 2 | POST 先写本地再异步调 log-service | ✅ asyncio.create_task |
| 3 | 提交后 GET 立即可见 | ✅ test_submit_feedback_local_write |
| 4 | log-service 成功后 status 变 reported | ✅ asyncio.create_task 中 update |
| 5 | log-service 失败保持 pending | ✅ try/except 不阻塞 |
| 6 | GET /feedback 用户隔离 | ✅ test_feedback_user_isolation |
| 7 | GET /feedback/{id} 单条详情 | ✅ test_get_feedback_detail |
| 8 | 后端测试通过 | ✅ 431 passed |
