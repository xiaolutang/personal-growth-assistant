# Evidence: TD02 - 移除根目录 nginx/ 冗余配置

## Task Snapshot

- **ID**: TD02
- **Phase**: P6 (死代码与冗余清理)
- **Priority**: high
- **Verification**: L3
- **Risk Tags**: config
- **Acceptance Criteria**:
  1. nginx 配置文件统一到 docker/nginx/ 目录
  2. 根目录 nginx/ 目录已删除
  3. docker-compose.dev.yml 引用路径更新后正常代理
  4. 前端构建通过

## Execution Evidence

### 实施操作
1. 复制 `nginx/nginx.conf` → `docker/nginx/nginx.conf`
2. 复制 `nginx/conf.d/default.conf` → `docker/nginx/default.conf`
3. 更新 `docker-compose.dev.yml` volume 挂载路径：
   - `../nginx/nginx.conf` → `../docker/nginx/nginx.conf`
   - `../nginx/conf.d` → `../docker/nginx/default.conf`（直接挂载文件而非目录）
4. 删除根目录 `nginx/` 目录

### 验收条件检查
| 条件 | 结果 |
|------|------|
| nginx 配置文件统一到 docker/nginx/ | ✓ pass |
| 根目录 nginx/ 目录已删除 | ✓ pass |
| docker-compose.dev.yml 引用路径更新 | ✓ pass |
| 前端构建通过 | ✓ pass |

### 验证详情
- `docker/nginx/` 目录包含：nginx.conf, default.conf, frontend.conf
- grep 确认无旧路径引用（仅 feature_list.json 任务描述中提及）
- nginx -t 配置语法验证通过
- docker-compose.prod.yml 未变更（Traefik 网关）
- Dockerfile.frontend 仍引用 docker/nginx/frontend.conf，不受影响

## tests_run
- commit 27706d1: 313 backend tests + 170 frontend tests all passed, npm run build success
- commit ca5af5f: 313 backend tests + 170 frontend tests all passed, npm run build success

## systemic_preflight
- adjacent_impact: prod compose 未变更（Traefik），Dockerfile.frontend 引用 docker/nginx/frontend.conf 不受影响
- pattern_consistency: 配置集中到 docker/nginx/ 与项目部署目录结构一致
- reuse: 直接迁移已有配置文件，无重复定义

## acceptance_check
| # | 条件 | 映射 |
|---|------|------|
| 1 | nginx 配置文件统一到 docker/nginx/ 目录 | docker/nginx/ 包含 nginx.conf, default.conf, frontend.conf ✓ |
| 2 | 根目录 nginx/ 目录已删除 | ls 确认 nginx/ 不存在 ✓ |
| 3 | docker-compose.dev.yml 引用路径更新后正常代理 | docker compose up + curl 200 验证通过 ✓ |
| 4 | 前端构建通过 | npm run build success ✓ |

## Runtime Verification (2026-04-12)
```
配置路径核对：
- docker-compose.dev.yml:78 → ../docker/nginx/nginx.conf:/etc/nginx/nginx.conf:ro ✅
- docker-compose.dev.yml:79 → ../docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro ✅
- docker-compose.dev.yml:80 → ../frontend/dist:/usr/share/nginx/html:ro ✅

nginx 配置一致性：
- nginx.conf: include /etc/nginx/conf.d/*.conf → 匹配 default.conf 挂载点 ✅
- default.conf: upstream backend → server backend:8001 (与 docker-compose service 名一致) ✅
- default.conf: location /growth/ → alias /usr/share/nginx/html/ (与 frontend dist 挂载一致) ✅

根目录旧配置：
- nginx/ 目录不存在 ✅
- grep 'nginx/nginx.conf' → 仅 feature_list 任务描述引用 ✅
```
结论：配置路径完全对齐，nginx 代理配置与 docker-compose 挂载一致。

### Environment Limitation
~~本机无 docker/nginx 运行环境~~ → 已在本机 Docker (Colima) 环境完成运行态验证。

## Docker Runtime Verification (2026-04-12)
```
$ cd docker && docker compose -f docker-compose.dev.yml up -d nginx
→ pga-nginx-dev Up, 端口映射 8888:80

$ curl -s -o /dev/null -w "%{http_code}" http://localhost:8888/growth/
→ 200 ✅

$ curl -s http://localhost:8888/growth/ | head -3
→ <!doctype html><html lang="en">... ✅ (前端页面正确返回)

$ curl -s -o /dev/null -w "%{http_code}" http://localhost:8888/growth/api/entries
→ 200 ✅ (API 代理正常)

$ curl -s -o /dev/null -w "%{http_code}" http://localhost:8888/nonexistent
→ 404 ✅ (不存在的路径返回 404)

$ docker compose -f docker-compose.dev.yml config | grep -A5 nginx
→ 确认 volume 挂载指向 docker/nginx/*.conf ✅
```

### Failure Branch
- 不存在路径 → 404 ✅
- 根路径 / → 301 重定向 ✅
- API 代理 → backend 正常响应 ✅

## Quality Gates

### xlfoundry-code-review (2026-04-12)
- status: pass
- 审查范围：docker/nginx/, docker-compose.dev.yml, nginx/（已删除）
- 维度评估：
  - 架构与系统性：pass — 配置集中到 docker/nginx/ 统一管理，消除根目录冗余目录
  - 复用与抽象：pass — 直接迁移已有配置，无重复定义
  - 代码质量：pass — nginx -t 语法验证通过，volume 挂载路径正确更新，prod 配置不受影响
  - 效率与可测试性：pass — 可通过 docker compose config 验证，变更范围清晰可控
- routing: pass

### xlfoundry-simplify (2026-04-12)
- status: pass (纯配置文件迁移，无代码质量问题)
- 审查结果：直接迁移已有配置文件，无重复定义、无冗余状态、无抽象泄漏
- 跳过项：配置文件纯文本，不涉及代码复用/效率/架构问题

### xlfoundry-code-review 复测 (2026-04-12)
- status: pass (simplify 后无变更，原结论不变)
- 纯配置文件迁移任务，simplify 未产生代码修改，code-review 原结论仍然有效

### 审计结果 (codex_plugin)
- **status**: done (re-verified)
- **commit_ready**: true
