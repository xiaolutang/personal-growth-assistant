# B53 知识热力图 Neo4j 数据源升级

## Task Snapshot

- **ID**: B53
- **Phase**: P1
- **Module**: review
- **Verification**: L2
- **Depends on**: 无
- **Priority**: 1

### Key Files

- `backend/app/services/review_service.py` — get_knowledge_heatmap() Neo4j 优先 + SQLite 降级
- `backend/app/routers/deps.py` — ReviewService 注入 neo4j_client
- `backend/tests/unit/api/test_review_b53.py` — 32 测试用例

## Implementation Summary

### 修改文件

1. `backend/app/services/review_service.py`
   - HeatmapItem 增加 mention_count: int = 0, category: Optional[str] = None
   - ReviewService.__init__ 增加 neo4j_client=None 参数
   - get_knowledge_heatmap() 重写：Neo4j 优先 + try/except 降级 SQLite
   - _get_heatmap_from_sqlite() SQLite 降级路径（原逻辑提取）
   - _count_relationships_per_concept() 统计关系数
   - _neo4j_concept_to_heatmap() Neo4j 概念转换
   - _calculate_mastery_from_stats 增强：新增 relationship_count 参数

2. `backend/app/routers/deps.py`
   - get_review_service() 注入 storage.neo4j 到 ReviewService

3. `backend/tests/unit/api/test_review_b53.py`（新增）— 32 测试

## Structured Test Results

```bash
$ cd backend && uv run pytest tests/unit/api/test_review_b53.py tests/unit/api/test_review_b52.py tests/unit/api/test_review_api.py -q
# 结果: 87 passed (32 B53 + 13 B52 + 42 review_api + 回归)
```

| 测试 | 验证内容 | 结果 |
|------|---------|------|
| ReviewService neo4j_client 注入 | 构造函数接受可选参数 | ✅ |
| Neo4j 可用时使用增强数据 | Mock Neo4j 返回概念列表 | ✅ |
| Neo4j 空结果降级 SQLite | Mock 返回空列表 → 走 SQLite | ✅ |
| Neo4j 异常降级 SQLite | Mock 抛异常 → 不崩溃 | ✅ |
| 掌握度增强含 relationship_count | 综合评分公式验证 | ✅ |
| HeatmapItem category 字段 | 新字段存在且可序列化 | ✅ |
| HeatmapItem mention_count 字段 | 新字段存在 | ✅ |
| 按 mastery 排序 | advanced > intermediate > beginner > new | ✅ |
| 用户隔离（API 层） | 用户 A/B 数据互不影响 | ✅ |

## xlfoundry-code-review

- status: passed
- findings:
  - Neo4j 降级逻辑正确（try/except 包裹）
  - 掌握度增强公式合理
  - 测试覆盖 Neo4j 路径（mock）和 SQLite 降级路径
- simplify: not required

## Acceptance Criteria Check

| # | 条件 | 状态 |
|---|------|------|
| 1 | Neo4j 优先 get_all_concepts_with_stats() | ✅ |
| 2 | 数据含 category, mention_count | ✅ |
| 3 | Neo4j 不可用降级 SQLite tags | ✅ |
| 4 | 掌握度算法考虑 relationship_count | ✅ |
| 5 | HeatmapItem 增加 category | ✅ |
| 6 | 按 mastery 分组排序 | ✅ |
