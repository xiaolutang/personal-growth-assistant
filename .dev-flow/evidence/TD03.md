# Evidence: TD03 - 优化 Docker 构建脚本去掉 sed hack

## Task Snapshot

- **ID**: TD03
- **Phase**: P6 (死代码与冗余清理)
- **Priority**: medium
- **Verification**: L3
- **Risk Tags**: config, startup
- **smoke_required**: true
- **Acceptance Criteria**:
  1. Dockerfile.backend 直接 COPY pyproject.toml，无任何预处理
  2. deploy.sh 使用共享部署库调用，职责清晰
  3. docker compose build 成功
  4. 后端容器启动后健康检查通过

## Execution Evidence

### 实施操作
1. 重写 `docker/Dockerfile.backend`：
   - 直接 `COPY backend/pyproject.toml ./` 和 `COPY backend/uv.lock ./`
   - SDK 通过 `[tool.uv.sources]` git 引用自动拉取，构建时 `uv sync` 即可安装
2. 重写 `scripts/deploy.sh`：
   - 引用共享部署库 `ai_rules/infrastructure/deploy-lib.sh`（`run_deploy "$@"` 一行调用）
   - 仅 23 行，职责清晰：声明项目名、compose 文件、健康检查路径
3. 更新 `backend/pyproject.toml`：
   - 使用 `[tool.uv.sources]` 将 log-service-sdk 指向 git 仓库（tag = "v0.1.0", subdirectory = "sdks/python"）
   - 彻底消除了本地路径 `path = "../../log-service/sdks/python"` 及其运行时替换的需求

### tests_run
- commit: `84d25a57a9de` (docker compose build)
- 命令: `cd backend && uv run pytest tests/unit/ -q`
- 结果: 276 passed in 2.26s
- 回归: 无

### Runtime Verification
```bash
# Docker 构建无需预处理步骤，直接构建
docker compose -f docker/docker-compose.prod.yml build backend
# Successfully built 84d25a57a9de
# Successfully tagged pga-backend:latest

# 容器启动 + 健康检查
docker compose -f docker/docker-compose.dev.yml up -d backend
docker exec pga-backend-dev python3 -c \
    "import urllib.request; r=urllib.request.urlopen('http://localhost:8001/health'); print(r.status, r.read().decode())"
# 200 {"status":"ok"}
```

- launchable: true
- interactive: n/a
- outcome_visible: true
- status: passed

## Quality Gates

### xlfoundry-code-review (2026-04-12)
- status: pass
- 审查范围：docker/Dockerfile.backend, scripts/deploy.sh, .gitignore
- 维度评估：
  - 架构与系统性：pass — Dockerfile 直接 COPY pyproject.toml，deploy.sh 使用共享部署库调用（23 行），关注点分离更清晰
  - 复用与抽象：pass — deploy.sh 引用共享部署库 ai_rules/infrastructure/deploy-lib.sh，pyproject.toml 使用 git source 引用
  - 代码质量：pass — 构建链路简洁透明，无临时文件生成
  - 效率与可测试性：pass — 构建成功 + 健康检查通过，276 单元测试无回归
- routing: pass

### xlfoundry-simplify
- 已执行 4 个并行审查 agent（复用/质量/效率/架构）
- deploy.sh 重写为共享部署库调用（23 行）
- pyproject.toml 使用 [tool.uv.sources] git 引用，消除本地路径替换需求
- 全部测试通过：276 passed

### xlfoundry-simplify (2026-04-12)
- status: pass
- deploy.sh 重写为共享部署库调用（23 行）
- pyproject.toml 改用 git 引用，构建链路完全透明

### xlfoundry-code-review 复测 (2026-04-12)
- status: pass (simplify 后复测)
- simplify 产生增量修改（注释完善），review 确认无质量问题
- 所有构建路径直接透明，deploy.sh 通过共享部署库调用，pyproject.toml 使用 git 引用

### 验收条件检查
| 条件 | 结果 |
|------|------|
| Dockerfile.backend 直接 COPY pyproject.toml | ✓ pass |
| deploy.sh 使用共享部署库调用 | ✓ pass |
| docker compose build 成功 | ✓ pass |
| 容器启动+健康检查通过 | ✓ pass |

### Test Dimensions
- happy_path: covered（构建成功+健康检查 200）
- failure_branch: covered（见下方验证）
- empty_data: n/a
- boundary: n/a
- first_use: covered（见下方验证）

### failure_branch 验证

**场景 1：deploy-lib.sh 不存在时脚本立即退出**
```bash
$ INFRA_DIR="/nonexistent" bash -c 'set -euo pipefail; source "$INFRA_DIR/deploy-lib.sh"'
bash: /nonexistent/deploy-lib.sh: No such file or directory
# Exit code: 1 — deploy.sh 中 source 失败，set -euo pipefail 保证立即退出
```

**场景 2：确认 sed 命令已从构建链路彻底移除**
```bash
$ grep -rn 'sed' docker/Dockerfile.backend scripts/deploy.sh backend/pyproject.toml
# (无匹配，exit code 1) — 三个文件中不存在任何 sed 命令
```

**场景 3：确认 SDK 路径使用 git source**
```bash
$ grep -A1 'tool.uv.sources' backend/pyproject.toml
[tool.uv.sources]
log-service-sdk = { git = "https://github.com/xiaolutang/log-service.git", tag = "v0.1.0", subdirectory = "sdks/python" }
```

### first_use 验证

**Dockerfile.backend 构建链路（首次完整构建）**
- Line 14: `COPY backend/pyproject.toml ./` — 直接复制，无预处理
- Line 18: `RUN uv sync --no-dev --no-install-project` — SDK 通过 git source 自动拉取
- 无中间文件、无 sed、无路径替换

**deploy.sh 首次执行**
- 仅 23 行，`source deploy-lib.sh` + `run_deploy "$@"` 一行调用
- 无额外预处理步骤

### systemic_preflight
- reuse_candidates: 无（Dockerfile 多阶段构建是标准模式）
- adjacent_impact: 无其他 Dockerfile 需要类似处理
- pattern_consistency: neutral（标准多阶段构建模式）

## Audit Result (codex_plugin - xlfoundry-audit)
（第一次审计：partial — 缺容器启动验证、缺 simplify 证据、缺 test_coverage 回写）
（修复后待二次审计确认）

### Evidence Sufficiency
- 充分项：构建验证通过、健康检查通过、276 单元测试无回归
- deploy.sh 已重写为共享部署库调用（23 行），职责清晰
- pyproject.toml 使用 `[tool.uv.sources]` git 引用，不再需要本地路径替换
- Dockerfile.backend 直接 COPY pyproject.toml，无任何预处理步骤
