# B01 执行证据

## Task Snapshot

| 字段 | 值 |
|------|-----|
| Task ID | B01 |
| Phase | P1 |
| Module | auth |
| Name | User 模型 + users 表 + 密码哈希 |
| Verification | L1 |
| Started At | 2026-04-13 |

## Architecture Constraints

- 密码使用 bcrypt 哈希，永不存储明文
- JWT secret 必须通过环境变量配置

## Acceptance Criteria

1. users 表创建成功（id, username, email, hashed_password, is_active, created_at, updated_at）
2. UserStorage 类提供 create_user, get_by_username, get_by_email, get_by_id 方法
3. 密码哈希使用 bcrypt，verify_password 和 get_password_hash 函数可用
4. username 和 email 唯一约束生效
5. 单元测试覆盖：创建用户、重复 username 报错、密码验证

## Existing Patterns

- SQLiteStorage：`backend/app/infrastructure/storage/sqlite.py` — SQLite 存储类模式（_init_db, _get_conn, _migrate_schema）
- 模型：`backend/app/models/user.py` — User, UserCreate 模型

## Files to Create/Modify

- `backend/app/infrastructure/storage/user_storage.py`（新建）

## Execution Evidence

### 实现摘要

- 新建 `backend/app/infrastructure/storage/user_storage.py`：UserStorage 类 + get_password_hash/verify_password
- 新建 `backend/tests/unit/test_user_storage.py`：13 个单元测试
- 修改 `backend/pyproject.toml`：添加 bcrypt>=4.0.0 依赖
- 技术决策：直接使用 bcrypt（passlib 1.7.4 与 Python 3.13 不兼容）

### Code Review

- routing: pass（代码风格一致，测试覆盖完整）

### Audit

- status: done, commit_ready: true
- 13/13 单元测试通过

### 验证

```bash
uv run pytest tests/unit/test_user_storage.py -v  # 13 passed
```
