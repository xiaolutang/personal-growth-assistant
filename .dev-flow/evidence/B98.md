# B98: heatmap SQL 聚合升级

## Task Snapshot
- **ID**: B98
- **Module**: review-service-heatmap
- **Status**: completed
- **Fix Level**: L1 (性能优化)

## Execution Evidence

### Changes
1. **修改 `backend/app/services/review_service.py`** — `_get_heatmap_from_sqlite` 从 ~55 行 list_entries 循环改为 ~20 行 SQL 聚合（`get_tag_stats_for_knowledge_map`）
2. **修改 `backend/tests/unit/api/test_review_b53.py`** — 4 个测试从 `list_entries` 断言改为 `get_tag_stats_for_knowledge_map`
3. **新建 `backend/tests/unit/test_heatmap_sql.py`** — 5 个独立 SQL 聚合测试
4. **修改 `backend/tests/unit/api/test_morning_digest_pattern_insights.py`** — 补 `get_tag_stats_in_range` mock
5. **修改 `backend/tests/unit/api/test_morning_digest_personalize.py`** — 补 mock + `test_top_tags_in_stats` 改用 SQL 路径

### Tests Run
```bash
uv run pytest tests/unit/test_heatmap_sql.py -v
# 5 passed
uv run pytest tests/unit/api/test_review_b53.py -v
# 8 passed (4 updated + 4 unchanged)
uv run pytest tests/unit/api/test_morning_digest_pattern_insights.py tests/unit/api/test_morning_digest_personalize.py -v
# 19 passed
uv run pytest tests/ -q
# 1213 passed, 20 skipped
```

### Test Results
- test_heatmap_sql: 5 项 — 有 entries 正确统计、空 entries 空列表、计数一致、掌握度正确、按 mastery 排序
- test_review_b53: 4 项更新 — Neo4j 空/异常/无 client 降级 + SQLite 降级数据格式
- test_morning_digest: 7 项修复 — pattern_insights (6) + personalize (1)

### Acceptance Check
1. [x] `_get_heatmap_from_sqlite` 使用 `get_tag_stats_for_knowledge_map` SQL 聚合
2. [x] 不再调用 `list_entries`（所有调用点验证）
3. [x] 掌握度计算正确（`_calculate_mastery_from_stats` 委托到共享模块）
4. [x] 结果按 mastery 排序（advanced → intermediate → beginner → new）
5. [x] 测试覆盖充分（5 service mock + 8 API 集成 + 4 降级测试）

### Code Review
- 第一轮: fail（排序缺 tie-break + 缺真实 SQLite 测试）
- 修复后第二轮: pass

## Audit Result
- status: partial → 回写完成后待重新审计
