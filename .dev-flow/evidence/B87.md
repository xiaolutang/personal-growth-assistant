# B87 Evidence: 模式洞察 LLM 增强

## Task Snapshot

- **ID**: B87
- **Name**: 模式洞察 LLM 增强
- **Module**: review
- **Verification**: L2
- **Priority**: P1
- **Risk Tags**: [network]

### Acceptance Criteria

1. LLM 可用时生成最多 5 条模式洞察（string[]，每条为中文描述）
2. LLM 不可用时降级到现有 3 条规则引擎洞察
3. LLM 洞察基于近 30 天数据分析（分类分布、完成率、时间模式）
4. 10 秒超时保护不变
5. 空数据（30天无条目）时返回空列表
6. LLM 返回 5xx 或非预期结构时降级到规则引擎

### Test Tasks

1. LLM 正常：返回最多 5 条洞察，格式为 string[]
2. LLM 降级：LLM 不可用时返回规则引擎结果（最多 3 条）
3. LLM 超时：降级到规则引擎
4. 空数据：30 天无条目时返回空列表
5. LLM 5xx：模拟 LLM 返回 500 错误，降级到规则引擎
6. LLM 异常结构：模拟 LLM 返回非列表，降级到规则引擎
7. 回归：现有模式洞察测试通过

---

## Execution Evidence

### Implementation

- **Modified**: `backend/app/services/review_service.py`
  - 新增 `_generate_pattern_insights_llm()` 异步方法
    - 构建 30 天统计数据（分类分布、完成率、高频标签）
    - LLM prompt 要求返回 JSON string 数组
    - 10 秒超时保护
    - 解析失败/超时/异常均返回空列表
  - 修改 `get_morning_digest()` 调用点
    - 先调用 `_generate_pattern_insights_llm()`
    - 返回空时降级到 `_generate_pattern_insights()` 规则引擎

- **New**: `backend/tests/unit/api/test_morning_digest_pattern_insights.py` (9 tests)

### Test Results

```
B87 测试: 9 passed (0.56s)
回归测试: 128 passed (29.54s) — 含全部晨报相关测试
```

### Acceptance Check

1. ✅ LLM 返回最多 5 条洞察 — test_llm_returns_insights + test_llm_returns_more_than_5_truncated
2. ✅ LLM 不可用降级 — test_llm_unavailable_returns_empty + test_digest_llm_insights_fallback_to_rule_engine
3. ✅ 基于近 30 天数据 — stats 包含 category_distribution、task_completion_rate、top_tags
4. ✅ 超时保护 — test_llm_timeout_returns_empty
5. ✅ 空数据返回空 — test_empty_data_returns_empty
6. ✅ LLM 异常降级 — test_llm_5xx_returns_empty + test_llm_non_list_response

### xlfoundry-code-review: pass (local)
