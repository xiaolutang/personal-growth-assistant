# B94: _recommend_from_tags 定向查询优化

## Task Snapshot

- **ID**: B94
- **Module**: knowledge
- **Description**: 将 list_entries(limit=100) + 内存标签匹配改为基于 tag 的定向查询
- **Verification**: L1
- **Files**: knowledge_service.py

## Execution Evidence

- **started_at**: 2026-04-25T10:50:00

### Changes

1. `list_entries(limit=100)` 全量遍历 → `search_tags_by_keyword(keyword=concept, limit=10)` 定向查询
2. 新增空 concept 保护（strip 检查）
3. 新增异常保护（search_tags_by_keyword 失败时优雅降级）
4. 行为等价：大小写不敏感部分匹配、去重、最多 5 条

### Acceptance Check

| # | 验收条件 | 状态 |
|---|---------|------|
| 1 | 改为基于 tag 的定向查询 | ✅ search_tags_by_keyword |
| 2 | 大小写不敏感的部分匹配保持 | ✅ SQLite LIKE %keyword% |
| 3 | 去重逻辑保持 | ✅ seen set |
| 4 | 最多 5 条保持 | ✅ |
| 5 | 空标签优雅处理 | ✅ concept strip 检查 |
| 6 | 异常标签不报错 | ✅ try/except 降级 |
| 7 | 现有测试通过 | ✅ 1176 passed |

### Tests Run

```bash
uv run pytest tests/ -q  → 1176 passed, 20 skipped
```
