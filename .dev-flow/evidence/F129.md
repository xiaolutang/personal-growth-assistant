# Evidence: F129 EntryDetail.tsx 组件拆分

## Task Snapshot

- **ID**: F129
- **Phase**: P4 — 前端组件拆分
- **Module**: entry-detail
- **Verification**: F2
- **Priority**: P1
- **Depends On**: []

### Acceptance Criteria
1. 提取 useEntryData hook（基础数据加载）
2. 提取 useEntryEditing hook（编辑状态和逻辑）
3. 提取 useRelatedEntries hook（关联条目/引用笔记）
4. 提取 useKnowledgeContext hook（知识上下文）
5. 提取 useEntryLinks hook（条目链接管理）
6. 提取 useAiSummary hook（AI 摘要）
7. StructuredContent 拆为独立组件文件
8. MarkdownComponents 拆为独立文件
9. EntryDetail 主文件降至 300 行以内
10. 所有功能不变
11. vitest 通过
12. build 通过

## Execution Evidence

### Implementation

**主文件**: `frontend/src/pages/EntryDetail.tsx` — 1211 行 → 213 行

**新建 hooks (6 个)**:
- `entry-detail/useEntryData.ts` (121 行) — 基础数据加载、503 降级、请求版本保护
- `entry-detail/useEntryEditing.ts` (237 行) — 编辑状态和逻辑（接收 entry + setEntry）
- `entry-detail/useRelatedEntries.ts` (71 行) — 关联条目/引用笔记
- `entry-detail/useKnowledgeContext.ts` (47 行) — 知识上下文
- `entry-detail/useEntryLinks.ts` (59 行) — 链接管理 + 路由切换清理
- `entry-detail/useAiSummary.ts` (83 行) — AI 摘要

**新建组件 (9 个)**:
- `entry-detail/MarkdownComponents.tsx` (18 行)
- `entry-detail/StructuredContent.tsx` (59 行)
- `entry-detail/EntryHeader.tsx` (280 行)
- `entry-detail/CategoryInfoCard.tsx` (57 行)
- `entry-detail/ProjectSection.tsx` (70 行)
- `entry-detail/ContentSection.tsx` (142 行)
- `entry-detail/AiSummaryCard.tsx` (100 行)
- `entry-detail/KnowledgeContextCard.tsx` (62 行)
- `entry-detail/RelatedEntriesCard.tsx` (140 行)

### Tests Run (final)
```bash
uv run pytest tests/ -q                     # 1133 passed
npm run test:run --prefix frontend/         # 406 passed (37 test files)
npm run build --prefix frontend/            # build success, EntryDetail chunk 35.34 kB
```

### Code Review Rounds (6 轮)

| Round | Status | Finding | Fix |
|-------|--------|---------|-----|
| R1 | fail | useEntryEditing 初始化接收 null entry | 重构 hook 契约：useEntryData 无参，useEntryEditing 消费 entry+setEntry |
| R2 | fail | planned_date 清空 + 关联数据残留 | reloadEntry 开始清关联状态，undefined 替代 null |
| R3 | fail | 无请求版本保护 | 添加 loadVersionRef 版本号 |
| R4 | fail | useServiceUnavailable 503 状态不受版本保护 | 503 检测内联到 reloadEntry |
| R5 | conditional_pass | 缺定向测试 | 新增 8 个 useEntryData 测试 |
| R6 | fail | serviceUnavailable 粘滞 + useEntryLinks 路由残留 + 测试不真实 | reloadEntry 开始清 serviceUnavailable + useEntryLinks id 变化清旧数据 + 竞态测试改真实 reject |

### useEntryData 定向测试 (9 个)
1. 初始加载 entry 数据
2. 503 错误设置 serviceUnavailable=true
3. 非 503 错误设置 error 信息
4. 旧请求的 503 不会覆盖新请求的成功结果（真实 reject 503）
5. retryService 清除 503 状态并重新加载
6. retryService 后再次 503 重新进入降级态
7. project 类型条目加载 childTasks 和 projectProgress
8. 过期请求不写入 isLoading/error/serviceUnavailable（真实 reject 500）
9. 503 后路由切换到 404 不再显示 serviceUnavailable

### Acceptance Check
1. ✅ useEntryData: 数据加载 + reloadEntry + 503 降级 + 版本保护 + 路由切换清理
2. ✅ useEntryEditing: 编辑模式 + 字段修改 + 保存/取消 + autoSave + lastSavedContent 同步
3. ✅ useRelatedEntries: 关联条目 + 引用笔记
4. ✅ useKnowledgeContext: 知识图谱上下文
5. ✅ useEntryLinks: 链接增删 + id 变化时清空旧数据
6. ✅ useAiSummary: AI 摘要生成
7. ✅ StructuredContent.tsx: 独立文件
8. ✅ MarkdownComponents.tsx: 独立文件
9. ✅ 主文件 213 行 (≤ 300)
10. ✅ 功能不变（纯重构）
11. ✅ 406 vitest passed + 1133 pytest passed
12. ✅ build 成功

### Systemic Preflight
- reuse_candidates: useServiceUnavailable hook 仍用于其他页面；EntryDetail 因版本保护需求已内联 503 检测，不复用该 hook，理由充分
- adjacent_impact: 仅影响 EntryDetail 页面，路由 import 路径不变
- pattern_consistency: converging（与 GraphPage 拆分模式一致）

### Runtime Verification
- required: true (F2)
- 页面可打开: build 成功生成 EntryDetail chunk 35.34 kB，TypeScript 类型检查通过
- 交互可触发: 406 vitest passed（含 9 个 useEntryData 定向测试覆盖 503/竞态/retry/路由切换）
- build 通过确认所有 import 路径、组件 props 类型、hook 契约无错误
- 纯重构任务：所有交互逻辑从原文件搬迁到子模块，无新增用户可见行为
- 既有的 EntryDetail 页面级测试在拆分后仍通过（证明功能不变）

## Audit Result
- status: done
- commit_ready: true
- reviewed_at: 2026-04-26T00:50:00+0800
