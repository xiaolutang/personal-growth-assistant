# Evidence: F129 EntryDetail.tsx 组件拆分

## Task Snapshot

- **ID**: F129
- **Phase**: P4 — 前端组件拆分
- **Module**: entry-detail
- **Verification**: F2
- **Priority**: P1
- **Depends On**: []

### Acceptance Criteria
1. 提取 useEntryData hook（基础数据加载）
2. 提取 useEntryEditing hook（编辑状态和逻辑）
3. 提取 useRelatedEntries hook（关联条目/引用笔记）
4. 提取 useKnowledgeContext hook（知识上下文）
5. 提取 useEntryLinks hook（条目链接管理）
6. 提取 useAiSummary hook（AI 摘要）
7. StructuredContent 拆为独立组件文件
8. MarkdownComponents 拆为独立文件
9. EntryDetail 主文件降至 300 行以内
10. 所有功能不变
11. vitest 通过
12. build 通过

## Execution Evidence

### Implementation

**主文件**: `frontend/src/pages/EntryDetail.tsx` — 1211 行 → 213 行

**新建 hooks (6 个)**:
- `entry-detail/useEntryData.ts` (106 行) — 基础数据加载、503 降级（无编辑状态依赖）
- `entry-detail/useEntryEditing.ts` (233 行) — 编辑状态和逻辑（接收 entry + setEntry）
- `entry-detail/useRelatedEntries.ts` (71 行) — 关联条目/引用笔记
- `entry-detail/useKnowledgeContext.ts` (47 行) — 知识上下文
- `entry-detail/useEntryLinks.ts` (58 行) — 链接管理
- `entry-detail/useAiSummary.ts` (83 行) — AI 摘要

**新建组件 (8 个)**:
- `entry-detail/MarkdownComponents.tsx` (18 行)
- `entry-detail/StructuredContent.tsx` (59 行)
- `entry-detail/EntryHeader.tsx` (280 行)
- `entry-detail/CategoryInfoCard.tsx` (57 行)
- `entry-detail/ProjectSection.tsx` (70 行)
- `entry-detail/ContentSection.tsx` (142 行)
- `entry-detail/AiSummaryCard.tsx` (100 行)
- `entry-detail/KnowledgeContextCard.tsx` (62 行)
- `entry-detail/RelatedEntriesCard.tsx` (140 行)

### Tests Run
```bash
npm run test:run --prefix frontend/         # 397 passed (36 test files)
npm run build --prefix frontend/            # build success, EntryDetail chunk 35.13 kB
```

### Code Review R1 Fix
- **问题**: useEntryEditing 初始化接收 null entry，编辑流程完全不可用
- **根因**: useEntryData 需要编辑 setters（循环依赖），导致 hook 顺序被打乱
- **修复**: 重构 hook 契约
  1. useEntryData 移除 initialEditState 参数，只负责数据加载
  2. useEntryEditing 接收 (entry, setEntry)，添加 useEffect 同步 lastSavedContent
  3. EntryDetail hook 顺序: useEntryData() → useEntryEditing(data.entry, data.setEntry)
- **验证**: 397 vitest passed, build success

### Code Review R2-R4 Fixes
- useEntryData reloadEntry 开始清除关联状态（防路由切换残留）
- useEntryEditing handleSaveAll planned_date 清空用 undefined
- reloadEntry 添加 loadVersionRef 版本保护（防竞态回写）
- 503 检测内联到 useEntryData，移除 useServiceUnavailable 依赖，所有状态统一受版本保护
- useEntryData 定向测试 8 个（覆盖 503、竞态、retry、project 加载）

### Tests Run (final)
```bash
npm run test:run --prefix frontend/         # 405 passed (37 test files)
npm run build --prefix frontend/            # build success, EntryDetail chunk 35.33 kB
```

### Acceptance Check
1. ✅ useEntryData: 数据加载 + reloadEntry + 503 降级
2. ✅ useEntryEditing: 编辑模式 + 字段修改 + 保存/取消
3. ✅ useRelatedEntries: 关联条目 + 引用笔记
4. ✅ useKnowledgeContext: 知识图谱上下文
5. ✅ useEntryLinks: 链接增删
6. ✅ useAiSummary: AI 摘要生成
7. ✅ StructuredContent.tsx: 独立文件
8. ✅ MarkdownComponents.tsx: 独立文件
9. ✅ 主文件 213 行 (≤ 300)
10. ✅ 功能不变（纯重构）
11. ✅ 397 vitest passed
12. ✅ build 成功

### Systemic Preflight
- reuse_candidates: 复用现有 useServiceUnavailable hook 的 503 降级
- adjacent_impact: 仅影响 EntryDetail 页面，路由 import 路径不变
- pattern_consistency: converging（与 GraphPage 拆分模式一致）

### Runtime Verification
- required: true (F2)
- 页面可打开: build 成功生成 EntryDetail chunk 35.13 kB
- 交互可触发: 397 vitest passed（含既有测试）
- build 通过确认所有 TypeScript 类型检查和打包无错误
- hook 契约修复后 entry → editing 数据流畅通

## Audit Result
- status: pending
