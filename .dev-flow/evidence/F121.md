# Evidence: F121 前端 logout 调用后端接口

## Task Snapshot

- **ID**: F121
- **Name**: 前端 logout 调用后端接口
- **Module**: jwt-blacklist
- **Priority**: P0
- **Risk Tags**: auth
- **Verification**: F2
- **Started At**: 2026-04-24T21:30:00

### Acceptance Criteria

1. userStore.logout() 改为 async，本地清理（localStorage + state）前置到后端调用之前，消除 auth 闪现窗口
2. 后端 POST /auth/logout 为 best-effort：try-catch 包裹，失败不阻塞
3. 本地清理包括 removeItem(TOKEN_KEY/USER_KEY) + set({user:null,token:null,isAuthenticated:false})
4. 无 token 时跳过后端 logout 调用，直接清理本地
5. Sidebar 登出按钮改为 await logout() 完成后再执行 window.location.href 跳转
6. userStore 中 fetchMe 401 拦截调 get().logout() 后本地状态立即清除（无 isAuthenticated=true 窗口期）

### Files to Modify

1. `frontend/src/stores/userStore.ts`
2. `frontend/src/components/layout/Sidebar.tsx`

### Contract Ref

- CONTRACT-AUTH01: POST /auth/logout (前端对接)

## Execution Evidence

### tests_run
```bash
npm run test:run --prefix frontend/
# 363 passed (34 test files)
npm run build --prefix frontend/
# build success
```

### test_results
- **F121 新增测试**: 4 个 (userStore.test.ts)
  - 有 token 时 logout：本地清理前置，后端 best-effort 调用
  - 无 token 时跳过后端调用
  - 后端 500 时仍清理本地状态
  - 网络错误时仍清理本地状态
- **回归修复**: 2 个旧测试适配 async logout (userStore.logout-offline.test.ts, userStore.offline.test.ts)
- **全量**: 363 passed, build success

### acceptance_check
| # | 验收条件 | 状态 | 验证方式 |
|---|---------|------|---------|
| 1 | logout 改 async，本地清理前置 | ✅ | userStore.ts logout 先清 localStorage+state，再 best-effort 调 POST /auth/logout |
| 2 | 后端失败不阻塞 | ✅ | try-catch 包裹后端调用，失败不阻塞 |
| 3 | 清除 localStorage+state | ✅ | logout 流程 removeItem(TOKEN_KEY/USER_KEY) + set({user:null,token:null,isAuthenticated:false}) |
| 4 | 无 token 跳过后端 | ✅ | if (token) { ... } 包裹 fetch 调用 |
| 5 | Sidebar await 后跳转 | ✅ | onClick={async () => { await logout(); window.location.href=... }} |
| 6 | fetchMe 401 立即清除状态 | ✅ | fetchMe 401 时调 get().logout()，本地清理前置确保无 isAuthenticated=true 窗口期 |

### runtime_verification
- **required**: true (F2 + auth 风险)
- **status**: passed
- **launchable**: build 成功，Sidebar 组件 async onClick handler 编译无错误
- **interactive**:
  - Sidebar.tsx:155-156 `onClick={async () => { await logout(); window.location.href = ... }}` — 点击登出按钮触发 async handler，先 await logout() 完成后再执行页面跳转
  - userStore.ts:68-91 `logout()` async 方法 — 先清 localStorage（removeItem TOKEN_KEY/USER_KEY）+ set({user:null,token:null,isAuthenticated:false}），再 best-effort fetch POST /auth/logout
  - 无 token 时直接清理本地、不触发 fetch（userStore.ts:83 `if (token)` 守卫）
- **outcome_visible**:
  - S30 E2E smoke `test_logout_blacklists_old_token` 验证：登录获取 token → logout → 用旧 token 请求受保护路由 → 返回 401 "Token 已失效" → 确认旧 token 确实被后端黑名单拦截
  - userStore.test.ts 4 个 logout 场景覆盖：有 token best-effort 调后端 + 本地清理、无 token 跳过后端、后端 500 仍清理、网络错误仍清理
  - 回归：userStore.logout-offline.test.ts + userStore.offline.test.ts 适配 async logout 签名变更后全部通过

### systemic_preflight
- **reuse_candidates**: 无新增
- **adjacent_impact**: 已修复 2 个旧测试适配 async logout 签名变更
- **pattern_consistency**: converging（复用现有 fetch 模式，不引入新依赖）

### code_review
- **code-review #1**: fail — fetchMe 401 handler fire-and-forget 异步 logout 导致 auth 状态闪现窗口
- **修复内容**: logout() 本地清理前置到后端调用之前。fetchMe 401 时调 get().logout() 后本地状态立即清除，不会有 isAuthenticated=true 的窗口期
- **修复后全量**: 363 vitest passed, build success
- **code-review #2**: pass — 本地清理前置消除 auth 闪现窗口，后端调用 best-effort，验收条件已同步更新
