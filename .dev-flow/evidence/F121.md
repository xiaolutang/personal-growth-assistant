# Evidence: F121 前端 logout 调用后端接口

## Task Snapshot

- **ID**: F121
- **Name**: 前端 logout 调用后端接口
- **Module**: jwt-blacklist
- **Priority**: P0
- **Risk Tags**: auth
- **Verification**: F2
- **Started At**: 2026-04-24T21:30:00

### Acceptance Criteria

1. userStore.logout() 改为 async，先 await POST /auth/logout（带 Authorization header）
2. 后端 logout 调用失败不阻塞前端清理流程（catch 后继续）
3. logout API 调用成功后清除 localStorage token
4. 无 token 时跳过后端 logout 调用，直接清理本地
5. Sidebar 登出按钮改为 await logout() 完成后再执行 window.location.href 跳转
6. userStore 中 401 拦截的自动 logout 调用兼容 async

### Files to Modify

1. `frontend/src/stores/userStore.ts`
2. `frontend/src/components/layout/Sidebar.tsx`

### Contract Ref

- CONTRACT-AUTH01: POST /auth/logout (前端对接)

## Execution Evidence

### tests_run
```bash
npm run test:run --prefix frontend/
# 363 passed (34 test files)
npm run build --prefix frontend/
# build success
```

### test_results
- **F121 新增测试**: 4 个 (userStore.test.ts)
  - 有 token 时先调后端再清除
  - 无 token 时跳过后端调用
  - 后端 500 时仍清理本地状态
  - 网络错误时仍清理本地状态
- **回归修复**: 2 个旧测试适配 async logout (userStore.logout-offline.test.ts, userStore.offline.test.ts)
- **全量**: 363 passed, build success

### acceptance_check
| # | 验收条件 | 状态 | 验证方式 |
|---|---------|------|---------|
| 1 | logout 改 async，先调后端 | ✅ | userStore.ts logout 改 async，先 fetch POST /auth/logout |
| 2 | 后端失败不阻塞 | ✅ | try-catch 包裹，catch 后继续清理 |
| 3 | 成功后清除 localStorage | ✅ | logout 流程 removeItem(TOKEN_KEY/USER_KEY) |
| 4 | 无 token 跳过后端 | ✅ | if (token) { ... } 包裹 fetch 调用 |
| 5 | Sidebar await 后跳转 | ✅ | onClick={async () => { await logout(); window.location.href=... }} |
| 6 | fetchMe 401 兼容 async | ✅ | fetchMe 中 get().logout() 不 await（401 时后端已判无效） |

### runtime_verification
- **required**: true (auth 风险)
- **status**: passed
- **evidence**: vitest 363 passed, build success, logout 测试覆盖有/无 token、500、网络错误场景

### systemic_preflight
- **reuse_candidates**: 无新增
- **adjacent_impact**: 已修复 2 个旧测试适配 async logout 签名变更
- **pattern_consistency**: converging（复用现有 fetch 模式，不引入新依赖）

### code_review
- status: pending (将由 xlfoundry-code-review 执行)
