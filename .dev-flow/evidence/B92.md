# Evidence: B92 Neo4j 客户端降级 + 知识图谱路由层完善

## Task Snapshot

- **ID**: B92
- **Name**: Neo4j 客户端降级 + 知识图谱路由层完善
- **Module**: degradation
- **Priority**: P1
- **Verification**: L2
- **Started At**: 2026-04-24T22:20:00

### Acceptance Criteria

1. _get_session() 在 _driver=None 时抛 ConnectionError
2. _with_neo4j_fallback Neo4j 不可用时返回空数据结构
3. GET /knowledge-map Neo4j 不可用时返回 200 + 空 KnowledgeMapResponse
4. GET /knowledge/stats Neo4j 不可用时返回 200 + 空 ConceptStatsResponse
5. GET /knowledge-graph/{concept} 保留 503
6. 正常路径不受影响

## Execution Evidence

### tests_run
```bash
uv run pytest tests/unit/services/test_knowledge_degradation.py -v
# 21 passed (19 original + 2 ConnectionError 服务层传播)
uv run pytest tests/unit/api/test_knowledge_api.py::TestConnectionError503Routing -v
# 2 passed (API 层 ConnectionError → HTTP 503)
uv run pytest tests/ -q
# 1173 passed, 20 skipped
```

### test_results
- **B92 新增测试**: 19 个原始 + 4 个补测 (2 service层 + 2 API层)
- **全量回归**: 1173 passed, 20 skipped (after code-review fix + API层补测)

### code_review_fix
- **code-review #1**: fail — 路由层 except Exception 太宽 + /knowledge-graph 运行时断连不返回 503
- **修复内容**: 1) /knowledge-map 和 /knowledge/stats catch 收窄为 `(ConnectionError, ValueError)` 2) /knowledge-graph/{concept} 和 /related-concepts/{concept} 新增 `ConnectionError → 503` 映射
- **补测**: 2 个 ConnectionError 服务层传播测试（验证 Neo4j 运行时断连时 ConnectionError 从 service 层正确传播）+ 2 个 API 层路由测试（验证 ConnectionError 映射为 HTTP 503）
- **修复后全量**: 1173 pytest passed, 20 skipped
- **code-review #2**: pass — catch 收窄为 (ConnectionError, ValueError)，/knowledge-graph 和 /related-concepts 新增 ConnectionError→503，测试同步更新 |

### acceptance_check
| # | 验收条件 | 状态 | 验证方式 |
|---|---------|------|---------|
| 1 | _get_session ConnectionError | ✅ | test_get_session_raises_connection_error |
| 2 | _with_neo4j_fallback 空结构 | ✅ | 5 个降级测试 |
| 3 | /knowledge-map 200+空 | ✅ | test_knowledge_map_degradation |
| 4 | /knowledge/stats 200+空 | ✅ | test_knowledge_stats_degradation |
| 5 | /knowledge-graph 503 保留 | ✅ | test_knowledge_graph_keeps_503 |
| 6 | 正常路径回归 | ✅ | 3 个回归测试 |

### systemic_preflight
- **reuse_candidates**: 与 Qdrant 降级模式一致
- **adjacent_impact**: 路由层已有 try-except，降级不影响其他端点
- **pattern_consistency**: converging（对齐 Qdrant 降级模式）
