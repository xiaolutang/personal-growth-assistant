# B61: review_service 异步重构 + datetime 统一 + goal_service 修复

## Execution Evidence

### 改动
1. `backend/app/services/review_service.py` — 删除 `_run_async` + `concurrent.futures`，6 个方法改为原生 async
2. `backend/app/services/goal_service.py` — `recalculate_tag_auto_goals` 修复 linked_entries_count 计算 + `datetime.utcnow()` 统一
3. `backend/app/infrastructure/storage/sqlite.py` — 新增 `get_active_dates()` + `get_daily_activity_counts()` 公共聚合方法，消除 `_get_conn()` 绕过
4. `backend/app/routers/review.py` — 添加 `await` 调用
5. `backend/app/mcp/handlers.py` — 添加 `await` 调用
6. `backend/tests/unit/test_mcp_auth.py` — Mock → AsyncMock
7. `backend/tests/unit/api/test_review_b53.py` — Mock → AsyncMock

### 测试结果
- 855 passed, 20 skipped

## Acceptance Check

| # | 验收条件 | 状态 |
|---|----------|------|
| 1 | `_run_async` + ThreadPoolExecutor 删除 | ✅ |
| 2 | review_service 方法改为原生 async | ✅ 6 个方法 |
| 3 | `datetime.utcnow()` → `datetime.now(timezone.utc)` | ✅ |
| 4 | sqlite.py 公共聚合方法替代 `_get_conn()` 绕过 | ✅ 2 个新方法 |
| 5 | goal_service tag_auto 修复 | ✅ |
| 6 | 测试全部通过 | ✅ 855 passed |
