# Evidence: B102 review_service 进一步拆分

## Task Snapshot

- **ID**: B102
- **Phase**: P2 — review 子模块拆分
- **Module**: review
- **Verification**: L1
- **Priority**: P2
- **Depends On**: B101

### Acceptance Criteria
1. 识别可独立的方法组并拆分到 services/review/ 子模块
2. review_service.py 降至 1200 行以内
3. 所有 import 正确更新（路由层 + 测试）
4. 现有测试通过

### Architecture Constraints
- 不改变任何 API 行为
- 路由层 import 路径同步更新
- 保持 backward compat（re-export）

### Split Plan
- **morning_digest.py** (~470 lines): MorningDigestMixin — get_morning_digest, _generate_pattern_insights_llm, _generate_pattern_insights, _parse_entry_date, _parse_llm_json, _generate_daily_focus, _generate_morning_suggestion, _compute_30d_tag_stats, _calculate_learning_streak, 缓存变量
- **insights.py** (~330 lines): InsightsMixin — get_insights, _generate_deep_insights, _analyze_category_distribution, _generate_rule_based_insights

ReviewService 继承 MorningDigestMixin + InsightsMixin，所有外部 import 路径不变。

## Execution Evidence

### Tests Run
```bash
uv run pytest tests/ -q
```

### Test Results
- 1229 passed, 0 failed, 20 skipped

### Fix: Mock path update
- test_review_service.py::test_january_monthly_prev_december mock path: `app.services.review_service.date` → `app.services.review.insights.date`

### Acceptance Check
1. ✅ 拆分 MorningDigestMixin + InsightsMixin 到 services/review/ 子模块
2. ✅ review_service.py 从 1832 行降至 885 行（目标 ≤1200）
3. ✅ 路由层 import 无需修改（ReviewService import 路径不变），测试 mock path 已更新
4. ✅ 1229 pytest passed

### New Files
- backend/app/services/review/__init__.py
- backend/app/services/review/morning_digest.py
- backend/app/services/review/insights.py

### Modified Files
- backend/app/services/review_service.py (1832→885 lines)
- backend/tests/unit/services/test_review_service.py (1 mock path fix)

## Fix: Period 参数显式校验 (bb91c44)

### Code-Review Finding (codex_plugin 2026-04-25T19:14)
- `get_insights()` 不拒绝非法 period 值，非 weekly 静默走入 monthly 分支
- 导致 Pydantic ValidationError 而非预期的 ValueError

### Fix Applied
- `insights.py` L35: 新增 `if period not in ("weekly", "monthly"): raise ValueError(...)`
- 校验在 `_sqlite` 检查之前执行

### Tests Run
```bash
uv run pytest tests/ -q          # 1130 passed, 0 failed, 1 warning
npm run test:run --prefix frontend/  # 386 passed
npm run build --prefix frontend/     # build success
```

### Test Results
- 1130 pytest passed, 386 vitest passed, build 成功

### Code-Review Result
- status: pass (codex_plugin, 2026-04-25T19:16:04+0800)
- findings: []
- required_changes: []
- routing: pass

### Systemic Preflight
- reuse_candidates: 无新增可复用点（Mixin 模式与 B95 拆分一致）
- adjacent_impact: 无相邻入口遗漏（生产代码仅 review.py 路由调用 get_insights）
- pattern_consistency: converging（service 层 period 校验与 review_service.get_trend_data 模式一致）

## Audit Result

- status: done (codex_plugin, 2026-04-25)
- commit_ready: true
- evidence_sufficiency: sufficient
- spot_check: passed (review_service.py, insights.py, test_review_service.py, import scan)
- systemic_check: reuse=sufficient, adjacent_impact=covered, pattern_consistency=converging
- missing: none

### Acceptance Check (Final)
1. ✅ 拆分 MorningDigestMixin + InsightsMixin 到 services/review/ 子模块
2. ✅ review_service.py 从 1832 行降至 885 行（目标 ≤1200）
3. ✅ 路由层 import 无需修改，测试 mock path 已更新
4. ✅ 1229 pytest passed (拆分) + 1130 pytest passed (修复)
