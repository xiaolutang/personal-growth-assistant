# F112: 消除回顾页重复 API 请求

## Task Snapshot
- id: F112
- module: frontend
- verification: F2

## Execution Evidence

### Implementation
- File: `frontend/src/pages/Review.tsx` lines 97-113
  - Added unified `insightsData` + `insightsLoading` state
  - Added useEffect with cancel protection (`let cancelled = false`)
  - Only fetches when reportType is "weekly" or "monthly"
  - Period: "monthly" for monthly, "weekly" for weekly
- File: `frontend/src/components/review/InsightCard.tsx`
  - Removed: internal `getInsights` call, `useState`, `useEffect`
  - Changed to accept `insightsData` and `insightsLoading` props
- File: `frontend/src/components/review/AiSummaryCard.tsx`
  - Removed: internal `getInsights` call, insights useState/useEffect
  - Changed to accept `insightsData` and `insightsLoading` props
  - Kept `aiSummaryExpanded` state (local UI state)

### tests_run
- Frontend: 347 passed (vitest)
- Frontend build: success

### test_results
- All existing Review component tests pass
- All existing InsightCard tests pass
- All existing AiSummaryCard tests pass
- Build produces correct output

### runtime_verification
- Docker E2E: GET /growth/api/review/insights?period=weekly → returns JSON with insights data
- Docker E2E: GET /growth/api/review/daily → returns daily report
- Docker E2E: GET /growth/api/review/weekly → returns weekly report

### acceptance_check
- [x] Review.tsx has unified insightsData + insightsLoading state + useEffect
- [x] InsightCard accepts insightsData/insightsLoading props (no internal fetch)
- [x] AiSummaryCard accepts insightsData/insightsLoading props (no internal fetch)
- [x] Only one getInsights request per weekly/monthly view
- [x] Cancel protection migrated from children to parent
- [x] Frontend tests pass, build succeeds
