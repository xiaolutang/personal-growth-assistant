# Evidence: TD06 - deps.py 服务获取统一化

## Task Snapshot

- **ID**: TD06
- **Phase**: P7 (重构与结构优化)
- **Priority**: medium
- **Verification**: L1
- **Risk Tags**: startup
- **Acceptance Criteria**:
  1. getter 函数无每次调用的副作用
  2. 服务在初始化时获得完整的存储引用
  3. 后端单元测试通过（272+ 个）

## Execution Evidence

### 实施操作
1. 修改 `get_review_service()`：构造函数注入 `sqlite_storage=storage.sqlite`，删除每次调用的 `set_sqlite_storage()`
2. 修改 `get_knowledge_service()`：构造函数注入 `neo4j_client` 和 `sqlite_storage`，删除每次调用的 `set_neo4j_client()` 和 `set_sqlite_storage()`
3. 新增 `backend/tests/unit/test_deps.py`（9 个测试用例）

### tests_run
```bash
cd backend && uv run pytest tests/unit/test_deps.py -v
# 9 passed in 0.79s
```

### test_results
```bash
cd backend && uv run pytest tests/unit/ -q
# 284 passed in 1.97s（含 9 个新增 deps 测试，无回归）
```

### 验收条件检查
| 条件 | 结果 |
|------|------|
| getter 无每次调用副作用 | ✓ pass |
| 初始化时注入完整存储引用 | ✓ pass |
| 后端单元测试通过 | ✓ pass (284) |

### Test Dimensions
- happy_path: covered（多次调用返回同一实例 + 存储引用正确）
- failure_branch: covered（storage=None 时抛 503）
- empty_data: n/a
- boundary: covered（reset 后重建实例引用正确）

### systemic_preflight
- reuse_candidates: 构造函数注入模式与 EntryService 一致
- adjacent_impact: get_entry_service 和 get_intent_service 已是初始化时注入模式，无需改动
- pattern_consistency: converging（所有 getter 统一到构造函数注入模式）

### Runtime Verification
- 后端服务 `uvicorn app.main:app` 可正常启动
- deps.py 中的 getter 函数被 FastAPI 路由在请求时调用，无需独立运行态验证
- 284 个单元测试通过（含 9 个 deps 专项测试），覆盖所有路径
- status: passed (L1 验证级别，单元测试充分覆盖)

## Quality Gates

### xlfoundry-code-review (2026-04-12)
- status: pass
- 审查范围：backend/app/routers/deps.py, backend/tests/unit/test_deps.py
- 维度评估：
  - 架构与系统性：pass — 所有 getter 统一到构造函数注入模式，消除 setter 调用的隐式副作用
  - 复用与抽象：pass — 注入模式与 EntryService/IntentService 一致，模式统一
  - 代码质量：pass — 删除冗余守卫条件，9 个 deps 专项测试覆盖 happy path / failure / reset 场景
  - 效率与可测试性：pass — 284 单元测试通过，getter 可安全重复调用无副作用
- routing: pass

### xlfoundry-simplify
- 已执行 4 个并行审查 agent
- 修复：删除 `get_knowledge_service` 中冗余的 `if storage else None` 守卫，改为 `raise HTTPException(503)` 与其他 getter 一致
- 跳过：get_intent_service setter 统一（超出 TD06 范围，LLM caller 异步初始化需要 setter）
- 跳过：删除 setter 死代码（超出 TD06 范围，需全仓库排查）
- 全部测试通过：284 passed

### xlfoundry-code-review 复测 (2026-04-12)
- status: pass (simplify 后复测)
- simplify 产生了代码修改：`get_knowledge_service` 中 `if storage else None` 守卫替换为 `raise HTTPException(503)`，与 `get_review_service` 模式一致
- 原 code-review 结论仍然有效

## Runtime Validation (2026-04-12)

### deps.py 导入与功能验证
```bash
cd backend && uv run python -c "
from app.routers.deps import get_storage, get_entry_service, reset_all_services
print('deps import OK')
reset_all_services()
print('reset_all_services OK')
try:
    s = get_storage()
    print('get_storage returned:', type(s).__name__)
except Exception as e:
    print('get_storage raised:', type(e).__name__, str(e))
"
```

**输出：**
```
deps import OK
reset_all_services OK
get_storage raised: HTTPException 503: 存储服务未初始化
```

**结论：** deps 模块导入正常，`reset_all_services()` 清空缓存成功，未初始化时正确抛出 503。

### 冷启动覆盖确认
- `reset_all_services` (deps.py:79) 清空所有服务缓存
- test_deps.py 覆盖：TestGetReviewService (3 tests) / TestGetKnowledgeService (3 tests) / TestStorageNotInitialized (3 tests)
- 共 9 个测试用例，覆盖同实例、构造注入、reset 重建、503 降级场景
- 冷启动：app lifespan 中 init_storage() 调用 `reset_all_services()`，test_deps.py `reset_then_recreate` 已覆盖
