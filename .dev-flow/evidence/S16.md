# Evidence: S16 回顾增强后端测试收口

## Task Snapshot

- **ID**: S16
- **Phase**: 7
- **Module**: review
- **Name**: 回顾增强后端测试收口
- **Verification**: L1
- **Depends On**: S15

## Execution Evidence

### Implementation

**Files Created:**
- `backend/tests/unit/services/test_review_service.py` — 新增 34 个服务级测试

**Test Classes:**
- `TestGetInsights` (7 tests): period 校验、SQLite 初始化、weekly/monthly 双周期查询验证、1 月跨年边界、空数据降级、user_id 传递
- `TestGenerateDeepInsights` (8 tests): LLM 成功+payload 校验、超时降级、非法 JSON 降级、代码块包裹 JSON、无 caller 降级、数组截断、无效 schema 字段降级、统计 payload 验证
- `TestGenerateRuleBasedInsights` (15 tests): 空数据、分类分布触发/不触发、总数阈值、monthly 标签、完成率趋势触发/不触发、灵感建议触发/不触发、低完成率建议触发/不触发、笔记建议触发/不触发、能力变化触发/不触发、上限 3 项
- `TestAnalyzeCategoryDistribution` (3 tests): 混合条目、空列表、单一类型

### Tests Run

- 34 service-level tests passed
- 909 total unit tests passed (no regression)

### Acceptance Check

- [x] 新增测试覆盖 /review/insights 端点（通过 get_insights 方法间接覆盖）
- [x] 新增测试覆盖 _generate_deep_insights 方法（8 个测试：成功/超时/JSON 失败/schema 失败/payload 校验/数组截断/代码块/无 caller）
- [x] 所有测试通过
- [x] 测试覆盖正常/边界/降级 3 类场景（阈值边界测试：60% 分布、15% 完成率差、3 个灵感、50% 完成率、3 个任务、标签不变）

### Code Review

- Round 1 (2026-04-22): fail — 3 findings (前周期查询断言缺失、阈值边界测试不足、LLM mock payload 未校验)
- Fixed: 补充前周期查询断言、1 月跨年边界测试、7 个阈值边界反面测试、LLM payload 结构验证、schema 验证降级测试
