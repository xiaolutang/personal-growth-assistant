# F64: 探索页新类型 E2E（R020）

## Task Snapshot

- **ID**: F64
- **Name**: 探索页新类型 E2E
- **Module**: e2e-new-types
- **Phase**: 3 (R020)
- **Status**: completed
- **Started**: 2026-04-20
- **Completed**: 2026-04-20
- **Commit**: b94ee06

## Acceptance Criteria

1. 探索页显示 7 个 Tab（全部/灵感/笔记/项目/决策/复盘/疑问）
2. 点击「决策」Tab → 只显示 decision 类型条目
3. 点击「复盘」Tab → 只显示 reflection 类型条目
4. 点击「疑问」Tab → 只显示 question 类型条目
5. 搜索关键词 + Tab 过滤 → 结果为交集
6. URL ?type=decision → 自动切换到决策 Tab
7. 空数据 Tab → 显示空状态引导
8. 点击「全部」Tab → 显示所有非 task 类型

## Execution Evidence

### Changes

1. **e2e/tests/new-types-ui.spec.ts**（新增）— 8 个 E2E 测试：

   **Tab 切换（3 个）**：决策 Tab / 复盘 Tab / 疑问 Tab
   **搜索 + 筛选（1 个）**：搜索关键词 + Tab 过滤交集
   **URL 参数同步（1 个）**：URL ?type=decision 自动切换
   **空状态（1 个）**：空数据 Tab 显示引导
   **全部 Tab（1 个）**：显示所有非 task 类型
   **Tab 完整性（1 个）**：验证 7 个 Tab 全部渲染

2. **frontend/src/pages/Explore.tsx**（Bug 修复）— getEntries limit 200→100

### Key Technical Decisions

1. **limit 修复**: Explore.tsx 中 getEntries 的 limit 参数从 200 改为 100，与后端默认分页一致，避免请求超量数据
2. **Tab 完整性验证**: 额外增加测试验证 7 个 Tab 全部正确渲染

### tests_run

| Command | Result |
|---------|--------|
| `cd e2e && npx playwright test tests/new-types-ui.spec.ts` | 8 passed |

### Acceptance Check

| # | Criteria | Status |
|---|----------|--------|
| 1 | 7 个 Tab 显示 | ✅ |
| 2 | 决策 Tab 过滤 | ✅ |
| 3 | 复盘 Tab 过滤 | ✅ |
| 4 | 疑问 Tab 过滤 | ✅ |
| 5 | 搜索 + Tab 交集 | ✅ |
| 6 | URL 参数同步 | ✅ |
| 7 | 空状态引导 | ✅ |
| 8 | 全部 Tab | ✅ |

## Runtime Verification

- 类型: UI 级 E2E（Playwright Browser + APIRequestContext）
- 依赖: 后端 uvicorn :18929 + 前端 vite :5173
- 结果: 8 passed
