# TD09 E2E 测试加强

## Task Snapshot

- **ID**: TD09
- **Phase**: P8
- **Priority**: medium

### Acceptance Criteria

1. 页面加载和导航用例使用硬断言（expect） ✓
2. 容错 skip 仅用于非关键路径 ✓
3. 新增数据创建+验证闭环用例（创建任务 → 搜索 → 验证结果） ✓
4. E2E 测试在开发环境可运行 ✓

### Changes

- 重写 e2e/tests/user_flows.spec.ts
- 3 个 describe 块：硬断言(5)、数据闭环(1)、容错(2)
- 硬断言：首页加载、导航栏、任务页导航、响应式
- 数据闭环：API 创建任务 → 页面验证可见 → API 删除清理
- 容错 skip：搜索、状态切换

### Verification

- TypeScript 语法检查通过
- E2E 依赖未安装（需 `npm install` + `npx playwright install`）
- 运行态验证需要前后端服务启动

## Quality Gates

### xlfoundry-code-review (2026-04-12)
- status: pass
- 审查范围：e2e/tests/user_flows.spec.ts
- 维度评估：
  - 架构与系统性：pass — 三层结构（硬断言/数据闭环/容错）覆盖不同测试优先级，职责清晰
  - 复用与抽象：pass — try/finally 保证清理，消除 waitForTimeout 冗余等待
  - 代码质量：pass — 硬断言替代软断言，容错用例添加有效断言（toBeVisible），数据闭环有创建+验证+清理
  - 效率与可测试性：pass — E2E 测试结构可维护，skip 策略用于非关键路径合理
- routing: pass

## Simplify（补执行）

4 个并行审查 agent 发现 3 个问题并已修复：

1. **数据闭环用例缺少 try/finally**（HIGH）→ 用 try/finally 包裹，finally 中执行 DELETE 清理
2. **冗余 waitForTimeout(2000)**（MEDIUM）→ 移除，toBeVisible 已有 timeout 等待
3. **容错用例无有效断言**（MEDIUM）→ 搜索用例改为 expect(resultArea).toBeVisible()，状态切换改为 expect(taskItem).toBeVisible()

## tests_run

- commit: db6c50a
- 8 E2E tests in e2e/tests/user_flows.spec.ts

## acceptance_check

| # | Criteria | Status |
|---|----------|--------|
| 1 | 硬断言 → all tests use strict assertions (toBe, toContain) | PASS |
| 2 | 导航测试 → covered | PASS |
| 3 | 数据闭环 → create/verify/cleanup pattern | PASS |
| 4 | 响应式 → skipOnMobileDryRun pattern | PASS |
| 5 | 容错 → non-critical path skips | PASS |

## systemic_preflight

- adjacent_impact: E2E 测试独立于单元测试，不影响其他测试文件
- pattern_consistency: Playwright + Page Object pattern
- reuse: 复用现有 test fixtures

## runtime_validation (2026-04-12)

```
环境: 本机 Vite dev server (localhost:5173) + uvicorn backend (localhost:18929)
前置: E2E_BASE_URL=http://localhost:5173 npx playwright test

结果: 6 passed, 2 skipped, 0 failed (1.7s)

详细:
✓ 页面加载与导航 - 首页加载成功 (title: Growth Assistant - 个人成长助手)
✓ 页面加载与导航 - 导航栏包含所有页面链接
✓ 页面加载与导航 - 导航到任务列表页
✓ 页面加载与导航 - 响应式：移动端页面可用
✓ 页面加载与导航 - 响应式：桌面端页面可用
✓ 数据创建与验证闭环 - 通过 API 创建任务后在列表中可见
⊘ 非关键路径 - 搜索功能可用 (skip: 首页无独立搜索框)
⊘ 非关键路径 - 任务状态切换 (skip: 无已有任务数据)

修复项:
- frontend/index.html title: "frontend" → "Growth Assistant - 个人成长助手"
- frontend/vite.config.ts proxy: 添加 rewrite 移除 /api 前缀，target 改为 localhost:18929
- e2e/tests/user_flows.spec.ts: API 路径对齐 /api/entries，任务列表导航 /growth/tasks
```

配套变更:
- frontend/index.html: 标题修正
- frontend/vite.config.ts: 代理 rewrite + 端口修正
- e2e/tests/user_flows.spec.ts: 路径/选择器修复

## Audit（补执行）

本地审核（补执行轮）：done, commit_ready=true
- 4 条验收条件全部满足
- 硬断言/容错/数据闭环三层结构清晰
- try/finally 清理保护到位，无冗余等待
- 证据文档充分
