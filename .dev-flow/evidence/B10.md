# B10 执行证据

| Task ID | B10 |
| Phase | P3 |
| Name | 认证中间件 + deps.py 用户上下文注入 |

## 实现摘要

- deps.py 添加 `get_current_user` 依赖函数
- 使用 HTTPBearer scheme 从 Authorization header 提取 token
- 复用 auth_service.get_current_user_from_token 验证 token + 加载用户
- 无效/过期 token → 401 + WWW-Authenticate: Bearer
- deps.py 保持全局单例，user_id 在路由层通过 user.id 传递

## 验证

```bash
uv run pytest tests/unit/ -q  # 363 passed
```
