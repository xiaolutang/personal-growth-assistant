# S05: E2E Helper 扩展（R020）

> 注意：本文件覆盖 R020 周期的 S05 任务。R004 周期的同名 S05 证据已归档。

## Task Snapshot

- **ID**: S05
- **Name**: E2E Helper 扩展
- **Module**: e2e-infra
- **Phase**: 1 (R020)
- **Status**: completed
- **Started**: 2026-04-20
- **Completed**: 2026-04-20

## Acceptance Criteria

1. 新增 e2e/tests/helpers/goals.ts — 封装 10 个 Goals API helper
2. 新增 e2e/tests/helpers/export.ts — 封装 exportEntries (markdown/json)
3. 扩展 e2e/tests/helpers/api.ts — 新增 searchEntriesFTS5（GET /entries/search/query）
4. 现有 E2E 测试全部通过

## Execution Evidence

### Changes

1. **e2e/tests/helpers/goals.ts**（新增 200 行）— 10 个 helper 函数：
   - `createGoal` — POST /api/goals（类型收紧：MetricType union, title/metric_type/target_value 必填）
   - `listGoals` — GET /api/goals（status 过滤收紧为 'active'|'completed'|'abandoned'）
   - `getGoal` — GET /api/goals/{id}
   - `updateGoal` — PUT /api/goals/{id}
   - `deleteGoal` — DELETE /api/goals/{id}
   - `linkEntry` — POST /api/goals/{id}/entries
   - `unlinkEntry` — DELETE /api/goals/{id}/entries/{entry_id}
   - `listGoalEntries` — GET /api/goals/{id}/entries
   - `toggleChecklist` — PATCH /api/goals/{id}/checklist/{item_id}
   - `getProgressSummary` — GET /api/goals/progress-summary
   - 所有 path 参数使用 `encodeURIComponent()` 编码

2. **e2e/tests/helpers/export.ts**（新增 50 行）— 1 个 helper 函数：
   - `exportEntries` — GET /api/entries/export（format 收紧为 'markdown'|'json'，type 收紧为 EntryCategory union）

3. **e2e/tests/helpers/api.ts**（新增 20 行）：
   - `searchEntriesFTS5` — GET /api/entries/search/query（FTS5 全文搜索，不依赖 Qdrant）
   - 保留原有 `searchEntries`（POST /api/search，向量搜索）

### tests_run

| Command | Result |
|---------|--------|
| `cd e2e && npm test` | 40 passed (1.5m) |

### Acceptance Check

| # | Criteria | Status |
|---|----------|--------|
| 1 | goals.ts 10 个 helper | ✅ createGoal, listGoals, getGoal, updateGoal, deleteGoal, linkEntry, unlinkEntry, listGoalEntries, toggleChecklist, getProgressSummary |
| 2 | export.ts exportEntries | ✅ 支持 markdown/json + type/start_date/end_date 过滤 |
| 3 | api.ts searchEntriesFTS5 | ✅ GET /api/entries/search/query?q=xxx&limit=10 |
| 4 | 现有 E2E 测试通过 | ✅ 40 passed |

## Code Review Result

- Provider: codex_plugin (xlfoundry-code-review)
- Round 1: fail — TypeScript 签名过宽 + path 参数缺 encodeURIComponent
- Round 2: pass — MetricType/EntryCategory union 收紧 + 所有 path 参数编码
- reviewed_at: 2026-04-20

## Audit Result

- Provider: codex_plugin (xlfoundry-audit)
- Round 1: partial — 证据文件未更新 + 跟踪文件未同步
- Fix: 写入 R020/S05 证据 + 同步 feature_map.md/test_coverage.md/alignment_checklist.md
