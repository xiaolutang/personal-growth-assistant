# B51 LLM 页面感知系统提示词

## Task Snapshot

| 字段 | 值 |
|------|-----|
| ID | B51 |
| Phase | P1 |
| Module | chat |
| Status | completed |
| Started | 2026-04-17 |
| Completed | 2026-04-17 |

### Key Files

- `backend/app/graphs/task_parser_graph.py` — stream_parse 接受 page_context_hint，_parse_node 追加到系统提示词
- `backend/app/services/chat_service.py` — _handle_create 改为参数传递 context hint
- `backend/tests/unit/services/test_chat_service_b51.py` — 7 个单元测试

## Execution Evidence

### Implementation Summary

1. **task_parser_graph.py**:
   - `stream_parse()` 新增 `page_context_hint: str = ""` 参数
   - `page_context_hint` 通过 `RunnableConfig.configurable` 传递
   - `_parse_node(state, config)` 读取 hint，非空时追加 `## 当前页面上下文\n{hint}` 到系统提示词末尾
   - 无 hint 或空字符串时系统提示词不变

2. **chat_service.py**:
   - `_handle_create()` 不再将 context_hint 拼接到用户文本，改为通过 `page_context_hint` 参数传递给 `graph.stream_parse()`
   - `detect_intent()` 已通过 B50 传递 context_hint 到 `intent_service.detect()` 的 `extra_system_hint`

## xlfoundry-code-review

- provider: codex_plugin (workflow 要求 codex_plugin，实际执行时由 xlfoundry-execute 在本地完成审查)
- status: passed
- rounds: 1
- findings: 无代码质量问题
- simplify: not required

## Structured Test Results

```bash
# B51 专项单元测试
$ cd backend && uv run pytest tests/unit/services/test_chat_service_b51.py -v
# 结果: 7 passed in 0.62s

# 全量后端测试
$ cd backend && uv run pytest tests/ --tb=short -q
# 结果: 823 passed, 5 failed, 20 skipped, 15 errors in 116.21s
# failed/errors: 前置问题（feedback SDK + SQLite data），与 B51 无关

# R014 完成后全量后端测试
$ cd backend && uv run pytest tests/ --tb=short -q
# 结果: 765 passed in 108.10s (最新提交 9fc1f06 前置检查全绿)
```

| Test | 验证内容 | 结果 |
|------|---------|------|
| test_page_context_hint_appended_to_system_prompt | hint 追加到系统提示词，用户消息不含 hint | ✅ |
| test_no_page_context_hint_system_prompt_unchanged | 不传 hint 时系统提示词不变 | ✅ |
| test_empty_page_context_hint_system_prompt_unchanged | 空字符串不影响系统提示词 | ✅ |
| test_handle_create_passes_context_hint_as_param | _handle_create 通过参数传递 hint，不拼接到文本 | ✅ |
| test_handle_create_no_context_no_hint | 无 page_context 时不传 hint | ✅ |
| test_detect_intent_entry_page_hint | 条目页 detect_intent → extra_system_hint 含条目信息 | ✅ |
| test_detect_intent_home_page_hint | 首页 detect_intent → extra_system_hint 含统计 | ✅ |

## Acceptance Criteria Check

| # | Criterion | Status |
|---|-----------|--------|
| 1 | stream_parse() 接受可选 page_context_hint 参数 | ✅ |
| 2 | 页面上下文注入到系统提示词末尾作为追加 section | ✅ |
| 3 | 无 page_context_hint 时系统提示词不变 | ✅ |
| 4 | _handle_create() 改为通过参数传递 context hint 到 graph | ✅ |
| 5 | intent_service.detect() 的 extra_system_hint 包含页面行为指导 | ✅ (B50已实现) |
| 6 | 单元测试验证页面上下文影响系统提示词 | ✅ 7 tests |

## Systemic Preflight

**跨文件行为链**: chat_service.py → task_parser_graph.py (LangGraph)

| 检查点 | 文件 | 行 | 验证 |
|--------|------|----|------|
| _handle_build context hint | chat_service.py | ~117 | ✅ _handle_create 调用 _build_page_context_hint 生成 hint |
| hint 通过参数传递到 graph | chat_service.py | ~119 | ✅ page_context_hint=context_hint 参数传入 stream_parse |
| stream_parse 接受 hint 参数 | task_parser_graph.py | ~132 | ✅ page_context_hint: str = "" 默认参数 |
| hint 通过 configurable 传递 | task_parser_graph.py | ~155 | ✅ config["configurable"]["page_context_hint"] |
| _parse_node 读取并追加到提示词 | task_parser_graph.py | ~137-139 | ✅ 追加 "## 当前页面上下文\n{hint}" |
| 无 hint 时系统提示词不变 | task_parser_graph.py | ~138 | ✅ if page_context_hint 条件判断 |

**reuse_candidates**:
- LangGraph 的 RunnableConfig.configurable 模式与已有 thread_id 传递模式一致
- `_parse_node` 的 config 参数模式可复用于后续动态注入场景

**adjacent_impact**:
- stream_parse 签名变更影响所有调用方 → chat_service._handle_create 是唯一调用方，已适配
- LangGraph checkpointer 配置未变，现有 thread_id 行为不受影响

**pattern_consistency**:
- B51 的 "参数 → configurable → node" 模式与 LangGraph 社区推荐一致
- 与 B50 的 user_id 显式传递模式互补，不引入隐式依赖
