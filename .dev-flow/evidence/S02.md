# S02 执行证据

| Task ID | S02 |
| Phase | P5 |
| Name | 全链路联调与首用验证 |

## 验证结果

### 自动化测试
- 后端单元测试：384 passed
- 前端单元测试：177 passed
- 前端构建：成功

### 认证链路验证
- 注册流程：POST /auth/register → 创建用户 → 自动登录 → 返回 token
- 登录流程：POST /auth/login → 验证凭证 → 返回 access_token（7天过期）
- Token 验证：GET /auth/me → Bearer token → 返回用户信息
- 登出流程：前端清除 localStorage token → 跳转 /login

### 数据隔离验证（单元测试覆盖）
- SQLite entries 表 user_id 过滤：test_sqlite_user_isolation.py（9 tests）
- Markdown 按用户目录隔离：test_storage_factory.py（7 tests）
- Session 会话按用户隔离：test_session_meta_store.py（8 tests）
- Neo4j/Qdrant 存储层 user_id 过滤：原有测试已覆盖
- 认证核心路径：test_auth.py（13 tests）

### 路由守卫验证
- 8 个路由文件全部添加 Depends(get_current_user)
- 无 token 请求返回 401/403
- 前端 ProtectedRoute 组件包裹所有需认证路由
- API 拦截器自动注入 Authorization header
- 401 响应自动跳转登录页

### 已有数据兼容
- entries 表 user_id 默认 '_default'（迁移脚本幂等）
- session_meta 表 user_id 默认 '_default'
- Markdown 数据迁移到 data/users/_default/（StorageFactory.migrate_default_user）

## 未自动验证项（需人工）
- 双用户隔离：注册两个用户，互相看不到对方数据
- Token 过期场景处理
- 前端 UI 交互流程完整性
