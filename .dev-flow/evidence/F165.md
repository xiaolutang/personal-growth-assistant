# F165: Flutter ApiClient API 方法 + 测试

## 任务描述
在 Flutter ApiClient 中添加新页面所需的 API 方法并编写测试。

## 新增方法列表

### api_client.dart（已存在，确认无变更需求）
| 方法 | HTTP | 路径 | 说明 |
|------|------|------|------|
| `createEntry` | POST | `/entries` | 创建条目 |
| `fetchGoals` | GET | `/goals` | 获取目标列表（status/limit 参数） |
| `fetchGoal` | GET | `/goals/{id}` | 获取目标详情 |
| `fetchMilestones` | GET | `/goals/{goalId}/milestones` | 获取里程碑列表 |
| `createMilestone` | POST | `/goals/{goalId}/milestones` | 创建里程碑 |
| `updateMilestone` | PUT | `/goals/{goalId}/milestones/{milestoneId}` | 更新里程碑 |
| `deleteMilestone` | DELETE | `/goals/{goalId}/milestones/{milestoneId}` | 删除里程碑 |
| `fetchReviewSummary` | GET | `/review/{period}` | 回顾报告（daily/weekly/monthly + date） |
| `fetchTrends` | GET | `/review/trend` | 趋势数据（period/days/weeks） |
| `fetchInsights` | GET | `/review/insights` | AI 深度洞察（period 必填） |

## 测试修复
- `api_client_test.dart`: 添加 `TestWidgetsFlutterBinding.ensureInitialized()`；`createTestClient` 清除拦截器避免 FlutterSecureStorage platform channel 问题；404 错误场景测试使用 `createTestClient`
- `explore_provider_test.dart`: `_FakeApiClient` 添加 9 个缺失的方法实现（createEntry, fetchGoals, fetchGoal, fetchMilestones, createMilestone, updateMilestone, deleteMilestone, fetchReviewSummary, fetchTrends, fetchInsights）

## flutter analyze 结果
```
0 errors, 0 warnings, 24 info (全部为预存的 trailing comma lint)
```

## flutter test 结果

### api_client_test.dart
```
39/39 All tests passed!
- 4 basic config tests
- 18 errorMessage tests
- 17 Business Methods tests (createEntry, fetchGoals x2, fetchGoal, fetchMilestones, createMilestone, updateMilestone, deleteMilestone, fetchReviewSummary x5, fetchTrends x2, fetchInsights, 404 error)
```

### explore_provider_test.dart
```
34/34 All tests passed!
```

## code_review_status
pass - 方法签名一致、与现有模式匹配、错误处理完整

## audit_status
done - 验收条件全部满足，commit ready
