# B97: 提取 _calculate_mastery_from_stats 到共享模块

## Task Snapshot
- **ID**: B97
- **Module**: mastery-utils
- **Status**: in_progress
- **Fix Level**: L1 (重构提取)

## Execution Evidence

### Changes
1. **新建 `backend/app/utils/__init__.py`** — utils 包初始化
2. **新建 `backend/app/utils/mastery.py`** — 独立掌握度计算函数 `calculate_mastery_from_stats`
3. **修改 `backend/app/services/review_service.py`** — `_calculate_mastery_from_stats` 改为委托到 `utils.mastery`
4. **修改 `backend/app/services/knowledge_service.py`** — `_calculate_mastery_from_stats` 直接导入 `utils.mastery`，移除 `from app.services.review_service import ReviewService` 循环依赖

### Tests Run
```bash
uv run pytest tests/unit/test_mastery.py -v
# 11 passed
```

### Test Results
- 基础阈值矩阵 4 项: new/beginner/intermediate/advanced
- relationship_count 折算 3 项: 正常折算/单独关系/负数处理
- KnowledgeService 调用路径 2 项
- ReviewService 调用路径 1 项
- 回归 1 项: ReviewService 静态方法委托验证

### Regression
```
uv run pytest tests/ -q
# 待回归结果
```

### Acceptance Check
1. [x] 新建 app/utils/mastery.py，包含 4 参数 calculate_mastery_from_stats 函数
2. [x] ReviewService._calculate_mastery_from_stats 委托到 utils.mastery
3. [x] KnowledgeService._calculate_mastery_from_stats 直接导入 utils.mastery，移除循环依赖
4. [x] 两个 service 的掌握度计算结果不变
5. [x] 现有测试通过
