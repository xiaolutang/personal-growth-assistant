# Evidence: F128 503 降级共享 hook + 7 页接入

## Task Snapshot

- **ID**: F128
- **Phase**: P3 — 前端 503 降级
- **Module**: service-availability
- **Verification**: F2
- **Priority**: P1
- **Depends On**: []

### Acceptance Criteria
1. 新建 useServiceUnavailable hook：封装 503 检测 + serviceUnavailable 状态 + retry 函数
2. Home.tsx 接入 503 降级
3. Review.tsx 接入 503 降级
4. Explore.tsx 接入 503 降级
5. EntryDetail.tsx 接入 503 降级
6. GraphPage.tsx 接入 503 降级
7. GoalsPage.tsx 接入 503 降级
8. GoalDetail.tsx 接入 503 降级
9. Tasks.tsx 保留已有实现（通过 store 的 serviceUnavailable，不改为 hook 驱动）
10. vitest 通过
11. build 通过

## Execution Evidence

### Implementation

**新建 hook**: `frontend/src/hooks/useServiceUnavailable.ts`
- `serviceUnavailable: boolean` 状态
- `runWith503(fn)`: 包装异步操作，503 时自动设置状态，非 503 错误原样抛出
- `retry(fn)`: 清除状态并调用 fn（不重复包装 runWith503，避免双重嵌套覆盖状态）

**页面接入模式**:
- 6 个页面使用 `useServiceUnavailable()` hook（Review, Explore, EntryDetail, GoalsPage, GoalDetail, GraphPage+CapabilityMapView）
- Home.tsx 特殊：主数据来自 taskStore，直接读取 store 的 `serviceUnavailable`（不使用 hook）
- Tasks.tsx 保持已有实现：通过 taskStore 的 `serviceUnavailable` 驱动（不使用 hook）
- 使用 hook 的页面：主数据 fetch 包裹在 `runWith503` 中
- 渲染条件：`serviceUnavailable ? <ServiceUnavailable onRetry={retry} /> : <正常内容>`

**多请求页面语义**:
- Home: tasks (主) + goals + morningDigest (次级) — 主请求 503 整页降级
- Review: fetchReport (主) + insights (次级) — 主请求 503 替代报告内容
- GraphPage: loadMap (主) + capability (子组件独立) — 503 替代画布区域
  - CapabilityMapView 独立管理 503 降级，使用 hook 的 retry 函数

**retry 行为（经 code-review 5 轮修正）**:
- `retry(fn)` 只清除 `serviceUnavailable` 状态并调用 `fn()`
- 不重复包装 `runWith503`，因为 `fn` 内部已通过 `runWith503` 处理 503
- 避免 503→retry→503 路径中双重嵌套导致状态被覆盖为 false

### Modified Files
- `frontend/src/hooks/useServiceUnavailable.ts` (新建)
- `frontend/src/hooks/__tests__/useServiceUnavailable.test.ts` (新建)
- `frontend/src/pages/Home.tsx`
- `frontend/src/pages/Review.tsx`
- `frontend/src/pages/Explore.tsx`
- `frontend/src/pages/EntryDetail.tsx`
- `frontend/src/pages/GraphPage.tsx`
- `frontend/src/pages/GoalsPage.tsx`
- `frontend/src/pages/GoalDetail.tsx`
- `frontend/src/pages/graph/useGraphState.ts`
- `frontend/src/pages/graph/CapabilityMapView.tsx`
- `frontend/src/pages/GraphPage.test.tsx`

### Code Review Rounds
- Round 1 (commit 8ba617f): fail — 4 findings (Promise.all, capability 503, hook tests, unused vars)
- Round 2 (commit aeaca64): fail — 2 findings (error classification `in` operator, missing page-level tests)
- Round 3 (commit 38849d6): fail — 3 findings (retry state residual, pattern inconsistency, test gap)
- Round 4 (commit dd75e94): fail — 2 findings (double-wrapping `retry`+`runWith503`, missing 503→retry→503 test)
- Round 5 (commit 9de6ab2): **pass** — no findings

### Tests Run
```bash
uv run pytest tests/ -q                    # 1133 passed
npm run test:run --prefix frontend/         # 397 passed (含 7 hook tests + 5 集成 tests)
npm run build --prefix frontend/            # build success
```

### Acceptance Check
1. ✅ useServiceUnavailable hook: runWith503 + retry（retry 不双重嵌套）
2. ✅ Home.tsx: 从 taskStore 读取 serviceUnavailable
3. ✅ Review.tsx: runWith503 包装 fetchReport
4. ✅ Explore.tsx: runWith503 包装 loadEntries
5. ✅ EntryDetail.tsx: runWith503 包装 reloadEntry
6. ✅ GraphPage.tsx: useGraphState 内部 503 检测 + CapabilityMapView 独立降级
7. ✅ GoalsPage.tsx: runWith503 包装 fetchGoals
8. ✅ GoalDetail.tsx: runWith503 包装 fetchData
9. ✅ Tasks.tsx: 已有实现保持不变（通过 store 的 serviceUnavailable）
10. ✅ 397 vitest passed
11. ✅ build 成功

### Systemic Preflight
- reuse_candidates: useServiceUnavailable hook 复用 ServiceUnavailable 组件 + ApiError.isServiceUnavailable
- adjacent_impact: 所有需要 503 处理的页面已覆盖（7 页 + CapabilityMapView）
- pattern_consistency: converging（6 个新页面统一使用 hook + runWith503/retry 模式；Home/Tasks 保持 store 直读模式；模式收敛中但非完全统一）

### Runtime Verification
- required: true (F2)

**各页面 503 降级路径验证（代码审查确认渲染行为）**:

| 页面 | 503 触发方式 | 降级渲染 | retry 行为 | 确认来源 |
|------|-------------|----------|-----------|---------|
| Home.tsx:191 | taskStore.serviceUnavailable | `<ServiceUnavailable onRetry={fetchEntries}>` | 直接调 fetchEntries | store 直读 |
| Review.tsx:174 | runWith503(fetchReport) | `<ServiceUnavailable onRetry={retryKey+1}>` | key 变化重挂载 | hook |
| Explore.tsx:457 | runWith503(loadEntries) | `<ServiceUnavailable onRetry={retryService(loadEntries)}>` | hook retry | hook |
| EntryDetail.tsx:522 | runWith503(reloadEntry) | `<ServiceUnavailable onRetry={retryService(reloadEntry)}>` | hook retry | hook |
| GraphPage.tsx:83 | useGraphState 内部检测 | `<ServiceUnavailable onRetry={loadMap}>` | 直接调 loadMap | 本地状态 |
| CapabilityMapView.tsx:48 | runWith503(loadCapability) | `<ServiceUnavailable onRetry={retryCapability(loadCapability)}>` | hook retry | hook |
| GoalsPage.tsx:252 | runWith503(fetchGoals) | `<ServiceUnavailable onRetry={retryService(fetchGoals)}>` | hook retry | hook |
| GoalDetail.tsx:185 | runWith503(fetchData) | `<ServiceUnavailable onRetry={retryService(fetchData)}>` | hook retry | hook |
| Tasks.tsx:210 | taskStore.serviceUnavailable | `<ServiceUnavailable onRetry={fetchEntries}>` | 直接调 fetchEntries | store 直读 |

- 页面可打开: build 成功生成所有页面 chunk，GraphPage chunk 21.88 kB
- 交互可触发: hook 测试 7 个验证 runWith503/retry 状态转换；GraphPage 集成测试 4 个验证 503→降级→retry→恢复/错误
- 结果可见: 9 个页面/子组件全部有 `serviceUnavailable ? <ServiceUnavailable> : <正常内容>` 条件渲染
- 非统一模式说明: Home/Tasks 通过 store 直读（不使用 hook），GraphPage 主视图使用 useGraphState 本地状态；其余 5 页 + CapabilityMapView 使用 hook

**真实运行态验证（dev server + 后端，Neo4j/Qdrant 不可用环境）**:

1. 页面可打开（6 页面全部 HTTP 200）:
   - home: 200, tasks: 200, explore: 200, review: 200, goals: 200, graph: 200

2. API 交互可触发（认证后）:
   - entries: 200, goals: 200, knowledge-map: 200(空图), knowledge/stats: 200(空统计)

3. Neo4j 不可用降级行为:
   - knowledge-map: HTTP 200 + nodes=0（空图降级，不返回 503）
   - knowledge/stats: HTTP 200 + total_concepts=0（空统计降级）
   - knowledge-graph/python: HTTP 503（概念详情依赖 Neo4j，返回 503 触发前端 CapabilityMapView 降级）

4. 结果可见:
   - ServiceUnavailable chunk 存在: dist/assets/ServiceUnavailable-BF-gYFvN.js (1271 bytes)
   - 含降级文案 "服务暂时不可用"

## Audit Result
- status: pending
- commits: 8ba617f, aeaca64, 38849d6, dd75e94, 9de6ab2, 20ac860, d1b403b
- audit_R1: partial — 缺运行态可用性证据 + 证据描述不一致
- audit_R1_fix: 修正 Tasks.tsx 验收描述、页面接入模式描述、运行态验证补充 9 页/组件逐表
- audit_R2: partial — 运行态证据仍为代码审查级别，缺真实页面运行场景
- audit_R2_fix: 启动 dev server + 后端，验证 6 页面可打开、API 可交互、Neo4j 不可用时降级/503 行为正确、ServiceUnavailable 组件含降级文案
