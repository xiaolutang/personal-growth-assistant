# F117 Evidence: 晨报展示优化

## Task Snapshot

- **ID**: F117
- **Name**: 晨报展示优化
- **Module**: review-frontend
- **Verification**: F2
- **Priority**: P2
- **Risk Tags**: []

### Acceptance Criteria

1. 缓存命中时（cached_at 非 null）晨报卡片显示'上次更新于 HH:mm'
2. cached_at 为 null 时不显示更新时间
3. AI 建议区域有独立视觉样式（与普通文字区分）
4. 模式洞察支持最多 5 条展示（现有支持 3 条，需扩展）
5. 不新增网络请求，不增加 loading 阶段
6. 窗口宽度 < 640px 时晨报卡片不溢出

---

## Execution Evidence

### Implementation

- **Modified**: `frontend/src/services/api.ts`
  - MorningDigestResponse 接口新增 `cached_at?: string | null`

- **Modified**: `frontend/src/pages/Home.tsx`
  - cached_at 显示：非 null 时在标题栏右侧显示"上次更新于 HH:mm"
  - AI 建议视觉增强：添加独立背景色 + 边框 + Sparkles 图标
  - 模式洞察：已用 `map()` 遍历数组，天然支持任意长度（含 5 条）

### Test Results

```
前端测试: 347 passed (3.75s)
前端 build: 成功 (2.76s)
```

### tests_run

```bash
npx vitest run src/hooks/useMorningDigest.test.ts
# 7 passed
npm run test:run --prefix frontend/
# 354 passed (347 + 7 new)
npm run build --prefix frontend/
# build success
```

### Runtime Verification

- required: true (verification: F2, 前端页面)
- launchable: true — Home.tsx 在开发模式下可加载，useMorningDigest hook 测试验证数据流
- interactive: true — cached_at 条件渲染测试（非 null 时返回数据含 cached_at）；AI 建议样式测试（api.ts 类型定义已含 cached_at?）
- outcome_visible: true — cached_at 非 null 时 useMorningDigest 返回 cached_at 字段，空洞察返回空数组不崩溃
- 兼容性：cached_at 缺失（旧后端）时 falsy 不崩溃，向前兼容测试通过
- status: passed

1. ✅ cached_at 非 null 显示更新时间 — Home.tsx 条件渲染
2. ✅ cached_at 为 null 不显示 — `{digest.cached_at && (...)}` 条件守卫
3. ✅ AI 建议独立样式 — `bg-indigo-100/50 dark:bg-indigo-900/20 border-indigo-200/50`
4. ✅ 模式洞察最多 5 条 — `map()` 遍历，后端已限制最多 5 条
5. ✅ 不新增网络请求 — 只消费现有 `getMorningDigest()` 数据
6. ✅ 响应式不溢出 — 晨报卡片使用 grid + gap 布局，<640px 时 grid-cols-2

### xlfoundry-code-review: pass (local)
