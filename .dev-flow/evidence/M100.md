# Evidence: M100 移动端拖拽排序

## Task Snapshot

- **ID**: M100
- **Phase**: P5 — 移动端独立功能
- **Module**: mobile
- **Verification**: F2
- **Priority**: P1
- **Depends On**: 无（Flutter 独立模块）

### Acceptance Criteria
1. 任务页支持拖拽排序（ReorderableListView）
2. 拖拽时有视觉反馈（缩放动画）
3. 排序为本地状态，不持久化
4. 切换筛选 Tab 时重置排序
5. 下拉刷新时重置排序
6. 单条目分组不显示拖拽
7. flutter analyze 通过
8. flutter test 通过

## Execution Evidence

### Implementation

**修改文件 (1 个)**:
- `mobile/lib/pages/tasks_page.dart`

**新增测试文件 (1 个, 9 tests)**:
- `mobile/test/pages/tasks_page_test.dart`

**改动内容**:
1. 新增 `_reorderedEntries` 状态管理本地排序
2. 新增 `_sameEntryIds()` 辅助方法检测数据变化
3. 多条目分组使用 `ReorderableListView.builder` 替代 `Column`
4. 单条目分组直接渲染 `TaskCard`（无法拖拽）
5. `proxyDecorator` 添加缩放动画（1.0 → 1.05）
6. `_onFilterChanged` 重置 `_reorderedEntries = []`
7. `RefreshIndicator.onRefresh` 重置 `_reorderedEntries = []`
8. `_buildBody` 中同步检测：数据长度或 ID 不匹配时重置排序

### Tests Run
```bash
flutter analyze                              # No issues found
flutter test test/pages/tasks_page_test.dart # 12 passed
flutter test                                 # 170 passed (All tests passed)
```

### Runtime Verification
- required: true (F2 前端页面)
- launchable: true — TasksPage 作为 GoRouter 首页 '/' 可渲染
- interactive: true — 拖拽测试通过 startGesture 连续手势触发 ReorderableDelayedDragStartListener
- outcome_visible: true — 拖拽后断言 B.dy < A.dy 证明渲染顺序变化
- status: passed

### Code Review Rounds
- Round 1: fail（2 findings: onReorder 分组索引映射错误、_sameEntryIds 不刷新 entry 数据）
- Round 2: fail（1 finding: offset-based 映射在非连续分组时错误，测试未覆盖修复路径）
- Round 3: fail（2 findings: 测试未真正触发 onReorder、未覆盖同 ID 数据刷新路径）
- Round 4: fail（1 finding: longPress + drag 是两个分离手势，未触发 ReorderableDelayedDragStartListener）
- Round 5: pass（0 findings）

### Bug Fixes (from code-review R1+R2)
1. **R1 Fix 1 → R2 Fix**: onReorder 从 offset-based 索引改为基于 ID 的映射：`groupIds.removeAt/insert` 重组分组顺序，`map` 遍历全局列表替换同组条目
2. **R1 Fix 2**: IDs 相同时，`else` 分支用 `state.entries.firstWhere((e) => e.id == local.id)` 刷新 entry 数据

### Bug Fixes (from code-review R1)
1. **onReorder 索引映射**: 将分组局部索引通过 `indexOf(entries.first) + offset` 转换为全局索引，修复跨组拖拽操作错误条目
2. **entry 数据同步**: IDs 相同时，`else` 分支用 `state.entries.firstWhere((e) => e.id == local.id)` 刷新 entry 数据，确保乐观更新后状态正确

### Test Scenarios
| 测试 | 场景 |
|------|------|
| 空列表显示空状态引导文案 | entries=[], 验证"暂无任务"文案 |
| 错误状态显示错误信息和重试按钮 | error="网络连接失败", 验证错误文本+重试按钮 |
| 加载中显示进度指示器 | isLoading=true, 验证 CircularProgressIndicator |
| 按状态分组显示任务 | 4 条不同状态, 验证分组标题 |
| 多条目分组使用 ReorderableListView | 2 条 doing, 验证 ReorderableListView 存在 |
| 单条目分组不使用 ReorderableListView | 1 doing + 1 waitStart, 验证无 ReorderableListView |
| 筛选 Tab 切换 | 点击"进行中" tab, 验证交互 |
| 渲染 4 个筛选 Tab | 验证 全部/进行中/待开始/已完成 |
| AppBar 标题为"任务" | 验证 AppBar title |
| 拖拽重排验证：ID 映射正确 | 交错 fixture + startGesture 连续手势 + 顺序断言 B.dy < A.dy |
| 同 ID 数据刷新后分组正确 | _MutableEntryListNotifier 二次 setState，验证分组从进行中(2)→进行中(1)+已完成(1) |
| 已暂停任务显示在已暂停分组 | 2 paused, 验证"已暂停(2)"标题 |

### Acceptance Check
1. ✅ ReorderableListView.builder 实现，支持拖拽排序
2. ✅ proxyDecorator 缩放动画 1.0→1.05
3. ✅ _reorderedEntries 本地状态，不持久化
4. ✅ _onFilterChanged 重置 _reorderedEntries = []
5. ✅ RefreshIndicator.onRefresh 重置 _reorderedEntries = []
6. ✅ entries.length == 1 时直接渲染 TaskCard，不使用 ReorderableListView
7. ✅ flutter analyze: No issues found
8. ✅ flutter test: 167 passed (All tests passed)

### Systemic Preflight
- reuse_candidates: 无 — TasksPage 使用标准 Flutter ReorderableListView
- adjacent_impact: 无相邻影响 — 仅修改 tasks_page.dart
- pattern_consistency: converging — 与项目现有 Flutter 组件测试模式一致（ProviderScope override + _FakeNotifier）
