# B82: knowledge.py 错误信息脱敏

## Task Snapshot
- id: B82
- module: knowledge
- verification: L2

## Execution Evidence

### Implementation
- File: `backend/app/routers/knowledge.py`
- Added: `import logging` + `logger = logging.getLogger(__name__)`
- Changed: 8 × `raise HTTPException(500, detail=f"查询失败: {str(e)}")` → `logger.error("查询失败", exc_info=True)` + `raise HTTPException(500, detail="查询失败，请稍后重试")`
- Changed: 1 × search error similarly
- Preserved: 3 × `except ValueError as e` → 503 blocks unchanged

### tests_run
- Backend: 923 passed, 20 skipped (pytest)
- Existing knowledge API tests verify error handling paths

### test_results
- All knowledge API tests pass
- Error responses no longer contain str(e) details
- ValueError 503 paths unchanged

### acceptance_check
- [x] 9 except Exception blocks no longer contain str(e) or f-string error info
- [x] Each block uses logger.error with exc_info=True
- [x] Client-facing messages are fixed text ("查询失败，请稍后重试")
- [x] except ValueError blocks (503) unchanged
