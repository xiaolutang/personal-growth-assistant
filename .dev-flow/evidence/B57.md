# B57: 认证加固 + 信息泄露修复 + 部署安全

## Task Snapshot
- **ID**: B57
- **Phase**: security
- **Priority**: 1
- **Verification**: L2

## Execution Evidence

### 改动文件
1. `backend/app/services/auth_service.py` — create_access_token 中 JWT_SECRET 空值检查
2. `backend/app/main.py` — lifespan 中 validate_jwt 从 warning 改为 raise（阻塞启动）
3. `backend/app/middleware/__init__.py` — 500 错误生产环境不返回异常详情，get_settings import 移到顶部
4. `deploy/docker-compose.yml` — NEO4J_PASSWORD 移除默认值 changeme123
5. `backend/tests/conftest.py` — 测试环境注入默认 JWT_SECRET
6. `backend/tests/unit/test_auth.py` — 新增空/缺失 JWT_SECRET 测试
7. `backend/tests/unit/test_middleware_error.py` — 新增中间件脱敏测试

### 测试结果
- 后端测试: 831 passed, 20 skipped

### Code Review
- Round 1: return_to_implementer（JWT 校验不阻塞启动、import 在热路径）
- Round 2 修复: lifespan raise + import 移到模块顶部

## Acceptance Check

| # | 验收条件 | 状态 |
|---|----------|------|
| 1 | JWT_SECRET 为空字符串时应用启动抛出明确错误 | ✅ |
| 2 | 500 错误响应不含 str(e) 和异常类型名（生产环境） | ✅ |
| 3 | docker-compose.yml 中 NEO4J_PASSWORD 无默认值 | ✅ |
| 4 | 现有测试全部通过 | ✅ 831 passed |
