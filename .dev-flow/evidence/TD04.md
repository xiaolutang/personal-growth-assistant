# TD04 MCP Server 拆分为多文件

## Task Snapshot

- **ID**: TD04
- **Name**: MCP Server 拆分为多文件
- **Phase**: P7
- **Priority**: medium

### Acceptance Criteria

1. mcp/ 目录下有 tools.py、handlers.py、server.py 三个文件 ✓
2. tools.py 只包含 Tool Schema 定义 ✓
3. handlers.py 包含 9 个 handler 函数 ✓
4. server.py 只包含初始化 + call_tool 分发 + main ✓
5. MCP Server 功能不变 ✓
6. 后端测试通过 ✓

### Files Changed

| File | Before | After | Description |
|------|--------|-------|-------------|
| `backend/app/mcp/server.py` | 654 lines | 109 lines | 初始化 + 路由分发 + main |
| `backend/app/mcp/tools.py` | - | 237 lines | 9 个 Tool Schema 定义 |
| `backend/app/mcp/handlers.py` | - | 329 lines | 9 个 handler + 辅助函数 |
| `backend/tests/unit/test_mcp_split.py` | - | NEW | 8 tests 覆盖导入/路由/常量 |

### Test Results

```
$ cd backend && uv run pytest tests/unit/test_mcp_split.py -v
8 passed in 0.85s
$ uv run pytest tests/unit/ -q
286 passed in 2.12s
```

### Key Design Decision

Handler 签名从闭包（依赖全局 storage）改为显式参数传递 `async def handle_xxx(storage: SyncService, args: dict)`，使：
- handler 可独立测试
- server.py 的 TOOL_HANDLERS 字典替代 if-elif 链
- 全局 storage 只在 server.py 层注入

## Simplify

纯结构拆分，无新逻辑引入，无需修复。

## Audit

本地审核：done, commit_ready=true
