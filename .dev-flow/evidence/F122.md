# F122: useMorningDigest error 状态增强

## Task Snapshot

- **ID**: F122
- **Module**: hooks
- **Description**: 将 error 从 useState(false) 改为 useState<string | null>(null)，存储错误消息而非 boolean
- **Files**: useMorningDigest.ts, Home.tsx, useMorningDigest.test.ts, Review.trend.test.tsx
- **Verification**: F1 (前端组件测试)

## Execution Evidence

- **started_at**: 2026-04-25T10:00:00

### Changes

1. `useMorningDigest.ts`: `error` 从 `useState(false)` → `useState<string | null>(null)`
2. `useMorningDigest.ts`: catch 从 `setError(true)` → `setError(err instanceof Error ? err.message : "加载失败")`
3. `useMorningDigest.test.ts`: 更新 3 个旧断言 + 新增 3 个测试（非 Error fallback、成功后 null、卸载不更新）
4. `Review.trend.test.tsx`: 修复相邻 mock `error: false` → `error: null`

### Acceptance Check

| # | 验收条件 | 状态 |
|---|---------|------|
| 1 | error 状态类型从 boolean 改为 string \| null | ✅ useState<string \| null>(null) |
| 2 | catch 中捕获错误消息存入 error（优先 message，fallback 为 '加载失败'） | ✅ instanceof Error 三元判断 |
| 3 | 调用者 Home.tsx 适配新 error 类型 | ✅ truthy 判断兼容，无需改动 |
| 4 | vitest 通过 | ✅ 366 passed |
| 5 | build 通过 | ✅ build success |

### Tests Run

```bash
npx vitest run src/hooks/useMorningDigest.test.ts  → 10 passed
npx vitest run                                    → 366 passed (34 files)
npm run build                                     → success
```

### Test Results

- useMorningDigest 专属测试: 10 passed (7 existing F117 + 3 new F122)
- 全量 vitest: 366 passed, 0 failed

### Code Review

- status: pass
- findings: 无架构/系统性/复用问题
- routing: pass

### Systemic Preflight

- reuse_candidates: 无（独立 hook 变更）
- adjacent_impact: Review.trend.test.tsx mock 已修复
- pattern_consistency: converging（统一为 string | null 类型）

### Runtime Verification

- required: false（F1 组件测试级别，无用户可见页面变更）
- 改动为内部 hook 类型，Home.tsx 渲染行为不变（truthy/falsy 兼容）
- status: skipped
