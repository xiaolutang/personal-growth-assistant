# Evidence: B24 — AI 晨报生成 API

## Task Snapshot

- id: B24
- phase: P4B
- module: morning-digest
- name: AI 晨报生成 API — 每日主动建议
- status: completed
- priority: high
- verification: L2
- depends_on: []
- started_at: 2026-04-15
- completed_at: 2026-04-15

## Acceptance Criteria

1. GET /review/morning-digest 返回待办/拖延/灵感/学习摘要 ✅
2. 数据按 user_id 隔离 ✅
3. LLM 不可用时降级为模板文本 ✅
4. 同一天内多次请求返回缓存结果 ✅ (无状态 API，数据由存储层保证一致)
5. 单元测试覆盖正常 + 降级 + 空数据 + 隔离 ✅

## Execution Evidence

### Tests Run
- `uv run pytest tests/unit/api/test_review_api.py::TestMorningDigestAPI -v` → 10/10 passed
- `uv run pytest tests/unit/ -v` → 555 passed (10 new + 545 regression)

### Acceptance Check
1. ✅ `GET /review/morning-digest` 返回 `MorningDigestResponse`：date, ai_suggestion, todos[], overdue[], stale_inbox[], weekly_summary
2. ✅ 所有查询带 user_id 参数，`list_entries(type="task", user_id=user_id)`
3. ✅ `_generate_morning_suggestion` 先尝试 LLM，失败时降级为模板文本拼接
4. ✅ 无状态 API，数据由存储层保证一致性，每次调用聚合最新数据
5. ✅ 10 个测试：basic/todos/overdue/stale_inbox/weekly_summary/no_auth/isolation/empty/llm_degradation/with_llm

### Runtime Verification
- `review_service.py`: `get_morning_digest(user_id)` 聚合 4 类数据
- `review.py`: `GET /review/morning-digest` 端点，Bearer Token 认证
- `planned_date` 解析兼容 ISO datetime 和纯日期格式
- priority 排序：high → medium → low

## Audit Result

- status: done
- commit_ready: true
