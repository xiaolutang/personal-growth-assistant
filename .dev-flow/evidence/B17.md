# B17 — 修复 R003 迁移遗漏 — inbox Markdown 文件未复制到用户目录

## Task Snapshot

- **ID**: B17
- **Phase**: P1A
- **Module**: content-recovery
- **Verification**: L2
- **Priority**: high
- **fix_level**: L2

## Root Cause

R003 迁移脚本只更新了 SQLite entries.user_id，未复制 Markdown 文件到用户目录。Markdown 是 Source of Truth，SQLite 只是索引层。list_entries 走索引正常返回，但 get_entry/update_entry 走文件读取返回 404。

## Execution Evidence

### Changes

1. **scripts/fix_inbox_migration.py** — 迁移修复脚本：
   - 扫描 SQLite 所有类别（inbox/task/note/project），对比索引与实际文件
   - 从 data/ 根目录找到源文件复制到用户目录
   - 幂等执行，已存在文件不覆盖

2. **backend/app/infrastructure/storage/markdown.py** — read_entry 兜底回退：
   - 用户目录找不到文件时，回退到 `data/` 根目录查找（两级 parent）
   - 新增 `_safe_relative_path` 处理回退场景下 `relative_to` 报错

3. **backend/tests/unit/storage/test_storage_markdown.py** — 新增回退路径单测

### Migration Results

```
14 orphan inbox entries → all fixed
0 orphan task/note/project entries
71 total SQLite entries for user → 0 missing files
```

### API Verification

```
GET /entries/inbox-8c6cb6e9 → 200 (previously 404)
PUT /entries/inbox-8c6cb6e9 {"category":"task"} → 200 success
GET /entries?type=task → inbox-8c6cb6e9 visible in task list
```

### tests_run

| Command | Result |
|---------|--------|
| `uv run pytest tests/` | 488 passed, 13 markdown tests |
| Idempotency check | Second run: "No orphan entries found" |
| Full validation | 71 entries, 0 missing files |

### Acceptance Check

| # | Criteria | Status |
|---|----------|--------|
| 1 | GET /entries/{inbox-id} 返回 200 | ✅ verified |
| 2 | PUT /entries/{inbox-id} category 变更成功 | ✅ inbox→task |
| 3 | 迁移脚本幂等 | ✅ second run = no orphans |
| 4 | 所有文件已复制 | ✅ 14 fixed, 71 total 0 missing |
| 5 | tasks/notes/projects 无遗漏 | ✅ full scan = 0 orphans |
| 6 | pytest 全通过 | ✅ 488 passed |

### Code Review

- **Round 1**: fail — 回退路径写错(parent vs parent.parent)、脚本只扫 inbox、缺测试
- **Round 2**: pending
