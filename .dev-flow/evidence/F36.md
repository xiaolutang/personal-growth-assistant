# F36: 回顾页目标概览

## Execution Evidence

- Review.tsx 新增「目标进展」卡片，仅在 weekly/monthly tab 显示
- useEffect 按 reportType 加载 GET /goals/progress-summary
- 显示活跃目标数 + 已完成目标数
- 每个目标显示标题 + 进度条 + 百分比
- 无目标时自动隐藏（goalSummary.goals.length === 0）
- API 失败不阻塞（catch + null）
- build pass
