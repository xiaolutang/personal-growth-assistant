# B05 执行证据

## Task Snapshot

| 字段 | 值 |
|------|-----|
| Task ID | B05 |
| Phase | P2 |
| Module | data-isolation |
| Name | Markdown 存储按用户隔离目录 |
| Verification | L1 |
| Started At | 2026-04-13 |
| Completed At | 2026-04-13 |
| Evidence File | .dev-flow/evidence/B05.md |

## Acceptance Criteria

1. StorageFactory 类，按 user_id 创建/缓存 MarkdownStorage 实例 — **PASS**
2. MarkdownStorage 构造函数接受 base_dir — **PASS**（已有，无需修改）
3. 新用户首次访问时自动创建目录结构 — **PASS**
4. 现有 data/ 目录内容迁移到 data/users/_default/ — **PASS**
5. 单元测试：不同用户目录隔离 — **PASS**

## Execution Evidence

### 实现摘要

- 新建 `storage_factory.py`：StorageFactory 类 + 缓存 + 迁移
- StorageFactory.get_markdown_storage(user_id) 按需创建，带缓存
- _ensure_user_dirs 自动创建 tasks/notes/projects 子目录
- migrate_default_user 幂等迁移（不覆盖已有文件）
- 新建 7 个隔离测试

### 验证

```bash
uv run pytest tests/unit/storage/test_storage_factory.py -v  # 7 passed
uv run pytest tests/unit/ -q                                 # 363 passed
```
