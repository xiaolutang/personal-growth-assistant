# B01 Evidence: run_eval 集成 LLM-as-Judge (R049)

## Task Info
- taskId: B01
- phase: 1
- module: eval-judge
- name: run_eval 集成 LLM-as-Judge
- verification: L2

## Execution Evidence

### Systemic Preflight
- reuse_candidates: LLMJudge（judge.py）、EvalTranscript（transcript.py）、build_report_data（report_generator.py）
- adjacent_impact: report_template.html 需新增 Section 8；run_eval.py main() 需新增参数解析
- pattern_consistency: converging — 复用现有 Judge 和 Report 模式

### Files Changed
- backend/tests/eval/run_eval.py — 新增 --judge/--no-judge 参数 + judge 集成调用
- backend/tests/eval/report_generator.py — 新增 judge_summary/judge_by_category 聚合 + _render_judge_section
- backend/tests/eval/report_template.html — 新增 Section 8 占位符 + judge CSS
- backend/tests/eval/test_run_eval_report.py — 新增 TestJudgeIntegration + TestJudgeReportSection (8 tests)
- backend/tests/eval/judge.py — 新增 is_degraded/degraded_reason 公开属性

### Tests Run
- `uv run pytest tests/eval/test_run_eval_report.py -v` → 56 passed
- `uv run pytest tests/eval/ -v -k "not slow"` → 328 passed

### Acceptance Check
- criterion: --judge 参数（默认启用），--no-judge 跳过 → passed
- criterion: judge.evaluate() 写入 transcript.judge_result → passed
- criterion: Judge 失败不阻塞主流程 → passed
- criterion: HTML 报告 judge 区块（加权平均分 + 7 维度条形图）→ passed
- criterion: 测试覆盖成功/失败/降级/网络异常/无 Judge → passed

## Quality Gates
- code_review_status: pass
- code_review_provider: codex_plugin
- audit_status: done
- audit_commit_ready: true
- audit_provider: codex_plugin
