# B02: transcript judge_result 补填脚本

## 任务信息
- task_id: B02
- phase: 1
- module: eval-judge
- verification: L2

## 实现文件
- `backend/tests/eval/backfill_judge.py` -- 补填脚本主文件
- `backend/tests/eval/test_backfill_judge.py` -- 单元测试（6 个测试用例）

## 验收条件达成

| # | 验收条件 | 状态 |
|---|----------|------|
| 1 | backfill_judge.py 在 tests/eval/ 下 | done |
| 2 | 读取指定日期目录，过滤 judge_result=null | done |
| 3 | 调用 LLMJudge(use_real_llm=True) 补填，写入原文件 | done |
| 4 | --dry-run 模式只输出统计不修改文件 | done |
| 5 | --limit 参数控制单次处理条数 | done |

## 测试结果

```
tests/eval/test_backfill_judge.py::test_backfill_writes_judge_result PASSED
tests/eval/test_backfill_judge.py::test_backfill_skips_already_filled PASSED
tests/eval/test_backfill_judge.py::test_dry_run_does_not_modify PASSED
tests/eval/test_backfill_judge.py::test_backfill_handles_judge_error PASSED
tests/eval/test_backfill_judge.py::test_limit_respected PASSED
tests/eval/test_backfill_judge.py::test_backfill_no_transcripts PASSED

6 passed in 0.03s
```

eval 全量: 334 passed in 0.28s

## Code Review

- 复用 TranscriptStore API（list/save/load），未自行实现文件读写
- 复用 LLMJudge（is_degraded / degraded_reason 属性），支持注入自定义 judge 实例
- 幂等性：通过 `judge_result is None` 过滤，重复运行不会重复评分
- --dry-run 条件判断正确，测试验证不写文件
- 异常处理：单条失败不阻塞整体流程，记录错误继续下一条
- argparse 设计合理，含 --date / --dry-run / --limit / --transcripts-dir 四个参数

## Quality Gates
- code_review_status: pass
- code_review_provider: codex_plugin
- audit_status: done
- audit_commit_ready: true
- audit_provider: codex_plugin

## 用法示例

```bash
# 补填 2026-04-29 全部缺失记录
uv run python -m tests.eval.backfill_judge --date 2026-04-29

# 先 dry-run 查看统计
uv run python -m tests.eval.backfill_judge --date 2026-04-29 --dry-run

# 限制每次 10 条
uv run python -m tests.eval.backfill_judge --date 2026-04-29 --limit 10
```
