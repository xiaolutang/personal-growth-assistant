# F03: 首页升级 -- 晨报卡片 + 快捷录入栏

## 完成时间
2026-05-07

## 变更文件

### 新建
| 文件 | 说明 |
|------|------|
| `mobile/lib/models/morning_digest.dart` | 晨报数据模型（MorningDigest, DailyFocus, MorningDigestTodo 等） |
| `mobile/lib/widgets/morning_digest_card.dart` | 晨报卡片组件（三态：loading/loaded/error，骨架屏 shimmer） |
| `mobile/test/widgets/morning_digest_card_test.dart` | 晨报卡片 Widget 测试（8 个测试用例） |

### 修改
| 文件 | 说明 |
|------|------|
| `mobile/lib/providers/today_provider.dart` | TodayState 增加 morningDigest 字段；新增 MorningDigestState/MorningDigestStatus；TodayNotifier 新增 loadMorningDigest() 方法；loadData() 并行调用晨报 |
| `mobile/lib/pages/today_page.dart` | 顶部添加 MorningDigestCard；底部添加快捷录入栏（TextField + IconButton → createInboxEntry）；布局改为 Column(Expanded(ListView) + QuickInputBar) |
| `mobile/test/providers/today_provider_test.dart` | 扩展测试覆盖 morningDigest 字段和 MorningDigest 模型 fromJson |

## 验收标准覆盖

| # | 验收标准 | 状态 |
|---|---------|------|
| 1 | TodayPage 顶部显示晨报卡片 | PASS |
| 2 | 晨报卡片：日期、AI 摘要、关键建议 | PASS |
| 3 | 晨报加载中 → 骨架屏/shimmer | PASS |
| 4 | 晨报加载失败 → 不显示（不阻塞页面） | PASS |
| 5 | 晨报为空 → 不显示卡片 | PASS |
| 6 | 底部快捷录入栏：内联输入框 + 发送按钮 | PASS |
| 7 | 输入 → POST /entries {category: inbox} → 清空 + SnackBar | PASS |

## 测试覆盖

| 测试用例 | 说明 |
|----------|------|
| error state renders nothing | 晨报加载失败不显示 |
| initial state renders nothing | 初始状态不显示 |
| loading state shows skeleton | 骨架屏动画 |
| loaded with no data renders nothing | 无数据用户不显示 |
| loaded with data shows content | 完整数据渲染 |
| shows daily focus | 每日聚焦卡片 |
| shows pattern insights | 模式洞察列表 |
| ai suggestion renders as markdown | Markdown 渲染 |
| MorningDigest.fromJson parses all fields | 全字段解析 |
| MorningDigest.fromJson handles missing optional fields | 缺失字段兼容 |
| TodayState copyWith morningDigest | 状态更新 |
| MorningDigestState copyWith | 晨报状态管理 |

## 质量门

- `flutter analyze` (变更文件): **0 warnings**
- `flutter test` (新/改测试): **23 passed, 0 failed**
- `flutter test` (全量): **448 passed, 1 pre-existing failure** (quick_actions_test.dart pumpAndSettle timeout, 非 F03 引入)

## 架构合规

- 状态管理：Riverpod (TodayNotifier + MorningDigestState)
- 复用 EntryListNotifier.createInboxEntry 方法
- Provider 无 UI 导航逻辑
- Widget 不直接调 ApiClient（通过 ref.read(notifier) 调用）
- 晨报失败不阻塞主页面（独立 try-catch）
